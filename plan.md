# 바로가 — 개인 선호도를 반영한 수단간 연계 경로탐색 알고리즘 · 구현 계획

> 알고리즘의 설계 결정·실험·기각의 흐름은 **[method.md](method.md)** 에 정리한다(요약은 §0). 이 문서는 설계와 로드맵만 둔다.
> 데이터 파이프라인 실행은 [data/README.md](data/README.md), 데모 웹 실행은 [web/README.md](web/README.md).

- **작성일**: 2026-09-10 / **최종 수정**: 2026-09-16

---

## 1. 프로젝트 개요

기존 상용 지도 앱은 대중교통 기반 길찾기만 제공하고, **대중교통 + 택시/공유 이동수단을 결합한 통합 경로는 제공하지 않는다.** 정해진 노선·배차 간격 안에서만 탐색하므로 크게 우회하거나 환승 대기가 과도해지는 비효율이 발생한다.

> "현재 위치에서 A지점까지는 직행 버스가 없으니 택시를 이용하고, A지점부터 목적지까지는 다시 대중교통으로 이동한다"

이 판단은 현재 숙련된 이용자의 경험과 직관에만 의존한다. **본 과제는 이 판단을 알고리즘으로 자동화한다.**

---

## 2. 확정된 범위

| 항목 | 결정 | 비고 |
|---|---|---|
| **대상 지역** | 미확정. **경기도 등 대중교통 밀도가 낮은 외곽 지역** 지향 | 정적 데이터는 서울·경기 전역을 갖췄다. 버스는 당분간 서울·경기 노선만(인천 노선은 나중) |
| **대상 수단** | **택시 우선** → 완성 후 공유 PM 확장 | PM 은 자전거 경로 조회로 A·B 가 같은 구조로 돌게 설계에만 반영 |
| **결과물** | 알고리즘 + **데모 웹앱** | FastAPI 백엔드 + 빌드 없는 순수 JS, Leaflet + VWorld 배경지도 |
| **대중교통 라우팅** | 카카오맵 대중교통 API 를 **블랙박스**로 | 바닥부터 라우터를 만들지 않는다 |
| **택시 시간·요금** | 확정값은 카카오모빌리티 자동차 경로 **응답값** (택시 예상요금 + 통행료) | 가지치기에는 오프라인 근사(시간 236초 + 178초/km, 공개 요금 체계 2계수)를 순위용으로만 쓴다 (method.md R13, E18) |
| **선호** | 실험 CLI 는 길찾기마다 **시간 중시 / 균형 / 비용 중시** 중 선택, 데모 웹은 **시간 중시로 고정** | 단계마다 VOT 하나. 진술선호 조사는 하지 않는다 (R14) |

---

## 3. 핵심 접근: 문제 재정의

가장 어려운 부분은 **어디서 택시로 갈아탈지(= 앵커 지점)를 어떻게 고를 것인가**다. 수도권 정류소는 5만 개 이상이고 API 쿼터는 유한하다. 전수 조합은 불가능하고, 무작위로 고르면 좋은 해를 놓친다.

> **본 과제의 알고리즘 기여 = "제한된 API 쿼리 예산 하에서 유망한 앵커를 선택하는 휴리스틱"**

- 설계·평가 가능한 고유 알고리즘이 된다
- 쿼터 제약(§10-4)이 실제로 존재하므로 인위적 가정이 아니다
- "쿼리 수 대비 해 품질" 형태의 명확한 실험이 가능하다 (§11)

---

## 4. 하이브리드 경로 유형 분류

| 유형 | 형태 | 발생 빈도 | 난이도 | 설명 |
|---|---|---|---|---|
| **A. First-mile** | 택시 → 대중교통 | 높음 | 낮음 | 출발지가 역세권 밖, 첫 버스가 우회 |
| **B. Last-mile** | 대중교통 → 택시 | 높음 | 낮음 | 목적지가 대중교통 사각지대 |
| **C. Mid-leg (환승 스킵)** | 대중교통 → 택시 → 대중교통 | 낮음 | **높음** | 과제의 헤드라인 케이스 |
| **D. Gap-filling** | 긴 대기/도보를 택시로 대체 | 중간 | 중간 | 실시간 데이터가 실제로 쓰이는 지점 |
| **E. 기준선** | 전구간 대중교통 / 전구간 택시 | — | — | 비교용 상·하한 |

### 구현 순서

```
1단계  A · B 로 파이프라인 완성      → 검증: 경기 외곽 OD에서 시간 절감 경로가 실제로 나오는가
2단계  D 추가 (대기시간 연결)        → 검증: 대기시간 반영 전/후 추천 결과가 달라지는가
3단계  C 구현 (차별점)              → 검증: 우회·환승대기 케이스가 자동 탐지되는가
4단계  공유 PM 확장                 → 검증: 자전거 경로 API로 A·B가 동일하게 동작하는가
```

A·B가 실질 편익의 대부분을 차지하고 쿼리도 저렴하다. C를 먼저 붙들면 파이프라인이 돌지 않는 상태로 시간을 소모하므로 순서를 지킨다.

---

## 5. 앵커 선택 전략

**채택 조합: (a) + (b) + (c).** (a)·(b)와 C 재탐색은 구현했고 (c)는 아직이다. (a)/(b)/(c)/무작위 격자를 쿼리 수 대비 해 품질로 비교하는 ablation을 실험에 포함한다(§11).

### (a) 기준 경로 섭동

기준 대중교통 경로의 구간 하나를 택시로 치환한다. 후보 수 = 구간 수(3~5개)로 매우 저렴하다. 경로 위 중간 정류소 좌표(구현됨)를 쓰면
구간 경계가 아닌 정류소에서도 자를 수 있다. → **A·B·D 유형에 대응.**
D(긴 대기·환승 도보 한 구간을 택시로 메우기)는 구현했다 — 나머지 구간이 기준 경로에 있어 검증이 자동차 1콜이다 (method.md §3.7)

**C(환승 스킵)** 는 기준 경로를 잘라 붙이지 않는다 — 경로가 목적지에서 멀어지기 시작하는 정류장에서 내려 택시로 목적지까지 가거나,
그 정류장을 출발지로 둔 (b)로 새 앵커를 찾는다. 가운데 구간 대체는 쓸 데가 거의 없었고 원래 노선 재합류는 배차만큼 다시 기다린다 (method.md §3.7, R28)

### (b) 직행 노선 역추적 — ★ 핵심 전략

```
입력: 출발지 O, 목적지 D, 선호 단계(→ VOT)

1. 기준 경로:  TransitAPI(O, D) → R0 (대안 경로 다수 포함)
2. D 주변 반경 r(400m, 비면 800·1,500m) 내 정류소/역 집합  S_D
3. S_D 를 지나는 노선 집합  L_D                    <- 노선 순서표, 오프라인
4. 각 노선 l in L_D 의 상류(같은 노선에서 순번이 작은) 정류소 중,
   O 에서 택시로 T_max(15분) 이내 도달 가능한 정류소 P    <- 택시 시간 근사, 오프라인
   -> 앵커 후보 (l, P)
5. 오프라인 근사 스코어 = 일반화 비용
   택시시간(O->P) + 배차간격/2 + 승차시간(P->D) + 하차도보 + (택시요금 + 대중교통요금) / VOT
   -> 상위 N개(5~10)만 잔존 (노선마다 2개까지)
6. 온라인 검증: 각 앵커에 대해 CarAPI(O->P: 시간·요금) + TransitAPI(P->D)
7. 일반화 비용 계산 -> Pareto 필터 -> 상위 3~4개 제시
```

- A 방향의 요청당 대중교통 쿼리 = `1 + N`. 유형마다 콜 수가 다르다(§10-4). 1~5단계는 전부 오프라인이라 공짜
- **대칭 버전**(O를 지나는 노선의 하류 정류소 → 택시로 D)이 B 유형을 커버 — 반경만 맞바꾼 같은 알고리즘으로 구현했다
- 후보 집합이 작으면서도 **"환승 없이 목적지로 데려다주는 노선"에 정확히 조준**된다
- 입력: 버스 노선–정류소 순서표·도시철도 역 순서표·배차간격표·택시 근사를 모두 갖췄다(§9.2, method.md E18)
- 점수에 요금이 없으면 상위 후보가 택시 도달 경계 한 점으로 붕괴한다 (method.md R23)
- 수요응답형 버스(똑버스·DRT)는 정해진 노선·배차가 없어 후보로 쓰지 않는다 (method.md R29)

### (c) 허브 기반 — 보완재

O–D 직선을 초점으로 하는 타원(우회 허용계수 α) 안의 주요 환승 거점만 앵커로 사용. (b)에 저렴하게 덧붙인다. 아직 구현하지 않았다.

---

## 6. 시스템 아키텍처 — 2-tier "오프라인 탐색 / 온라인 검증"

```
[ Tier 1 · 오프라인 · 무제한 · 무료 ]  ── 탐색 & 가지치기
  · 정류소·역 좌표 DB (버스 50,019 · 역 685)          -> 앵커 후보 생성
  · 버스 노선–정류소 순서표 · 도시철도 역 순서표        -> (b) 상류·하류 정류소
  · 택시 시간·요금 근사 (고정+거리비례 · 공개 요금 체계) -> 후보 가지치기
  · 배차간격표 (버스 노선·요일, 도시철도 역·시간대)    -> 대기시간 근사
  => 수천 개 후보를 수십 개로 압축
                    |
                    v
[ Tier 2 · 온라인 · 쿼터 제한 · 정확 ] ── 검증 & 제시
  · 카카오맵 대중교통 경로 조회      -> 실제 노선·환승 (1,000건/일)
  · 카카오모빌리티 자동차 길찾기      -> 택시 시간·요금, 실시간 교통 (10,000건/일)
  · 실시간 도착 정보 (버스 두 BIS · 도시철도 1종) -> 실제 대기시간
  => 상위 N개만 정확히 평가
```

탐색 단계에서는 쿼터 걱정 없이 넓게 탐색하고, 정확도가 필요한 곳에만 API를 쓴다. 대상 지역을 좁히더라도 정적 데이터를 다시 받을 필요는 없다.

배포 구조는 **웹 프론트 + 백엔드** 하나다. Tier 1 데이터와 모든 API 키는 백엔드에 둔다(§9.1).

---

## 7. 선호도 모델 및 결과 제시

### 7.1 일반화 비용

```
GC = 총소요시간 + 총비용 / VOT          (VOT = 시간가치, 원/분)
```

### 7.2 선호 단계

- 사용자가 길찾기마다 **시간 중시 / 균형 / 비용 중시** 중 하나를 고른다. 단계마다 VOT 값 하나를 둔다
- 데모 웹은 시간 중시로 고정하고 기준선을 가장 빠른 대중교통 경로로 둔다 — 택시를 섞는 사람은 돈보다 시간이 급하다
- 단계별 값(원/분)은 잠정값(시간 500 / 균형 300 / 비용 150)으로 돌리고 있다. 확정은 §12

### 7.3 결과 제시 규칙 — ★ UX 핵심

**단일 최적해로 붕괴시키지 않고 Pareto 집합을 제시한다.** 개인화는 *정렬·필터*의 역할로 둔다.

- **Pareto(skyline) 필터**: (총 소요시간, 총 비용) 최소. 필요시 (환승 횟수, 도보 거리) 추가
- **절감 효율을 "원/분" 단일 숫자로 표시**

  > 택시 6,000원 추가 → 14분 절약 → **분당 429원**
  > 고른 선호 단계의 VOT가 300원/분이면 → "비추천" 표시

- **다양성 제약**(실험 CLI): 최대 4개로 자르되 구조적으로 다르게 — 유형(전구간 대중교통 · A · B · C · D)마다 하나씩 먼저 채운다.
  데모 웹은 자르지 않고 일반화 비용 순으로 모두 보여 주며 Pareto 여부와 추천을 표시한다.
  전구간 택시는 비교용 상한이고 후보로 만들지 않는다
- 택시를 쓰는 최소 기준(§8)을 넘지 못한 후보는 Pareto 에 넣기 전에 뺀다
- 대중교통 요금이 범위(최소–최대)로 오면 정렬·비교 모두 낮은 쪽을 쓴다
- 도보 거리 = 환승 도보 + 출발지→첫 승차·마지막 하차→도착지 직선거리(블랙박스가 첫·끝 도보를 주지 않는다)

---

## 8. 대기시간 및 불확실성 모델

채택 API에 출발 시각 파라미터가 없어(§10-2) 대기시간을 직접 추정해야 한다.

- **실시간 도착 정보가 있으면 그것을 쓰고, 없으면 배차간격으로 추정**한다(가장 단순한 형태 = 배차간격/2). 원천은 확정했고(method.md E14), 보정된 예측 초 → 남은 정류소 수 → 정적 배차의
  세 층으로 떨어진다(method.md §7.5). 도시철도 배차는 앞뒤 1시간을 합친 3시간 창으로 세고, 1역 전 열차(전역 출발·진입·도착)는
  남은 역 수 1로 읽는다(method.md E14, E20). 남은 것은 기대 대기 상한·첫·막차 시간대·신선도 임계값·급행 구간 대기다(method.md §0.3)
- "다음 버스 14분 후"가 곧 택시 개입의 타깃 (D 유형)
- **택시 구간 시간** = 호출 대기 90초 + 이동 시간(ETA) + (내려서 차를 타면) 연결 버퍼 60초.
  처음 둔 `택시ETA × 1.2 + 3분` 에서 ETA 비례분을 뺐다 — 대중교통 구간에는 붙이지 않는 흔들림을 택시에만 붙이면 택시가 한쪽으로 불리해진다
- **택시를 쓰는 최소 기준**: 택시 구간 직선 1.1 km(표준형 기본거리 1.6 km 를 직선으로) 이상, 기준선보다 5분 이상 빠를 것
- **(확장)** 이동 중 재평가 — "지금 택시 타면?" 이벤트 기반 재계산

---

## 9. 데이터 및 API

### 9.1 채택 API

**대중교통 — 카카오맵 REST API** (심사 불필요. 앱 → 제품 설정 → 카카오맵 → 사용 설정)

```
GET https://dapi.kakao.com/v2/routing/publictraffic    일 1,000건 무료 / 초과 10원
GET https://dapi.kakao.com/v2/routing/bicycle          일 1,000건 무료   <- PM 확장에 직결
```

- **한 번의 호출로 대안 경로를 최대 15개** 준다 → 기준 경로 확보가 1콜로 끝난다
- 정류소는 **이름만** 준다(좌표·ID 없음). 출발 시각·대기시간·경유지 파라미터도 없다 (method.md E2, E9)
- **키는 `[앱] > [플랫폼 키] > REST API 키` 하나만 쓴다.** JavaScript 키·네이티브 앱 키는 불필요하다
- ⚠️ **REST API 키는 클라이언트에 두지 않는다** — 백엔드에 보관하고 프론트는 자체 API를 경유한다
- 무료 쿼터는 **개발자 계정당 "첫 번째로 활성화한 앱"에만** 적용 → 앱 등록을 따로 만들지 않는다

**택시 — 카카오모빌리티 자동차 길찾기 API**: 일 10,000건 무료, 실시간 교통 반영. 택시 예상요금·통행료를 함께 줘 요금에 그대로 쓴다. 쿼터 병목 아님.

**장소·주소 검색 — 카카오 로컬 API** (데모 웹의 출발·도착 입력): 키워드·주소 검색 각 일 100,000건 무료. 결과는 짧은 캐시도 금지.

**공공 지오코딩·행정경계·배경지도 — VWorld** (키: `.env`의 `VWORLD_API_KEY`)

- 카카오와 달리 결과 저장 제약이 없어 **영속 데이터에 쓴다** — 역 좌표 보정, GTX-A 역 좌표
- 배경지도는 **Leaflet 1.9.4 + VWorld 기본지도**(다크 모드는 야간지도, OSM 대체). 카카오맵 JS SDK 는 쓰지 않는다(경로는 좌표 배열이라 어느 지도에나 그릴 수 있고, 카카오 공식 답변(2026-08-18)이 타사 지도 조합을 허용). 타일은 백엔드 프록시로 받아 키를 숨긴다

**공공데이터**: 정류소·역·노선 원천(§9.2). 경기 GBIS 기반정보는 `.env` 의 `DATA_GO_KR_API_KEY` 로 1콜 받는다.

**실시간 도착 정보**: 버스는 경기·서울 두 BIS(정류소 번호로 바로 조회), 도시철도는 서울 실시간 도착 1종(역명 정확 일치라 매핑표
`subway_live_stations.csv` 로 685역 중 642역, 22 노선군 중 19개) — method.md E14. Tier 1 택시 시간·요금 근사는 오프라인 계산으로 확정했다(method.md E18)

### 9.2 정적 데이터와 정제 산출물

원본은 `data/csv/`(커밋 안 함)·`data/external/`(스크립트가 받음)에 두고 수정하지 않는다. 산출물은 `data/processed/`(언제든 다시 만든다).
명령·출처·라이선스는 [data/README.md](data/README.md).

| 데이터 | 원천 | 산출물 | 규모 |
|---|---|---|---|
| 버스 정류소 | 국토교통부 전국 버스정류장 위치정보 (서울·경기 BIS 레코드는 경기 GBIS 정류소 · 서울 OA-15067 최신 파일로 교체) + 좌표 보정 4곳 | `bus_stops.csv` | 50,019곳 |
| 도시철도역 | 전국도시철도역사정보 표준데이터 + GTX-A 참조표 + 좌표 보정 3역 | `subway_stations.csv` | 685역 |
| 도시철도 역 순서 | 전국도시철도노선정보 표준데이터 `정거장구성` + 보정표 26건 | `subway_line_seq.csv` | 39 순서 (22 노선군, 지선 포함) |
| 버스 노선–정류소 순서 | 서울 OA-1095 노선별 정류소 + 경기 GBIS 기반정보 (한강버스 제외) | `bus_route_stops.csv` | 3,852 노선 · 235,938행 |
| 버스 배차간격 | 경기 GBIS 노선 파일(요일 유형별 최소·최대·첫막차) + 서울 노선 목록(대표값·첫막차) | `headway_bus.csv` | 3,823 노선 · 10,934행 |
| 도시철도 배차간격 | KTDB 대중교통 GTFS (2025년 3월 평일 1일) — (역, 노선군, 방향, 시간대)별 운행 수(앞뒤 1시간을 합친 3시간 창) | `headway_rail.csv` | 683역 · 24,801행 |
| 행정경계 | vuski/admdongkor (원천 통계청 SGIS) | `admin_sgg.geojson` | 서울 25구 + 경기 31시군 |

정제 원칙 (근거는 method.md §4, E3–E8):

- 버스 정류소 키 = 정류장번호 뒤 9자리 — 서울·경기 BIS 이중 등록은 병합하고, 길 양쪽 상·하행은 보존한다. 노선 원천의 정류소 ID 와 변환 없이 맞물린다
- 지역 = 좌표 + 행정경계 폴리곤 (원천 도시명은 운영 주체라 쓰지 않는다)
- 도시철도역 노선명은 운영 노선명 → 노선군(승객 노선) 매핑, 걸친 구간은 역 단위 보정
- 원천 오류(틀린 좌표·빠진 구간·옛 역명·잘못 들어간 행)는 `data/ref/` 참조표에 한 줄씩 고치고, 원천이 바뀌어 보정이 맞지 않으면 파이프라인이 멈춘다

---

## 10. 제약사항과 대응

### 10-1. 카카오 응답에 정류소 좌표가 없다 — 이름만 제공

> **대응 (구현됨)**: 정류소 공간 DB(§9.2) + 이름→좌표 매칭 + 노선·역 순서표 접지. 경기 외곽 5 OD 에서 승·하차 버스 발견률 100%(모호 7 → 0),
> 경로 위 정류소 버스 828곳 · 지하철 92곳 전부 DB 위치(method.md E13). 같은 이름 정류소의 진행 방향은 노선 운행 순서로 가르고,
> 토막 순서가 뒤바뀌거나 개명된 이름은 토막 집합 일치·노선 순서 구제로 찾는다(method.md §5.5–5.6, E16). 부분 이름 매칭은 기각했다(R16)

### 10-2. 출발 시각 파라미터가 없고, 대기시간도 없다

API는 시간대 무관한 평균 소요시간을 준다. "택시 10:23 도착 → 다음 버스 10:31 → 8분 대기" 같은 시간 정합성 판단을 API만으로는 못 한다.

> **대응**: 대기시간 모델(§8) — 실시간 도착 정보, 없으면 배차간격.

### 10-3. 카카오 응답 데이터 저장·캐싱 금지 (실시간 호출만 허용)

> **대응**:
> - **파생 지표만 저장** (절감 시간, 비용 차, 원/분 등 — 원본 응답이 아닌 계산 결과)
> - **영속 데이터는 카카오가 아닌 소스에서 얻는다** — 정류소·역·노선은 공공데이터, 지오코딩·행정경계는 VWorld. 카카오는 조회 시점의 일회성 호출로만 쓴다

### 10-4. 대중교통 쿼터 1,000건/일이 바인딩 제약

데모 웹(검증 5개)은 요청당 대중교통 최대 12콜(기준 1 + 검증 최대 5 + C 최대 6)이라 하루 약 80건 이상 검색 — [경로 검색] 결과를 넘기면 기준 1콜은 들지 않는다. (a)의 B·D 는 나머지 구간이 기준 경로에 있어 자동차만 쓴다. 데모용으로는 충분, 대량 실험엔 빡빡하다. 1,000건/일 × 30일 = 약 **OD 2,000쌍**을 한 달에 걸쳐 점진적으로 수집하면 평가 표본으로 충분하다. 이 제약이 §3 앵커 선택 휴리스틱의 존재 이유다.

### 10-5. 경유지 파라미터가 없다

> **대응**: 연계 경로는 **구간을 따로 호출해 이어 붙인다** — 택시 O→P + 대중교통 P→D. §5 (b)의 `1 + N` 설계가 이 전제와 맞는다. C 유형은 요청당 정류장 2곳 × (택시로 목적지 1 + 앵커 3)을 따로 확인한다.

---

## 11. 평가 설계

- **기준선(baseline)**: 대중교통 전용 최적 경로
- **지표**: 시간 절감(분) / 비용 증가(원) / 절감 효율(원/분) / Pareto 개선율 / **하이브리드가 비지배해인 OD 비율**(핵심)
- **OD 표본**: (도심↔도심 / 도심↔외곽 / 외곽↔외곽) × (피크 / 평시 / **심야**) 계층 표본. 심야에 편익이 가장 클 것으로 예상되므로 반드시 포함
- **Ablation**: 앵커 전략별 (a)/(b)/(c)/무작위 격자 → **쿼리 수 대비 해 품질** (= §3 기여 증명)
- **현장 검증**: 대표 경로 3~5개를 실제로 타고 시간 측정
- **사용자 스터디**: 제시된 대안의 선택률 / 만족도
- 접지 단계 실측의 공통 표본: 경기 외곽 5 OD (동탄역→경기도청, 수지구청→서현역, 운정→대화역, 다산→구리시청, 배곧→안산 중앙역)

---

## 12. 미결정 사항

- [ ] **대상 지역 확정** — 매칭률과 경로 품질을 보고 좁힌다
- [x] **Tier 1 택시 시간 근사 방식** — **고정 비용 + 직선거리 비례**(236초 + 178초/km)로 확정. 여기에 공개 요금 체계를 옮긴
  2계수 요금 근사를 더해 가지치기 점수를 일반화 비용으로 만든다 (method.md E18, R23, R25).
  값은 비혼잡 시각(2026-09-14 19:37) 사용 구간(직선 1–6 km) 150 표본으로 맞췄다
- [ ] **선호 3단계의 VOT 값** (원/분) — 잠정값(시간 500 / 균형 300 / 비용 150)을 `algo/anchors.py` 에 두고 돌리는 중
- [x] **배차간격·실시간 도착 정보의 원천** — 버스 배차는 노선 원천, 도시철도는 GTFS, 실시간은 두 BIS + 도시철도 1종 (method.md E14)
- [ ] **대기시간 모델의 남은 규칙** — 기대 대기 상한, 첫·막차 시간대 배차, 실시간 신선도 임계값, 급행 구간 대기, 대기를 모르는 경로의 비교 (method.md §0.3)
- [x] 같은 이름 정류소 중 택시 하차점 선택 규칙 (진행 방향 판정) — 노선 운행 순서와 정류소 개수 간격으로 가른다 (method.md §5.5, E13)
- [ ] 카카오 대중교통 API의 장거리·시외 경로 처리 방식 (실측 필요)
- [ ] 유료 쿼터 사용 여부 및 예산 (필요시 10원/건)

---

## 13. 다음 단계

**끝난 것** (근거와 수치는 method.md §0.4 · §9)

- 정제 파이프라인: 버스 정류소 50,019 (두 BIS 최신 정류소 파일 반영) · 도시철도역 685 (GTX-A 보강, 원천 좌표 결함 3역 보정)
- 카카오 API 실측 · 승·하차 매칭 · 경로 위 모든 정류소 위치 복원 · 노선 순서표 접지(진행 방향 판정, 모호 7 → 0) · 토막 집합·노선 순서 구제 (경기 외곽 5 OD)
- 전략 (b)의 정적 입력: 도시철도 역 순서표 39 순서, 버스 노선–정류소 순서표 3,852 노선 (서울 OA-1095 + 경기 GBIS)
- 데모 웹: 대중교통·택시 경로 검색, 승·하차·경로 위 정류소 표시, 주소·장소 검색 (web/)
- **앵커 선택 (a) + (b) 구현** (`algo/`): 후보 생성 OD 당 8–22 ms, 경기 외곽 5 OD 에서 온라인 검증까지 한 바퀴.
  (b)는 **A·B 두 방향**(택시로 타러 가기 / 내려서 택시)을 반경만 맞바꿔 같은 알고리즘으로 돈다.
  가지치기 점수를 일반화 비용으로 만들어, 상위 후보가 택시 도달 경계로 붕괴하고 비싼 노선이 상위를 차지하던 문제를 잡았다
  (method.md §3.7, E18)
- **D 유형(공백 메우기)**: 기준 경로의 긴 대기(386초 이상 — 호출 대기 90 + 택시 고정 236 + 버퍼 60)·긴 환승 도보 한 구간을 택시로 메운다. 동탄역 → 경기도청에서 66.8분 → 51.1분 경로가
  검증을 거쳐 Pareto 에 들었다 (method.md §3.7)
- **첫 승차 대기 실시간**: 기준선과 D 의 첫 대기를 실시간 도착으로 매긴다 — 정류장까지 걷는 동안 지나가는 차는 빼고, 도시철도는
  '○○방면' 으로 방향을 가른다 (method.md §3.7)
- **C 유형**: 경로가 돌아가기 시작하는 정류장(목적지까지 거리가 다시 늘기 시작하는 곳)에서 내려 택시로 목적지까지 · 직행 노선 앵커로
  경로를 다시 찾는다. 동탄역 → 경기도청에서 구성에서 내려 광교 쪽 버스로 갈아타는 경로가 79.3분 → 58.8분 (method.md §3.7)
- **택시 규칙**: 호출 대기 90초 + 이동 + 연결 버퍼 60초(ETA 비례분 제거), 최소 직선 1.1 km · 기준선보다 5분 이상 빠를 것
- **대기 입력 보강**: 1역 전 열차를 도착 코드로 읽기(method.md E20), 도시철도 배차를 3시간 창으로 세기(E14), 배차를 모르는 노선의 대기 900초(버스 평일 배차 중앙 30분의 절반)
- **수요응답형 버스(똑버스·DRT) 제외**: 정해진 노선·배차가 없어 (b)·C 후보에서 뺀다 (method.md R29)

**다음**

```
1. 시간대별 택시 계수: 출퇴근·심야 표본으로 기울기가 얼마나 갈리는지 잰다 (algo.cli calibrate --min-km 1 --max-km 6)
   -> 검증: 비혼잡 계수 하나로 두어도 상위 8 앵커가 유지되는가 (겹침이 크게 떨어지면 시간대별 계수를 둔다)

2. 대상 지역 확정
   -> 검증: 해당 지역에서 매칭률과 경로 품질이 실험에 쓸 만한가

3. (c) 허브 추가 + ablation: 쿼리 수 대비 해 품질 (§11)

4. 실시간 첫 대기의 남은 규칙: 신선도 임계값, 당역 '도착' 열차의 정차 시간, 택시로 정류장에 닿는 A 의 첫 대기, 전역 출발 열차의 낙관 편향 (§8)

5. 대기를 모르는 경로의 비교: 비교 시간이 모르는 대기를 0 으로 세어 그 경로가 유리해진다 — 후보 점수(900초)와 잣대를 맞춘다 (method.md §0.3)

6. 공유 PM 확장 (C 유형은 구현됨)
```

---

## 14. 참고 링크

**API 문서**

- [카카오맵 REST API](https://developers.kakao.com/docs/ko/kakaomap/rest-api) / [쿼터 및 요금](https://developers.kakao.com/docs/ko/getting-started/quota)
- [앱 설정 — 앱 키 위치](https://developers.kakao.com/docs/ko/app-setting/app) / [앱 키 개편 안내 (2025-12-03)](https://developers.kakao.com/docs/ko/getting-started/app-key-migration)
- [카카오모빌리티 — 가격 및 문의](https://developers.kakaomobility.com/price/)
- [대중교통 API는 도보 상세경로 미포함 (공식 답변)](https://devtalk.kakao.com/t/api/150809)
- [응답 데이터 저장 금지 (공식 답변)](https://devtalk.kakao.com/t/api/151435)

**정적 데이터셋** (모두 사용 중 — 파일 이름·받는 법은 data/README.md)

- [국토교통부_전국 버스정류장 위치정보](https://www.data.go.kr/data/15067528/fileData.do)
- [전국도시철도역사정보표준데이터](https://www.data.go.kr/data/15013205/standard.do)
- [전국도시철도노선정보표준데이터](https://www.data.go.kr/data/15013203/standard.do)
- [서울시 버스노선별 정류소 정보 (OA-1095)](https://data.seoul.go.kr/dataList/OA-1095/F/1/datasetView.do)
- [서울시 버스정류소 위치정보 (OA-15067)](https://data.seoul.go.kr/dataList/OA-15067/S/1/datasetView.do)
- [서울특별시_노선정보조회 서비스 (15000193)](https://www.data.go.kr/data/15000193/openapi.do)
- [서울시 지하철 실시간 도착정보 역 목록 (OA-12764)](https://data.seoul.go.kr/dataList/OA-12764/F/1/datasetView.do)
- KTDB 국가교통DB 대중교통 GTFS 기반정보 (2025년 3월 평일 1일, 자료신청)
- 경기도 버스정보 기반정보 — 공공데이터포털 15080658
- [vuski/admdongkor 행정경계](https://github.com/vuski/admdongkor) (CC BY 4.0)

**보류 (재검토용)**

- [ODsay LAB](https://lab.odsay.com/guide/guide) — 좌표·요금 다 제공하나 무료 30건/일
- [TMAP 대중교통 API](https://transit.tmapmobility.com/) — 종량 0.88원/건, 유료 교차검증용

**선행 연구**

- 장지인, 정서윤, 이찬우. *MaaS 환경에서 택시–대중교통 연계를 고려한 하이브리드 경로 탐색 모델 연구.* 한국정보기술전략혁신학회 학술대회 논문집
  - 차별점: 실시간 교통 데이터 미반영 / 가중치 도출 방법론 부재 / 공유PM 확장 없음
- 신지유, 유기윤. (2025-05-22). *공유 자전거 따릉이를 포함한 다중모드 교통 네트워크의 실시간 최적 경로 탐색.* 대한공간정보학회 학술대회, 강원.
- A. Xu et al., "THAN: Multimodal Transportation Recommendation With Heterogeneous Graph Attention Networks," *IEEE T-ITS*, vol. 24, no. 2, pp. 1533-1543, Feb. 2023, doi: 10.1109/TITS.2022.3221370
