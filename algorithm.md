# 바로가 — 하이브리드 경로 알고리즘 (현재 구현)

> 지금 코드가 **어떤 연계 유형을 어떻게 만들고 견주는지**만 적는다. 설계가 왜 이렇게 됐는지(실험·기각)는 [method.md](method.md),
> 로드맵은 [plan.md](plan.md) 에 있다. 알고리즘은 [algo/anchors.py](algo/anchors.py)(후보 생성) · [algo/compare.py](algo/compare.py)(비교·제시) ·
> [algo/cli.py](algo/cli.py)(실험 실행·검증)에 있고, 데모 웹은 [web/backend/app/hybrid.py](web/backend/app/hybrid.py) 가 같은 함수로 같은 흐름을 돈다(§6).

- **기준일**: 2026-09-16

---

## 1. 유형 한눈에

| 유형 | 형태 | 만드는 방식 | 택시 구간 | 검증 쿼터(후보당) |
|---|---|---|---|---|
| **E 기준선** | 전 구간 대중교통 | 카카오 대중교통 경로 1콜(대안 최대 15개) | 없음 | 요청당 대중교통 1 |
| **A 첫 구간 택시** | 택시 → 대중교통 | (b) 직행 노선 역추적 · (a) 기준 경로 섭동 | 출발지 → 승차 앵커 | 자동차 1 + 대중교통 1 |
| **B 끝 구간 택시** | 대중교통 → 택시 | (b) 역추적 대칭판 · (a) 섭동 | 하차 앵커 → 목적지 | (b) 자동차 1 + 대중교통 1 · (a) 자동차 1 |
| **D 공백 메우기** | 긴 대기·환승 도보 한 구간을 택시로 | 기준 경로에서 공백 탐지 | 경로 위 정류장 → 정류장 | 자동차 1 |
| **C 재탐색** | 돌아가기 시작하는 정류장에서 내려 다시 찾기 | 기준 경로에서 우회 지점 탐지 → 재탐색 | 그 정류장 → 목적지 또는 새 앵커 | 목적지까지 자동차 1 · 앵커 자동차 1 + 대중교통 1 |

후보의 `strategy` 는 만든 전략이다 — (a) 기준 경로 섭동 `a` · (b) 직행 노선 역추적 `b` · C 재탐색 `re`. D 는 (a) 가 만들어 `a` 다.
`re` 는 계획서의 **(c) 허브 타원**(구현하지 않았다)과 구분하는 이름이다.

## 2. 요청 한 번의 흐름

```
입력: 출발 O · 도착 D · 선호(시간 중시 / 균형 / 비용 중시 → 시간가치 VOT) · 기준 시각

1. (b) 직행 노선 역추적 A·B 후보 생성                         쿼터 0
2. 기준 경로 조회 → 정류장 위치 복원 · 정적 대기               대중교통 1
3. 첫 승차 대기를 실시간으로 바꾼 경로 사본                    실시간 도착 조회(정류장당 1)
     기준선 · B · D · C 는 사본, A 는 원래 경로
4. (a) 기준 경로 섭동 A·B · D 공백 메우기 · C 재탐색 후보       쿼터 0
5. 병합 — 같은 검증 쿼리는 하나만, C 는 예산을 따로 둔다
6. 온라인 검증 — 자동차 경로 · 대중교통 경로로 시간·요금 확정   후보당 1–2콜
7. 같은 잣대로 비교 → 5분 기준 → Pareto → 원/분 → 제시
```

후보 생성(1·4)은 정적 표만 쓰는 오프라인 단계이고, 확정 값은 온라인 검증(6)이 준다 — method.md §2.4 의 2단 구조다.
기준 시각을 따로 주면 '지금' 의 도착정보가 그 시각과 맞지 않으므로 3단계를 건너뛰고 정적 배차만 쓴다.

## 3. 유형별 동작

### 3.1 (b) 직행 노선 역추적 → A·B — `propose_backtrack`

- **교집합**: 택시를 타는 쪽 끝에는 택시 도달 반경(15분)을, 걷는 쪽 끝에는 도보 반경(400 m, 비면 800 · 1,500 m)을 주고,
  양쪽 반경 안 정류장을 **함께 지나는 노선**만 본다. A 와 B 는 두 반경을 맞바꾼 같은 알고리즘이다
  - A: 출발지 쪽 택시 반경 · 목적지 쪽 도보 반경 — 앵커는 **승차 지점**
  - B: 출발지 쪽 도보 반경 · 목적지 쪽 택시 반경 — 앵커는 **하차 지점**
- **방향**: 버스는 노선 순서표의 순번으로 승차 → 하차가 이어지는지 본다. 경기 순서표는 상·하행이 한 순번열이라 반대 방향 짝도
  순번이 이어지므로, 운행 거리가 직선의 1.6배를 넘는 짝은 뺀다. 도시철도는 순서표 덩어리에 방향이 없어 방향을 걸지 않는다
- **운행 시간**: 배차표의 첫·막차로 그 시각에 다니지 않는 버스 노선을 뺀다(막차가 첫차보다 이르면 자정을 넘긴 것으로 읽는다)
- **수요응답형 버스 제외**: 똑버스·DRT 는 앱 호출로 구역 안을 달려 정해진 노선·배차가 없으므로 노선을 모으는 단계에서 뺀다
  (`EXCLUDED_ROUTE_TYPES`). 순서표에는 남겨 둔다
- **묶음**: 후보는 앵커 단위로 묶는다 — 검증 쿼리가 앵커마다 하나이기 때문이다. 환승역의 노선별 여러 행은 한 실체(`phys_id`)로 접는다
- **대기**: 한 앵커에서 여러 노선을 탈 수 있으면 배차 빈도를 더한다(10분·15분이면 유효 6분). 같은 번호 노선 후보는 한 노선으로 보고
  평균한다. B 는 앵커가 하차 지점이라 **같은 승차 지점**의 노선끼리만 더한다
- **점수**: 택시 구간 시간 + 대기 + 승차 시간(순서표 거리 ÷ 노선 유형별 표정속도) + 도보 + (택시 요금 + 대중교통 요금) ÷ VOT
- **상위 N**: 노선마다 2개까지만 남긴다 — 한 노선이 지나는 정류장이 여럿이면 상위를 그 노선 하나가 채운다

### 3.2 (a) 기준 경로 섭동 → A·B — `propose_perturb`

- 기준 경로의 승·하차 지점에서 앞을 택시로 바꾸면 A(출발지 → 승차 지점), 뒤를 바꾸면 B(하차 지점 → 목적지)다
- 남은 구간의 시간은 기준 경로 응답에 이미 있어 후보를 만드는 데 쿼터가 들지 않는다. 다만 O→D 최적화의 부산물이라 비관적 상한이다
- 요금은 경로 단위로만 오므로 기준 경로 요금을 상한으로 둔다
- A 는 택시로 정류장에 닿으므로 원래 경로를, B 는 걸어서 첫 정류장에 닿으므로 실시간 첫 대기 사본을 넘긴다 — `hybrids` 로 따로 만든다

### 3.3 D 공백 메우기 — `propose_gap`

- **공백**: 한 승차 구간의 기대 대기가 386초(호출 대기 90 + 택시 고정 비용 236 + 버퍼 60) 이상이면 그 정류장에서 기다리지 않고
  택시로 그 구간의 하차 지점까지 간다(대기와 차내 시간을 함께 대신한다). 두 승차 구간 사이 환승 도보가 그만큼 길면 앞 하차 지점에서
  뒤 승차 지점까지 간다. 경로의 끝이 아니라 **한가운데 한 구간**만 바꾸므로 택시 양끝이 모두 경로 위 정류장이다
- **시간**: 기준 경로 시간 − 공백 구간 + 택시 구간 시간. 대기를 모르는 구간이 하나라도 있는 경로는 건너뛴다
- **최소 절약**: 택시가 대신한 시간을 알므로 5분 기준을 후보 단계에서 거른다
- **요금**: 기준 경로 요금이 상한이다. 앞 차 하차 → 다음 차 승차(환승 도보 + 택시 + 다음 차 대기)가 **환승 인정 시간**
  (다음 차 승차 시각 기준 평시 30분, 21–07시 60분)을 넘기면 다음 차에서 기본요금을 새로 낸다. 첫·마지막 구간은 끊길 환승이 없다

### 3.4 C 재탐색 — `propose_detour`

- **정류장 시각**: 경로 위 모든 정류장(승차·중간·하차)에 출발부터 닿는 시각을 매긴다 — 구간 시간을 정류장 사이 직선거리 비율로 나누고
  첫 도보·대기를 앞에 더한다(`route_points`)
- **돌아가기 시작하는 정류장**: 경로를 따라 목적지까지 직선거리가 줄다가 **300 m 넘게 다시 늘기 시작하는 곳**이다. 그 정류장을 떠나
  목적지에 다시 그만큼 가까워질 때까지 기준 경로가 쓰는 시간(돌아가는 비용)으로 순위를 매긴다(`detour_points`)
- **다시 찾기**: 돌아가는 비용이 큰 정류장 2곳(서로 150 m 이상 떨어진)에서 내려
  - 택시로 목적지까지 (`via=taxi_to_d`)
  - 그 정류장을 출발지로 둔 (b) 역추적 A 방향 앵커 3개로 갈아타기 (`via=reanchor`) — 그 정류장 시각으로 첫·막차·배차·할증을 읽는다
- 원래 노선에 다시 합류하지 않는다 — 합류하면 그 정류장에서 더 좋은 길을 못 보고, 같은 노선에 다시 타면 줄인 시간이 배차간격을 넘어야만 이득이다
- **요금**: 내린 곳까지 기준 경로 요금이 상한이다. 택시를 사이에 둔 환승이 인정 시간 안이면 뒤 노선은 기본요금을 뺀 거리 비례분만,
  넘기면 뒤 노선 요금을 통째로 더한다
- **예산**: 요청당 정류장 2곳 × (목적지 택시 1 + 앵커 3) — 다른 유형의 상위 N 과 따로 전부 확인한다

## 4. 모든 유형에 공통인 규칙

| 항목 | 규칙 | 코드 |
|---|---|---|
| 택시 시간 근사 | 236초 + 직선 1 km 당 178초 (2026-09-14 비혼잡 시각, 직선 1–6 km 150표본 — 절대오차 중앙 2.4분) | `taxi_time_s` |
| 택시 요금 근사 | 기본요금 4,800원 + 기본거리 초과분 × 거리단가. 기본거리·단가는 시군 유형(표준 1.6 km · 가 1.8 km · 나 2.0 km)별, 심야(서울 22–04시 ×1.2 · 경기 23–04시 ×1.3)·시계외(×1.2, 공동사업구역 제외) 할증. 후보 순위용이고 확정 요금은 자동차 경로 응답값 | `taxi_fare` |
| 택시 구간에 잡는 시간 | 호출 대기 90초 + 이동 시간 + (내려서 차를 탈 때만) 연결 버퍼 60초 — 후보 점수와 검증에 같은 값을 쓴다 | `taxi_plan_s`, `taxi_connect_s` |
| 택시 최소 기준 | 택시 구간 직선 1.1 km 이상(표준형 기본거리 1.6 km ÷ 우회계수 1.42), 기준선보다 5분 이상 빠를 것 | `TAXI_MIN_M`, `saves_enough` |
| 첫 승차 대기 | 실시간 도착에서 정류장까지 걷는 시간을 뺀다 — 걷는 동안 지나가는 차는 탈 수 없다. 버스는 노선 id, 도시철도는 행선의 '○○방면' 으로 탈 차를 가린다. 알려진 차를 모두 놓치면 배차간격으로 다음 차를 잇는다 | `with_realtime_first_wait` |
| 남은 역·정류소 수 | 예측 초가 없으면 남은 수를 시간으로 바꾼다 — 도시철도는 그 열차가 지나올 역 사이 실제 거리 ÷ 32 km/h(순서표로 못 읽으면 역당 134초), 버스는 정류소당 72초. 1역 전 열차는 남은 역 문구 없이 도착 코드(전역 출발·진입·도착)로 와서 1로 읽는다. 수신 경과를 뺀다 | `arrival`, `subway_upstream_s`, `realtime.subway` |
| 첫 대기 출처 | `realtime`(도착 예측 초) · `realtime_stops`(남은 수 추정) · `realtime+headway`(놓쳐서 배차로 이음) — 앞의 것만 '실시간' | `realtime_first_wait` |
| 뒤 구간 대기 | 정적 배차의 절반. 버스는 노선·요일 유형(최소·최대의 중간값, 그 요일 값이 없으면 평일), 도시철도는 역·시간대(앞뒤 1시간을 합친 3시간 창으로 센 배차, 방향 평균). 함께 탈 수 있는 노선은 빈도 합. 한 구간이라도 배차를 모르면 경로 대기 합은 없다 | `app.wait`, `app.headwaydb` |
| 배차를 모르는 대기 | 후보 점수에서는 900초(버스 평일 배차 중앙 30분의 절반). 비교 시간(`reconstruct`)에서는 0초로 센다 | `WAIT_UNKNOWN_S` |
| 도보 | 직선거리 × 우회율 1.39 ÷ 0.83 m/s (블랙박스 자신의 도보에서 역산) | `walk_s` |
| 대중교통 요금 근사 | 노선 유형별 — 공항 8,000 · 시외 4,000 · 광역·직행 2,800 · 그 밖 1,450원 | `transit_fare` |
| 잣대 통일 | 기준선과 모든 후보를 "구간 시간 합 + 추정 첫·끝 도보 + 대기" 로 다시 계산한다 — 블랙박스 총 시간에는 첫·끝 도보가 들어 있고 대기는 없다 | `compare.reconstruct` |
| 후보 점수 | 일반화 비용 = 시간 + (택시 + 대중교통 요금) ÷ VOT — 요금을 넣어야 상위 후보가 택시 도달 경계 한 점으로 쏠리지 않는다 | 각 `propose_*` |
| 병합 | 같은 유형이고 앵커가 같거나 150 m 안이면 한 검증 쿼리로 본다. C·D 는 택시 양끝을 모두 비교한다 | `merge_candidates` |

## 5. 검증과 제시

### 5.1 유형별 검증 시간·요금 — `cli._verify_one`, `cli._verify_detour`

| 유형 | 검증 호출 | 시간 | 요금 |
|---|---|---|---|
| A | 자동차(O→앵커) + 대중교통(앵커→D) | 택시 구간(버퍼 포함) + 앵커→D 경로 | 택시·통행료 + 앵커→D 요금 |
| (b) B | 대중교통(O→앵커) + 자동차(앵커→D) | O→앵커 경로 + 택시 구간(버퍼 없음) | 택시·통행료 + O→앵커 요금 |
| (a) B | 자동차(앵커→D) | 기준 경로 앞부분 + 택시 구간(버퍼 없음) | 택시·통행료 + 기준 요금(상한) |
| D | 자동차(공백 시작→끝) | 기준 시간 − 공백 + 택시 구간(뒤에 탈 차가 있으면 버퍼) | 택시·통행료 + 기준 요금 (+ 환승이 끊기면 기본요금) |
| C 목적지까지 | 자동차(정류장→D) | 정류장까지 기준 시간 + 택시 구간(버퍼 없음) | 택시·통행료 + 기준 요금(상한) |
| C 앵커 | 자동차(정류장→앵커) + 대중교통(앵커→D) | 정류장까지 기준 시간 + 택시 구간(버퍼 포함) + 앵커→D 경로 | 택시·통행료 + 기준 요금 + 뒤 노선 요금(환승이 이어지면 기본요금 제외) |

검증에서 받은 대중교통 경로는 모두 `compare.reconstruct` 로 같은 잣대에 맞춘 뒤, 대안 중 일반화 비용이 가장 작은 것을 쓴다.
앵커에서 다시 받은 경로의 첫 대기는 정적 배차다. 환승 인정 여부는 응답의 ETA 로 다시 판정한다.

### 5.2 제시 — `cli._report`, `compare`

- **기준선**: 기준 경로의 대안 중 일반화 비용이 가장 작은 것
1. **5분 기준**: 기준선보다 5분 이상 빠르지 않은 후보는 뺀다(`saves_enough`)
2. **Pareto**: 기준선을 포함해 (시간, 요금) 비지배 후보만 남긴다(`pareto`)
3. **원/분**: 추가 요금 ÷ 절약 시간. 고른 선호의 VOT 보다 비싸면 "비추천" 이다(`saving_per_min`)
4. **구조 다양성**: 일반화 비용 순으로 두고 유형마다 하나씩 먼저 채운 뒤 남는 자리를 순서대로 줘 4개로 자른다(`diversify`)

## 6. 데모 웹 — `web/backend/app/hybrid.py`

후보 생성·병합·검증은 §2–§5.1 과 같은 함수를 쓴다. 다른 점은 다음과 같다.

- **선호**: 시간 중시(VOT 500원/분)로 고정한다
- **기준선**: 같은 잣대(`reconstruct`)로 잰 **가장 빠른** 대중교통 경로다. 시간이 같으면 요금이 싼 쪽이다
- **기준 경로 재사용**: 화면이 [경로 검색] 결과를 넘기면 대중교통을 다시 부르지 않는다. 첫 승차 대기를 배차 추정으로 되돌린 사본(`static_copy`)을
  받아 A 에는 그대로 쓰고, 나머지는 요청 시각의 실시간으로 다시 매긴다
- **검증 수**: 앵커 기본 5개(최대 8개)이고 C 는 따로 전부 확인한다. 요청당 대중교통 최대 1 + 5 + 6콜(기준 경로를 넘기면 앞의 1콜이 없다) ·
  자동차 최대 5 + 8콜
- **제시**: 5분 기준으로 거른 뒤 모든 후보를 일반화 비용 순으로 돌려주고, Pareto 여부와 추천(원/분이 VOT 이하)을 표시한다 — 구조 다양성으로 자르지 않는다

## 7. 튜닝 상수

| 상수 | 값 | 근거 |
|---|---|---|
| `TAXI_BASE_S` · `TAXI_S_PER_KM` | 236초 · 178초/km | 자동차 경로 150표본 적합 (method.md E18) |
| `DETOUR` | 1.42 | 같은 표본의 도로/직선 중앙값 |
| `TAXI_PICKUP_S` · `TAXI_BUFFER_S` | 90초 · 60초 | 합의한 상수 — 실측 아님 (method.md R27) |
| `TAXI_MIN_M` · `TAXI_MIN_SAVING_S` | 약 1,127 m · 300초 | 표준형 기본거리 ÷ 우회계수 · 합의한 기준 |
| `T_MAX_S` | 900초 | 택시로 갈아타러 가는 이동 시간 상한 |
| `WALK_RADIUS_M` | 400 · 800 · 1,500 m | 걷는 쪽 끝의 반경, 비면 넓힌다 |
| `RIDE_DETOUR_MAX` | 1.6 | 반대 방향 짝을 거르는 운행 거리/직선 상한 |
| `BUS_KMH` · `RAIL_KMH` | 마을 15 … 공항 40 · 32 km/h | 정차 포함 표정속도 — 버스는 실측 아님 |
| `GAP_MIN_S` | 386초 | 호출 대기 + 택시 고정 비용 + 버퍼 |
| `WAIT_UNKNOWN_S` | 900초 | 배차를 모르는 노선의 대기(후보 점수) — 버스 평일 배차 중앙 30분의 절반 |
| `TRANSFER_WINDOW_S` · `…_NIGHT_S` | 30분 · 60분(21–07시) | 수도권 통합환승 인정 시간 |
| `DETOUR_RISE_M` · `C_POINTS` · `C_ANCHORS` | 300 m · 2 · 3 | 우회 판정 · C 검증 예산 |
| `RAIL_S_PER_STATION` · `BUS_S_PER_STOP` | 134초 · 72초 | 순서표로 거리를 못 읽을 때의 대체값 (method.md E19) |
| `NEAR_ANCHOR_M` | 150 m | 같은 검증 쿼리로 볼 앵커 거리 |
| `VOT` | 시간 500 · 균형 300 · 비용 150 원/분 | **잠정값** (plan.md §12) |
| 검증 앵커 수 | CLI `--top` 8 · 데모 웹 5(최대 8) | 요청당 대중교통 쿼터 |

## 8. 아직 없거나 근사인 것

- **(c) 허브 타원 전략과 전략 비교 실험**(쿼리 수 대비 해 품질)은 없다 — 과제의 본 실험이다
- **VOT 3단계**는 잠정값이다
- **호출 대기·버퍼**는 합의한 상수이고, 택시 근사 계수는 비혼잡 시각 한 번의 표본이다 — 출퇴근·심야 기울기는 모른다
- **대기를 모르는 경로**: 비교 시간이 모르는 대기를 0초로 세어 배차를 아는 경로보다 짧게 보이고, 후보 점수(900초)와 잣대가 다르다
- **도시철도 첫·막차 시간대 배차**는 3시간 창이 운행 없는 시간을 품어 넓게 나온다. 기대 대기의 상한도 없다
- **A 유형과 앵커에서 다시 받은 경로의 첫 대기**는 정적 배차다 — 택시로 정류장에 닿는 시각 기준의 실시간 대기가 없다
- **대중교통 요금**은 노선 유형별 근사와 기준 요금 상한이고, 환승이 끊겼을 때도 기본요금 한 번만 더한다
- **실시간 근사**: 버스 남은 정류소 수(정류소당 72초)는 실측하지 않았고, 전역 출발 열차는 한 구간 전으로 봐 최대 한 구간만큼 늦게 본다.
  급행 열차를 가르는 규칙과 오래된 실시간 값을 버리는 규칙은 없다
- **C** 는 동탄역 → 경기도청 한 OD 에서만 이득이 확인됐다 — 차를 3번 이상 갈아타거나 크게 돌아가는 OD 표본으로 평가해야 한다
