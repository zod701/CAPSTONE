# 바로가 — 대중교통·택시 연계 경로탐색 알고리즘: 설계와 발전

> 과제의 핵심 축인 **수단 연계 경로탐색 알고리즘**이 어떤 문제 인식에서 출발해, 어떤 결정·실험·기각을 거쳐 지금의 구조에 이르렀는지만 적는다.
> 데모 웹 구현, API 호출 방법, 화면·운영 세부는 담지 않는다.
>
> **읽는 법** — §0 은 요약뿐이다. 근거·수치·과정은 뒤에 있고, 요약 각 줄 끝의 `→` 가 그 위치다.
> `E` = 실험(§9), `R` = 기각한 설계·가설(부록 A). 실측 수치는 부록 B 에 모아 두었다.

---

## 0. 요약

### 0.1 문제와 기여

- **문제** — 상용 지도 앱은 대중교통만으로 경로를 짠다. "직행이 없으니 A 까지는 택시, A 부터는 대중교통" 같은 수단 연계 판단은
  숙련 이용자의 직관에 맡겨져 있다. 이 판단을 자동화한다. → §1
- **접근** — 대중교통 경로는 상용 라우터를 **블랙박스**로 쓰고, 알고리즘은 **어디서 수단을 잇는가(앵커)** 에 집중한다. → §2
- **기여** — **제한된 쿼리 예산 아래에서 유망한 앵커를 고르는 휴리스틱**. "쿼리 수 대비 해 품질"로 평가한다. → §3, §8
- **선결 문제** — 블랙박스는 정류소를 **이름으로만** 알려 준다. 앵커를 다루려면 이를 **좌표와 순서**로 되살려야 한다.
  알고리즘 이전에 먼저 풀어야 했던 기술 문제다. → §4–§6

### 0.2 알고리즘 구성

```
입력: 출발 O · 도착 D · 선호(시간 중시 / 균형 / 비용 중시 → VOT)
 ① 기준 경로     블랙박스 1콜 → 대안 경로 최대 15개                                  → §2.2
 ② 경로 접지     정류소 이름 → 좌표: 승·하차 매칭 + 경로 위 모든 정류소 위치 복원        → §5, §6
 ③ 앵커 후보     (a) 기준 경로 섭동 · (b) 직행 노선 역추적 · (c) 허브 타원               → §3
 ④ 가지치기      오프라인(Tier 1): 택시 시간 근사 + 배차 근사 → 상위 N 개                → §2.4
 ⑤ 검증         온라인(Tier 2): 택시 O→P(자동차 경로: 시간·요금) + 대중교통 P→D, 요청당 1 + N 콜 → §2.4, §7.1
 ⑥ 비용·제시     일반화 비용 · Pareto(시간·비용·환승·도보) · 원/분 · 구조 다양성          → §7
```

성숙도(큰 단위): ②와 그 기반인 정류소 공간 DB(§4)는 구현·실측됐다. ①은 블랙박스의 성질을 실측으로 확정했다. ③–⑥은 설계 단계다
(③의 입력 중 버스 노선–정류소 순서표와 도시철도 역 순서표는 갖췄고, ②의 접지도 이 표들을 쓴다. ④의 배차 근사와 §7.5 대기시간 모델이 쓸
배차간격표·실시간 도착 원천도 확정돼, 남은 것은 값을 쓰는 방식이다 → §3.6, §7.5, E14).

### 0.3 열린 설계 문제

| 문제 | 남은 것 | → |
|---|---|---|
| 앵커 전략 구현과 ablation | (a)(b)(c)를 구현해 쿼리 수 대비 해 품질을 비교한다 — 과제의 본 실험 | §3.5 |
| 대기시간 모델의 형태 |  남은 것은 **첫·막차 구간 밖 판정**, 실시간과 정적 배차를 어디서 갈아탈지(수신 경과 신선도 임계값), 분산·환승 실패 위험을 비용에 어떻게 넣을지, **급행만 목적지에 닿는 구간**의 대기를 '그 역 아무 열차나' 배차와 어떻게 가를지다 | §7.4, §7.5, E14, E15, E17 |
| 선호 3단계의 VOT 값 | 시간 중시 / 균형 / 비용 중시를 원/분 값으로 정한다 | §7.6 |

### 0.4 발전 과정 한눈에

| 단계 | 생각이 어떻게 바뀌었나 | 계기 | → |
|---|---|---|---|
| 1 | "길찾기 알고리즘을 만든다" → **상용 라우터를 블랙박스로 두고 연계 지점을 푼다** | 무료로 쓸 수 있는 대중교통 경로 API 가 있었다(초기 가정 "없다"는 틀림) | §2.1, E1, R4 |
| 2 | 전수 조합 → **쿼리 예산 아래의 앵커 휴리스틱**, 평가 = 쿼리 수 대비 해 품질 | 정류소 5만 개 × 대중교통 1,000콜/일 | §2.3, §3 |
| 3 | 등시선 만남 (d) → **직행 노선 역추적 (b) 중심 + (a) 섭동 + (c) 허브** | 이산화하지 않은 앵커는 쿼리가 폭발한다 | §3, R1 |
| 4 | "응답에 좌표·순서가 있을 것" → 이름뿐 → **정류소 공간 DB 를 직접 구축** | 응답 구조 실측 | §2.2, E2 |
| 5 | 거리로 중복 제거 → **정류장번호 뒤 9자리로 같은 정류소를 식별**, 길 양쪽 상·하행은 보존 | 이중 등록 5,405쌍 vs 상·하행 ≈1.1만 쌍 | §4.1, E3, R2 |
| 6 | 원천 도시명으로 지역 판정 → **좌표 + 행정경계** | 서울 표기 정류소 1,891곳이 실제로는 경기 | §4.2, E4, R3 |
| 7 | 이름 일치 → **이름 수준(key > alias > prefix) · 반경 · 모호 판정 · 노선 필터** | 경기 외곽 5 OD 에서 버스 발견률 100%, 원천에 없는 노선(GTX-A) 발견 | §5, E7, E8 |
| 8 | 승·하차만 → **경로 위 모든 정류소**. 정류소별 탐욕 선택 → **순서 보존 동적계획** | 갔다가 되돌아오는 노선에서 탐욕 선택이 순서를 깬다 | §6, E10, R8 |
| 9 | 요금 = 값 하나 → **범위 요금도 있음**. 도보 = 환승 도보뿐 → **첫·끝 도보(직선 추정) 포함** | 실측 | §7.2, §7.3, E11 |
| 10 | 택시 요금 = 공개 산식으로 오프라인 계산 → **자동차 경로 응답의 택시 예상요금** | 자동차 경로는 병목이 아니고, 응답이 실시간 교통과 시계외 할증을 반영한다 | §7.1, E5, R13 |
| 11 | 선호 = 진술선호 조사로 VOT 추정 → **길찾기마다 시간 중시 / 균형 / 비용 중시 중 선택** | 범위 축소 | §7.6, R14 |
| 12 | 정류소 DB = 전국 원본(연 1회) → **두 BIS 의 최신 정류소 파일로 교체**, 최신 파일의 좌표 오류는 보정표 | 노선 위 대상 안 정류소 970곳이 DB 밖이었다 | §4.1, E12, R15 |
| 13 | 접지를 이름·기하로만 → **노선·역 순서표에서 구간을 그대로 읽고, 진행 방향은 운행 순서로 판정** | 길 양쪽 동명 정류소는 거리로 못 가리고, 개명·복합 이름은 이름으로 못 찾는다 | §5.5, §6.6, E13, R16 |
| 14 | 대기시간 = 배차간격/2 하나 → **실시간 도착은 보정해서 쓰고, 예측이 없으면 남은 역·정류소 수로, 그것도 없으면 (역·방향·시간대) 배차로 추정하는 세 층 폴백** | 배차·실시간 원천을 확정했다. 시각표 자료의 버스 시각표는 대표 배차를 균등 분배한 합성값이고, 실시간 예측은 0 에 '모름'을 섞어 준다 | §3.6, §7.5, E14, R18, R19 |
| 15 | 승·하차는 이름으로만 → **토막 집합 일치(`parts`)와 노선 순서 구제(`route`)를 더하고, 간격을 이름 수준보다 먼저 본다** | 토막 순서가 뒤바뀐 표기(블랙박스 쪽)와 개명이 10 m 옆 정류장을 미매칭으로 만들었다 | §5.6, E16, R20 |
| 16 | 대기 = 노선 하나의 배차/2 → **구간에서 함께 기다리는 노선은 빈도 합, 같은 번호의 후보는 한 노선으로, 한 구간이라도 모르면 경로 합을 내지 않는다** | 블랙박스가 한 구간에 여러 차량을 주고(E2), 같은 번호 노선이 시·군마다 있어 매칭이 후보를 여럿 남긴다 — 둘을 같게 다루면 대기가 절반이 된다 | §7.4, §7.5, R21 |
| 17 | 급행 구간도 순서표로 떼야 한다 → **떼지 않는다** — 급행이 실제로 물리는 곳은 매칭이 아니라 대기다 | 급행이 도시철도 구간의 43%인데 정차역 위치는 이미 96%가 나오고, 순서표로 다시 떼도 같은 역이 나온다(376/376) | §6.6, §7.5, E17, R22 |

---

## 1. 문제 정의

### 1.1 동기
기존 길찾기는 정해진 노선·배차 안에서만 경로를 찾아, 크게 우회하거나 환승 대기가 긴 경로를 그대로 제시한다.

> "현재 위치에서 A 지점까지는 직행 버스가 없으니 택시를 타고, A 부터 목적지까지는 대중교통으로 간다"

이런 판단은 숙련 이용자의 경험에만 기대고 있다. 본 과제는 이를 알고리즘으로 자동화한다.

### 1.2 하이브리드 경로 유형

| 유형 | 형태 | 빈도 | 난이도 | 설명 |
|---|---|---|---|---|
| A. First-mile | 택시 → 대중교통 | 높음 | 낮음 | 출발지가 역세권 밖이거나 첫 버스가 우회 |
| B. Last-mile | 대중교통 → 택시 | 높음 | 낮음 | 목적지가 대중교통 사각지대 |
| C. Mid-leg | 대중교통 → 택시 → 대중교통 | 낮음 | 높음 | 환승 건너뛰기 — 과제의 대표 사례 |
| D. Gap-filling | 긴 대기·도보를 택시로 대체 | 중간 | 중간 | 실시간 대기 정보가 실제로 쓰이는 곳 |
| E. 기준선 | 전 구간 대중교통 / 전 구간 택시 | — | — | 비교용 상·하한 |

구현 순서는 A·B → D → C → 공유 PM 확장이다. 편익 대부분이 A·B 에 있고 쿼리도 싸다. C 를 먼저 붙들면 파이프라인이 돌지 않는 채로 시간을 쓴다.

### 1.3 목표와 범위
- **수단**: 택시를 먼저, 공유 PM 은 확장. PM 은 자전거 경로 조회로 A·B 가 같은 구조로 동작하도록 설계에만 반영한다
- **지역**: 대중교통 밀도가 낮은 **경기 외곽**을 지향한다. 서울은 대중교통이 촘촘해 연계 편익이 작다. 정적 데이터는 서울·경기 전역을 쓴다
- **목표 함수**: 단일 최적해가 아니라 **Pareto 집합**을 제시하고, 개인 선호는 정렬·필터로 반영한다(§7.4)

---

## 2. 출발점 — 블랙박스 라우터와 쿼리 예산

### 2.1 라우터를 만들지 않는 이유
상용 라우터는 노선·환승·요금이 이미 최적화돼 있다. 과제의 기여는 라우팅이 아니라 **수단 결합**이다.
처음에는 "공개 대중교통 경로 API 가 없다"고 보고 자체 라우터나 우회책을 검토했다. 그런데 무료로 하루 1,000콜을 쓸 수 있는 대중교통 경로 API 가
있었고, 다른 후보는 실험 규모를 버티지 못했다(무료 30콜/일, 10콜/일) → E1, R4.

### 2.2 블랙박스의 성질 — 알고리즘이 메워야 할 빈칸
응답 구조를 실측해서 확정했다 → E2.

| 준다 | 안 준다 → 알고리즘이 메우는 방법 |
|---|---|
| 1콜에 대안 경로 최대 15개(버스·지하철·혼합) | **정류소 좌표·ID** → 공공데이터로 정류소 공간 DB 구축(§4), 이름 → 좌표 접지(§5, §6) |
| 구간별 유형·시간·거리·차량(노선명·유형) | **첫·끝 도보**(출발지 → 첫 승차, 마지막 하차 → 도착지) → 직선 추정(§7.3) |
| 구간의 **정류소 이름 전부**(승차·중간·하차) | **출발 시각·대기시간** → 대기 모델(§7.5) |
| 구간 폴리라인, 환승 도보의 실제 폴리라인 | **경유지** → 연계 경로는 구간별 호출을 이어 붙인다(E9) |
| 경로 요금 — 값 하나 또는 **범위** | 범위 요금의 비교 규칙(§7.2, E11) |

- 약관상 응답을 저장·캐싱할 수 없다. 그래서 영속 데이터(정류소 DB·행정경계)는 **공공데이터로만** 만들고, 평가에는 **파생 수치만** 남긴다
- 이름 → 좌표 접지가 필요한 이유가 여기서 나온다. 앵커는 좌표가 있어야 택시와 이을 수 있는데, 블랙박스는 이름만 준다

### 2.3 쿼리 예산
- 대중교통 경로 1,000콜/일이 **묶이는 제약**이다. 자동차 경로는 10,000콜/일로 병목이 아니다
- 요청당 `1 + N`(N = 5–10) 콜이면 하루 약 90–160회 탐색. 한 달이면 평가 표본으로 OD 약 2,000쌍을 모을 수 있다
- 이 제약이 앵커 선택 휴리스틱이 존재하는 이유다. 인위적인 가정이 아니다

### 2.4 2단 구조 — 오프라인 탐색, 온라인 검증

```
Tier 1 (오프라인 · 무제한 · 무료)  넓게 탐색하고 가지친다
  정류소·역 공간 DB · 노선–정류소 순서 · 배차간격 · 택시 시간 근사
  → 수천 개 후보를 수십 개로
Tier 2 (온라인 · 쿼터 · 정확)      상위 N 개만 정확히 평가한다
  대중교통 경로(블랙박스) · 자동차 경로(실시간 교통 · 택시 예상요금) · 실시간 도착 정보
```

---

## 3. 앵커 선택 — 핵심 기여

**앵커** = 택시와 대중교통이 만나는 정류소·역. 문제는 "어느 앵커를 온라인으로 검증할까"다. 채택 조합은 **(a) + (b) + (c)** 다.

### 3.1 (a) 기준 경로 섭동
기준 대중교통 경로의 구간 하나를 택시로 바꾼다. 후보 수가 구간 수(3–5)라 매우 싸다. C·D 유형에 대응한다.
경로 위 **중간 정류소까지** 좌표를 되살려 두면(§6) 섭동 지점을 구간 경계에만 두지 않고 경로 위 어느 정류소에나 둘 수 있다.

### 3.2 (b) 직행 노선 역추적 ★ 중심 전략

```
1. 기준 경로: 블랙박스(O, D) → R0 (대안 여러 개)
2. D 주변 반경 r(≈400 m) 안의 정류소·역 S_D
3. S_D 를 지나는 노선 L_D                                    ← 정적 노선 DB (오프라인, 무료)
4. 각 노선 l 의 D 방향 상류 정류소 중 O 에서 택시로 T_max 안에 닿는 P → 앵커 후보 (l, P)   ← 오프라인 등시선
5. 근사 점수 = 택시시간(O→P) + 배차간격/2 + 승차시간(P→D) → 상위 N(5–10)   ← 배차를 대기로 접는 규칙은 §7.5
6. 온라인 검증: 자동차 경로(O→P, 택시 시간·요금) + 블랙박스(P→D)
7. 일반화 비용 → Pareto → 상위 3–4개 제시
```

- 1–5단계는 모두 오프라인이다. 요청당 대중교통 쿼리는 `1 + N`
- 대칭판(O 를 지나는 노선의 하류 정류소 → 택시로 D)이 B 유형을 덮는다
- 후보가 작으면서도 **환승 없이 목적지까지 데려다주는 노선**을 정확히 겨눈다

### 3.3 (c) 허브 타원
O–D 를 초점으로 하는 타원(우회 허용계수 α) 안의 주요 환승 거점만 앵커로 쓴다. (b)에 싸게 덧붙이는 보완재다.

### 3.4 기각: (d) 등시선 만남
O 와 D 에서 택시·대중교통 등시선을 넓혀 만나는 곳을 찾는 방식이다. 앵커를 이산화하지 않으면 쿼리가 폭발한다. (b)가 이 생각을 노선 단위로 이산화한 형태다 → R1.

### 3.5 평가 설계
- **ablation**: (a) / (b) / (c) / 무작위 격자 — **쿼리 수 대비 해 품질**(§8)
- 기준선은 대중교통 전용 최적 경로(유형 E)

### 3.6 필요한 입력
| 입력 | 쓰는 곳 | 출처·상태 |
|---|---|---|
| 정류소·역 좌표, 지역(시군) | 후보 생성, 요금 | §4 — 구축됨 |
| 경로 위 정류소 좌표 | (a) 섭동 지점 | §6 — 구현됨 |
| 버스 노선–정류소 순서 | (b) 상류·하류 정류소, 접지(§5.5, §6.6) | 서울 노선별 정류소 + 경기 노선–정류소 일괄 자료로 순서표를 만들었다. 정류소 번호가 우리 키와 **변환 없이** 맞물린다. 순번 = 운행 순서라 상류·하류를 순번 비교로 정한다 → E6 |
| 도시철도 역 순서 | (b) 지하철 노선, 접지(§6.6) | 노선정보 표준데이터의 **정거장 구성** + 보정표로 역 순서표를 만들었다(지선·순환·빠진 구간 포함, 역 DB 전부). 역번호 순서는 못 쓴다(코레일 노선에서 최대 59 km 점프) → E6, R7 |
| 배차간격·실시간 도착 | (b) 근사 점수, 대기 모델(§7.5) | 버스 = 경기 요일 유형별 최소·최대(노선 일괄 자료) + 서울 대표값(노선 조회) — 순서표 3,852 노선 중 3,823 에 원천 행이 있고 배차 분값은 3,294(3,852 의 85.5%). 도시철도 = 신청형 시각표 자료(평일 1일)의 실제 운행에서 센 (역, 노선군, 방향, 시간대) 배차로 685역 중 683역. 버스의 시간대별 배차는 같은 자료의 버스 시각표가 합성값이라 못 쓴다(R18). 실시간 도착은 버스 두 BIS(정류소 번호로 바로 지목) · 도시철도 1종(역명 정확 일치만 받아 매핑표가 필요하고 22 노선군 중 19개) → E14. 값을 대기로 접는 규칙은 §7.5, 남은 것은 §0.3 |
| 택시 시간 근사 | 후보 생성·가지치기 | 검증 단계에서는 자동차 경로 응답이 시간·요금을 준다 → §7.1 |

---

## 4. 정류소 공간 기반 — 알고리즘의 좌표계

블랙박스가 좌표를 주지 않으므로, 앵커를 표현할 좌표계를 공공데이터로 직접 만들었다. 핵심은 **같은 정류소를 하나로 식별하는 규칙**과 **지역 판정**이다.

### 4.1 정류소의 정체 — 번호 뒤 9자리
- 전국 버스정류장 자료에서 서울·경기 BIS 가 등록한 레코드(접두 `GGB`·`SEB`)만 쓴다. 두 BIS 가 **같은 물리 정류소를 각각 등록**했고, 정류장번호의
  **접두 3글자를 떼면 나머지 9자리가 같다** → 이 9자리를 정류소 키로 쓴다 → E3
- 쌍 판정(먼저 맞는 규칙): **50 m 이하면 이름과 무관하게 병합** → 50–300 m 이고 이름이 호환되면(키가 같거나 한쪽이 다른 쪽의 앞부분) 병합 → 그 외에는 분리.
  50 m 안에서 이름으로 막지 않는 이유는, 5 m 떨어진 같은 정류소가 이름이 전혀 다르게 등록된 사례가 있기 때문이다(ARS 번호는 같음)
- 대표 레코드는 소재지의 BIS 것으로 둔다(서울이면 서울 BIS). 노선–정류소 원천(서울·경기)과 키가 그대로 맞물린다
- **길 양쪽의 상·하행 정류소는 합치지 않는다.** 15–60 m 떨어진 같은 이름·같은 기관의 쌍은 중복이 아니라 상·하행이다. 거리만으로 중복을 지우면 이들이
  뭉개진다 → R2. 병합 뒤에도 상·하행 쌍이 새로 합쳐지거나 생기지 않았다 → E3
- 전국 원본은 연 1회 갱신이라 노선 원천보다 오래된다. 서울·경기 BIS 레코드는 **두 BIS 의 최신 정류소 파일로 바꾼 뒤** 같은 규칙으로 정제한다.
  최신 파일에서 빠진 레코드는 폐지로 본다(서울 파일은 서울 소재만 담아, 서울 BIS 의 서울 밖 레코드는 전국 원본 것을 둔다). 최신 파일의 좌표 오류는
  보정표로 고친다 → E12, R15
- 결과: **50,019개**(단독 44,373 · 근접 병합 5,443 · 원거리 병합 169 · 분리 34)

### 4.2 지역 판정 — 좌표 + 행정경계
- 원천의 도시명은 **소재지가 아니라 BIS 운영 주체**다. 서울 BIS 레코드는 전부 "서울"로 표기돼 있지만, 좌표로 보면 1,891곳이 경기에 있다 → R3, E4
- 그래서 시도·시군을 **행정경계 폴리곤 포함 판정**으로 다시 매긴다. 서울 25구 · 경기 31시군이다. 경기의 일반구는 시로 합친다 — 최근의 구 신설·부활도
  시 외곽 경계는 바꾸지 않았다
- 폴리곤 밖의 점은 **가장 가까운 폴리곤이 대상 시군이고 2 km 이내일 때만** 그 시군으로 붙인다(서해안 매립지·방조제 위 정류소). 인접 시도가 더 가까우면 제외한다

### 4.3 도시철도역 — 노선군과 보강
- 원천은 전국 도시철도 역사 표준데이터다. 역 좌표와 환승 정보가 있지만, 노선명이 **운영 노선명**이라 블랙박스가 쓰는 승객 노선명과 다르다
  (예: 승객의 1호선 = 운영 노선 5개) → **노선명 → 노선군 매핑**
- 운영 노선 하나가 승객 노선 둘에 걸치는 구간은 **역 단위 보정**으로 푼다(용산–왕십리 구간의 운영 노선명은 1호선 계열이지만 실제 운행은 경의중앙선)
- 원천에 없는 개통 노선은 참조표로 보강한다. GTX-A 9역을 넣었고, 이것으로 실경로 미매칭이 해소됐다 → E8
- 원천 좌표가 틀린 역 3곳(다른 지역의 동명 역 좌표, 옆 역 좌표)은 다시 찍어 보정표로 고치고, 서지 않는 노선으로 잘못 들어간 행은 뺀다 → E8
- 결과: 대상 지역 **685역**

### 4.4 이름 정규화 — 매칭의 기준 키
정류소 이름과 블랙박스의 이름을 같은 규칙으로 키로 만든다.
- 유니코드 정규화(NFKC), 구분자(`·` `∙` `,` 등)를 `.` 로 통일
- 괄호 부기명 제거: `천호(풍납토성)` → `천호`, `(중)` · `(미정차)` 도 제거
- **지하철만** 끝의 "역"을 뗀다: `주안역` → `주안`. 버스의 `판교역` 은 정류소 이름의 일부라 두 경우를 다르게 다룬다
- "미정차" 가상 정류소(승하차 불가)는 매칭 후보에서 뺀다

---

## 5. 이름 → 좌표: 승·하차 매칭

### 5.1 기준점
블랙박스가 좌표 대신 주는 유일한 공간 단서는 **구간 폴리라인**이다. 그 **시작점을 승차**, **끝점을 하차**의 기준점으로 쓴다.
구간의 정류소 이름 목록 양끝이 안내문의 승·하차 이름과 모든 실측에서 일치했다. 그래서 목록 양끝을 승·하차 이름으로 쓴다 → E2.

### 5.2 후보와 순위
- 기준점 반경(버스 **300 m**, 지하철 **1,000 m**) 안의 DB 행을 모은다
- 이름 수준을 매긴다: **key**(정규화 키 일치) > **alias**(병합으로 모인 다른 이름과 일치) > **prefix**(한쪽이 다른 쪽의 앞부분, 둘 다 4글자 이상 — 잘린 이름 대응)
- **가장 높은 수준**의 후보 중 **가장 가까운 것**을 고른다
- 지하철은 차량의 노선명을 노선군으로 바꿔 **같은 노선군 후보만** 남긴다. 맞는 노선이 없으면 필터 없이 고르고 그 사실을 표시한다
- 버스는 차량 이름으로 노선 순서표를 찾아 **그 노선이 서는 정류장만** 남긴다 — 길 양쪽을 가르는 단계다 → §5.5

### 5.3 모호 판정
1위와 **다른 실체**인 후보가 1위보다 버스 **10 m**, 지하철 **150 m** 이내로 더 멀리 있으면 **모호(ambiguous)** 로 표시한다.
같은 이름의 지하철 행이 300 m 안에 있으면 한 역의 환승 노선 행들로 보고 같은 실체로 친다.

### 5.4 실측과 한계 → E7, E8
- 버스는 모두 찾았고(발견률 100%) 오차 중앙은 6–7 m 대다. 지하철 오차가 큰 것(중앙 25–97 m)은 역 대표 좌표와 승강장 쪽 경로 끝점의 차이다
- **모호 7건**은 동명 정류소 2–4곳이 10 m 안에 붙은 경우다(상·하행, 중앙차로 정류소와 가로변 정류소). 1위와 2위의 거리 차이가 0.7–9.7 m 로
  기준점 오차와 크기가 같아 거리로는 가릴 수 없다 → 노선 순서로 푼다(§5.5)
- **복합 이름**: 블랙박스 이름이 DB 이름보다 자세하거나(`A.B` 꼴, 출구 번호) **토막 순서가 뒤바뀌면** key·alias·prefix 에 모두 걸리지 않는다 → R9.
  경로 중간에서는 순서표가 이름 없이 자리를 알려 주어 사라졌고(§6.6), 남아 있던 승·하차는 토막 집합과 노선 순서로 푼다(§5.6, E16)
- **원천에 없는 노선**은 매칭이 원리적으로 불가능하다. GTX-A 보강으로 해소됐다 → E8

### 5.5 진행 방향 판정 — 노선 순서로 길 양쪽을 가른다
같은 이름 정류소가 길 양쪽에 붙어 있으면 기준점만으로는 어느 쪽인지 알 수 없다. 노선 순서표가 그 답을 갖고 있다 → E13.

- 블랙박스가 구간마다 주는 **차량 이름**으로 순서표의 노선을 찾는다. 같은 번호가 시·군마다 있어(이름이 `5` 인 노선 21개, 그중 마을형 11개) 이름만으로는 하나가 되지 않는다
- 승·하차 후보를 **운행 순서가 승차 → 하차** 로 잇는 노선만 남긴다. 그러면 그 노선이 서지 않는 반대편 정류장이 떨어진다
- 그것만으로는 부족하다: 경기 순서표는 상행 뒤에 하행이 **한 순번열**로 이어져 '상행 승차 + 하행 하차' 짝도 이 조건을 지난다.
  **구간의 정류소 개수와 순서 간격이 가장 잘 맞는** 짝만 남긴다 — 반대 방향 짝은 간격이 크게 벗어난다(실측: 기대 2 인 곳에서 27·56)
- 이름이 갈라진 경우가 있다 — 블랙박스는 요일별 운행을 `11-A(평일 출퇴근)`·`11-C(주말)` 로 부르고 순서표에는 `11` 하나뿐이다.
  그대로 맞는 이름이 없으면 괄호와 끝의 변형 글자를 뗀 이름으로 한 번 더 찾는다(순서표에만 `(임진각)`·`(예약)` 이 붙은 이름도 244개 있다)
- 도시철도는 이미 노선군 필터(§5.2)가 같은 일을 한다 — 역은 길 양쪽에 갈라져 있지 않다

### 5.6 이름이 글자로 어긋날 때 — 토막 집합과 노선 순서 구제
같은 정류소를 블랙박스가 다른 표기로 부르면(토막 순서 뒤바뀜·개명·표기 차이) 10 m 옆에 두고도 못 찾는다.
중간 정류소는 순서표가 이름 없이 자리를 주지만(§6.6) 승·하차에는 그 근거가 없었다 → E16.

- **토막 집합 일치(`parts`)**: 이름을 `.` 로 나눈 토막의 집합이 같으면 순서가 달라도 같은 이름으로 본다.
  토막을 **하나도 버리지 않는다** — 하나만 겹쳐도 맞다고 보는 부분 일치는 오매칭이 많아 여전히 기각이다(R16).
  수준은 alias 와 prefix 사이다(글자 근거는 있지만 표기가 다르다)
- **노선 순서 구제(`route`)**: 글자가 맞은 후보가 그 노선 위에 하나도 없을 때만 이름을 버리고, 그 노선이 서는 정류장을 후보로 삼는다.
  글자가 맞는 행이 있는데 순서표에 없다면 그것은 **순서표가 낡았다는 신호**이므로 구제하지 않는다
- **거리로 고르지 않는다**: 노선은 반경 안에 정류소를 여러 개 두는 일이 흔해(300 m 안에 두 곳 이상이 93.5%) 거리는 증거가 못 된다.
  **구간 정류소 개수와 운행 순서 간격이 정확히 맞는 후보만** 받는다 → R20
- **간격이 이름 수준보다 앞선다**: 순위는 (간격이 맞나 · 이름 수준 · 거리) 순이다. 이름 수준을 먼저 보면 간격이 한 칸 어긋난
  상위 수준 후보가 간격이 맞는 낮은 수준 후보를 조용히 밀어낸다
- **이름 근거가 없으면 모호로 남긴다**: `parts`·`route` 수준에서는 경쟁 후보가 하나라도 있으면 거리 차와 무관하게 모호다.
  구제로 찾았다는 사실과 이름이 다르다는 사실을 진단에 남겨, 정류소 DB 가 낡았다는 신호를 구제가 삼키지 않게 한다
- 도시철도에는 넣지 않았다 — 반경이 1 km 라 같은 규칙이 그대로 통하는지 따로 재야 한다

---

## 6. 경로 위 모든 정류소의 위치 복원

### 6.1 왜 필요한가
앵커 섭동(a)과 택시 승·하차점 후보는 **경로 중간**의 정류소다. 블랙박스는 중간 정류소도 이름만 준다.
승·하차 매칭(§5)만으로는 경로 중간에 어떤 정류소가 어디 있는지 알 수 없다.

### 6.2 기준점이 없다 → 구간 선 복도
중간 정류소에는 기준점이 없다. 그래서 반경 대신 **구간 폴리라인까지의 거리**를 쓴다. 이름이 맞는 DB 행 중 선에서 **버스 100 m, 지하철 500 m** 안에 있는 것이 후보다.
복도를 좁게 두는 것은 DB 에 없는 정류소가 옆 도로의 같은 이름 정류소로 잘못 붙지 않게 하기 위해서다.

### 6.3 발전: 정류소별 탐욕 선택 → 순서 보존 동적계획
- **1안 — 정류소별 탐욕 선택**: 정류소마다 따로 최선 후보(노선 일치 > 이름 수준 > 선까지 거리)를 고른다.
  갔다가 되돌아오는 노선에서 같은 이름이 한 구간에 두 번 나오면, 둘 다 **첫 통과 지점**의 행에 붙는다. 그 뒤 위치를 추정하는 정류소가 선 한가운데로 끌려간다 → R8
- **2안 — 순서 보존 정렬**:
  1. DB 행이 선 곁을 **지나는 곳마다** 후보를 따로 만든다. 선이 꼭짓점에서 복도 밖으로 나가면 한 번의 통과로 끊는다. 긴 선분 하나가 복도를 스치는 것도 한 번으로 센다
  2. 각 후보에 (정류소 순번, 선 위 거리, 점수)를 매긴다. 점수는 노선 일치 > 이름 수준 > 선까지 거리이고, 어떤 후보든 0보다 커서 **하나라도 더 맞히는 조합이 이긴다**
  3. **정류소 순서와 선 위 순서가 어긋나지 않는**(뒤로 50 m 까지 허용) 후보 조합 중 점수 합이 최대인 것을 동적계획으로 고른다 — 가중 최장 증가 부분열과 같은 꼴, O(후보²)
- 정류소마다 후보가 몇 개뿐이라 후보² 는 작다. 대안 15개가 같은 구간을 자주 공유하므로 같은 구간은 한 번만 계산한다

### 6.4 못 찾은 정류소
앞뒤로 위치가 정해진 정류소의 **선 위 거리 사이를 정류소 순번 비율로** 나눈 점에 둔다. 양 끝의 기준은 선의 시작과 끝이다. 추정 위치임을 표시해 DB 위치와 구분한다.

### 6.5 실측 → E10
5 OD 에서 버스 98.9%, 지하철 97.8% 를 DB 에서 찾았고 순서 역전은 없었다. 남은 추정 위치의 대부분이 §5.4 의 복합 이름 문제였다.
나머지는 DB 에 없는 이름과 **역명 변경**(블랙박스가 옛 이름을 씀)이다. 둘 다 이름으로는 풀 수 없다 → §6.6.

### 6.6 이름을 거치지 않는다 — 순서표에서 구간을 그대로 읽기
노선과 승·하차가 정해지면 그 사이 정류소는 **순서표가 이미 알고 있다**(승·하차에도 같은 근거를 쓴다 — §5.6). 이름·기하로 다시 고르지 않고 승차 → 하차 구간을 그대로 읽는다 → E13.
길 건너편 정류소(노선이 양쪽에 다 서는 곳)와 이름으로는 못 찾는 정류소(개명·복합 이름)가 함께 풀린다.

- **믿을 수 있을 때만 쓴다**: 구간의 자리마다 이름이 맞는지 세어 60% 이상일 때만 받는다. 노선 변형이나 낡은 순서표를 막는 장치다.
  못 믿으면 §6.3 의 순서 보존 동적계획으로 돌아간다. 좌표는 정류소 DB 를 먼저 쓰고, DB 에 없는 정류소는 순서표 좌표를 쓴다
- **도시철도도 같다**. 다만 순서표의 한 덩어리(운행 노선·지선)에는 **방향이 없어** 승·하차가 방향을 정하고, 순환선은 끝에서 처음으로 이어 붙인다
- 운영이 갈려 **두 덩어리로 적힌 노선**(8호선 별내선 ↔ 서울 8호선, 4호선 진접선·안산선, 3호선 일산선, 수인분당선 …)은 양쪽 다 끝인 역에서 이어 붙인다.
  지선은 분기역이 본선 한가운데라 붙지 않고, 아직 안 이어진 GTX-A 북부·남부도 붙지 않는다
- **급행**은 블랙박스가 정차역만 주어 역 개수가 맞지 않는다 → 구간을 뗄 수 없어 이름·기하로 돌아간다. 급행 정차역은 이름이 어긋나지 않아 그 길로도 위치가 나오고, 구간을 부분수열로 다시 떼도 고르는 역이 같다 → E17

---

## 7. 비용과 선호

### 7.1 택시
- **소요시간·거리·요금 = 자동차 경로 응답**(실시간 교통 반영). 택시 경로는 차량 경로와 같다고 본다
- 요금은 응답의 **택시 예상요금**에 따로 오는 **통행료**를 더한다(통행료는 승객이 실비로 낸다)
- 처음에는 공개 요금 산식(서울, 경기 시군별 3유형, 심야·시계외 할증)으로 오프라인 계산하려 했다. Tier 1 이 온라인 호출 전에 요금을 알아야 한다고 봤기 때문이다.
  응답값으로 바꾼 이유 → R13
  - 가지치기에는 시간만 있으면 된다. 요금은 검증(Tier 2)에서 받으면 되고, 자동차 경로는 10,000콜/일로 병목이 아니다(§2.3)
  - 응답은 실시간 교통과 시계외 할증을 반영한 값이다. 한 사례에서 시계외 할증을 넣은 산식 추정과 맞았다 → E5
  - 산식에는 공개 자료로 확정되지 않는 규칙이 남는다(경기 심야와 시계외의 결합 방식, 읍면 복합할증)
- 같은 OD 라도 실시간 교통에 따라 경로·요금이 달라진다 → E2

### 7.2 대중교통 요금
경로 요금은 값 하나로 오기도 하고 **범위(최소–최대)** 로 오기도 한다. 한 OD 의 대안 15개 중 3개가 범위였다 → E11.
정렬에서는 낮은 쪽을 쓰고, 택시와 비용을 비교할 때도 같은 규칙을 그대로 쓴다.

### 7.3 도보 거리
블랙박스는 환승 도보만 실제 경로로 주고 **첫·끝 도보는 주지 않는다**. 그래서 도보 거리를 이렇게 정의한다.
- 도보 거리 = 환승 도보 거리 + **O → 첫 승차점, 마지막 하차점 → D 의 직선거리**(추정)
- 첫·끝을 빼면 첫 정류장이 1 km 떨어진 경로도 도보 0으로 계산돼 비교가 왜곡된다
- 한 OD 의 대안 15개에서 이 값이 304 m – 1.4 km 로 갈렸다. 경로 선택을 가르는 차원이다

### 7.4 비교 기준과 제시
- **비교 차원**: 소요 시간 · 비용 · 이동 거리 · 환승 수 · 도보 거리
- **일반화 비용** `GC = 총 소요시간 + 총비용 / VOT` (VOT = 시간가치, 원/분)
- **Pareto(skyline) 필터**: (시간, 비용) 최소를 기본으로, 필요하면 환승·도보를 더한다
- **절감 효율을 원/분 하나로**: "택시 6,000원 추가 → 14분 절약 → 분당 429원". 사용자가 고른 선호 단계의 VOT 보다 비싸면 비추천으로 표시한다
- **구조 다양성**: 3–4개로 자르되 서로 다르게 — 전 구간 대중교통 · first-mile 택시 · mid-leg 택시 · 전 구간 택시
- **잣대가 다르다**: 블랙박스가 주는 시간에는 대기가 없다(E15). 우리 비교는 대기를 넣은 시간으로 하므로 같은 대중교통 경로라도
  블랙박스 표기보다 길다 — 경기 외곽에서 중앙 9분 남짓 차이가 난다. 둘을 섞어 비교하면 배차가 긴 노선이 유리해 보인다
- **두 시간을 나란히 둔다**: 같은 경로에 블랙박스 시간과 대기 포함 시간을 함께 적는다. 대기는 한 구간이라도 배차를 모르면 합을 내지 않아(§7.5)
  경로마다 있고 없고가 갈리므로, 순서와 Pareto 의 기준은 아직 블랙박스 시간이다. 상한과 첫·막차 규칙까지 서서 모든 경로에 대기를 채울 수 있게 되면
  기준을 대기 포함 시간으로 옮긴다 → §0.3

### 7.5 대기시간과 불확실성 (설계)
- 블랙박스는 출발 시각과 무관한 평균 소요시간만 준다. "택시 도착 10:23 → 다음 버스 10:31 → 8분 대기" 같은 시간 정합성을 스스로 판단해야 한다
- **입력이 두 층이다**(원천과 커버리지는 §3.6, E14). 가지치기(Tier 1)는 오프라인 배차표로 기대 대기를 매기고, 검증(Tier 2)은 상위 후보의 앵커 몇 개에만
  실시간 도착을 쓴다. 실시간은 쿼터 자원이라 후보 단계에서 정류소·역마다 부를 수 없다 — 2단 구조(§2.4)가 그대로 대기시간에도 적용된다
- **정적 배차를 읽는 규칙**
  - 버스는 요일 유형별 **최소·최대 두 값**이고(서울은 요일 구분 없는 대표값 하나), 0·빈값은 정보 없음이다. 한쪽만 있는 행과 최소가 최대보다 큰 행이 있어
    두 값의 순서를 가정하지 않고 **둘의 중간값** 하나로 접는다(한쪽만 있으면 그 값). 최대 배차가 하루 단위로 큰 행이 있어 **기대 대기에 상한**이 필요하다 — 값은 아직 정하지 않았다(§0.3)
  - 도시철도는 (역, 노선군, 방향, 시간대) 값이라 §3.2 의 `배차간격/2` 를 그 시간대 값으로 바꿔 쓴다. **노선군으로 올려 평균하지 않는다** — 역별 편차는
    노선군마다 다르고(8시 최대/최소 1호선 27배, 반대로 8개 노선군은 3배 미만) 어느 쪽인지는 노선 이름으로 알 수 없다. 크게 갈리는 노선군에서는 평균이
    실제 대기와 멀고, 운행 계통이 갈리는 노선군은 평균이 어느 구간에도 맞지 않는다
  - 도시철도 배차는 급행·일반을 합쳐 센 '그 역에서 아무 열차나' 기준이라 특정 패턴을 기다리는 대기와 다르다 — 블랙박스가 급행으로 주는 구간이
    도시철도 구간의 **43%** 라 작은 부류가 아니다(E17). 정차가 1회뿐인 시간대와 첫·막차 시간대는 간격이 아니므로 기대 대기로 쓰지 않고
    첫·막차 구간 밖인지로만 쓴다
  - 주말·공휴일 전철 시각표가 없어 **평일 값으로 떨어뜨린다** — 주말 감편만큼 대기를 짧게 잡는 쪽이므로, 무엇으로 셌는지를 값과 함께 밝힌다.
    버스도 토요일·공휴일 칸이 절반만 채워져 같은 규칙을 쓴다: 그날 요일 유형 값이 없으면 평일 값으로 간다
- **구간 하나의 배차를 기대 대기로 접는 규칙**
  - 출발 시각을 모르고 승객이 아무 때나 온다고 보면 기대 대기는 **배차의 절반**이다. 탑승 구간마다(첫 승차와 환승 모두) 붙는다 — 블랙박스 시간에는
    어느 쪽도 들어 있지 않다(E15)
  - **한 구간에서 함께 기다릴 수 있는 노선이 여럿이면 빈도를 더한다**: 유효 배차 = 1 / Σ(1/hᵢ). 먼저 오는 차를 타기 때문이고, 10분·15분이면 6분이다.
    블랙박스가 한 구간에 여러 차량을 주는 경우(E2)가 이것이다
  - 반대로 **차량 이름 하나에 노선 후보가 여럿 남은 것은 함께 기다리는 노선이 아니다** — 같은 번호가 시·군마다 있어(§5.5) 매칭이 하나로 좁히지 못한
    식별 모호일 뿐이라 한 노선으로 보고 후보들의 평균을 쓴다. 여기에 빈도 합을 쓰면 같은 버스를 두 대로 세어 대기가 절반이 된다 → R21
  - 배차를 모르는 노선은 **빼고** 센다(그만큼 대기를 길게 잡는 쪽이라 과소 추정이 되지 않는다). 다만 **한 구간이라도 기댈 배차가 없으면 경로 전체의
    합은 내지 않는다** — 빠진 구간만큼 짧은 값은 없는 것보다 나쁘다
  - 시간대별 값(도시철도)은 출발 시각이 아니라 **그 구간을 타는 시각**으로 읽는다: 앞 구간들의 차내·도보 시간과 앞서 매긴 대기를 더해 온 시각이다.
    이 절 첫 줄의 시간 정합성을 대기 쪽에서 지키는 방법이다
  - 도시철도의 방향(상·하행)은 우리가 가릴 수 없어 **그 역·시간대의 방향 값을 평균**한다 — 한 방향만 나오는 82쌍과 원천이 한 방향의 회차를 적게 담은
    노선(E14)에서 한쪽만 고르면 배차가 실제보다 크게 나온다
- **실시간 값을 읽는 규칙**: 예측 초는 원천이 그 값을 받은 시각 기준이라 **수신 경과를 뺀** 뒤 쓰고, 빼서 음수면 지난 예측이므로 모름으로 둔다.
  예측 0 은 도착 상태로 가른다 — 당역 진입·당역 도착만 '지금'이고 그 밖은 모름이다. 수신 경과가 큰 행은 버린다. 빈 응답은 운행 없음이 아니다
- **그래서 대기 추정은 세 층으로 떨어진다**: 보정된 예측 초 → (예측이 없을 때) 남은 역·정류소 수 → 정적 배차. 실시간이 원천에 없는 노선군·역과
  주말·공휴일 전철은 처음부터 세 번째 층이다. "다음 버스 14분 후"가 곧 택시 개입의 표적(D 유형)이다
- **연결 버퍼**: 택시 도착 예정 시각은 추정치다. `필요시간 = 택시 ETA × 1.2 + 3분` 같은 버퍼로 취약한 환승을 거른다
- **아직 정하지 않은 것**: 기대 대기의 상한(최대 배차가 하루 단위인 행), 첫·막차 구간 밖을 대기로 어떻게 표현할지(배차 분값 없이 첫·막차만 있는 529 노선이
  여기 걸린다), 수신 경과가 얼마나 크면 버릴지(신선도 임계값), 배차의 분산(정시성)과 환승 실패 위험을 비용에 어떻게 넣을지 → 열린 문제(§0.3).
  도착 응답은 보관할 수 있어 예측과 실제의 오차로 이 값들을 교정할 수 있다(E14)
- 확장: 이동 중 재평가("지금 택시를 타면?")

### 7.6 선호 (설계)
사용자가 길찾기마다 **시간 중시 / 균형 / 비용 중시** 중 하나를 고른다. 단계마다 VOT 값 하나(원/분)를 두고 일반화 비용과 원/분 비추천 기준(§7.4)에 쓴다.
단계별 값은 정해야 한다.
처음에는 소규모 진술선호(SP) 조사로 시간·비용 조합 선택을 받아 다항로짓(MNL)으로 VOT 를 추정하려 했다. 범위를 줄여 사용자가 직접 고르게 했다 → R14

---

## 8. 평가 설계

- **기준선**: 대중교통 전용 최적 경로
- **지표**: 시간 절감(분) · 비용 증가(원) · 절감 효율(원/분) · Pareto 개선율 · **연계 경로가 비지배해인 OD 비율**(핵심)
- **OD 표본**: (도심↔도심 / 도심↔외곽 / 외곽↔외곽) × (피크 / 평시 / 심야) 층화. 편익이 가장 클 것으로 보이는 심야는 반드시 넣는다
- **ablation**: 앵커 전략별 쿼리 수 대비 해 품질(§3.5)
- **현장 검증**: 대표 경로 3–5개를 실제로 타고 시간을 잰다
- **사용자 연구**: 제시한 대안의 선택률·만족도
- **측정 원칙**: 블랙박스 응답은 저장하지 않고 파생 수치만 남긴다. 접지 단계의 실측(E7, E10)은 경기 외곽 5개 OD 를 공통 표본으로 썼다:
  동탄역 → 경기도청(광교) · 수지구청 → 서현역 · 운정신도시 → 대화역 · 다산신도시 → 구리시청 · 배곧신도시 → 안산 중앙역

---

## 9. 실험 상세

각 항목: 질문 · 방법 · 결과 · 결론.

### E1. 대중교통 경로 원천 조사
- **질문**: 상용 지도의 대중교통 길찾기를 프로그램으로 쓸 수 있나. 쓸 수 있다면 실험 규모를 버티나
- **처음 가정과 기각**: "공개 오픈 API 에는 대중교통 경로가 없다(자동차 경로만 있다)" — 틀렸다. 대중교통·도보·자전거 경로 조회가 심사 없이 개방돼 있었다 → R4

| 후보 | 대중교통 | 무료 한도 | 판정 |
|---|---|---|---|
| 카카오맵 | 제공 | 1,000콜/일 | 채택 |
| ODsay | 제공(좌표·요금 포함) | 30콜/일 | 보류 — 유료 전환 시 재검토 가치(R12) |
| TMAP 대중교통 | 제공 | 10콜/일 | 유료 교차검증 후보 |
| 네이버 | 자동차만 | — | 탈락 |
| 구글 | 한국 도보 경로 불가 | — | 탈락 |

- **결론**: 블랙박스 라우터로 쓸 수 있는 것은 하나뿐이고, 그 한도(1,000콜/일)가 알고리즘의 예산 제약이 된다(§2.3)

### E2. 블랙박스 응답 구조
- **방법**: 수원역 → 판교역 등 실제 OD 호출. 응답은 저장하지 않고 구조·건수만 확인했다
- **결과**
  - 1콜에 대안 15개(한 사례: 버스 3 · 지하철 4 · 혼합 8). 기준 경로 확보가 1콜로 끝난다
  - 구간 정류소 이름 목록은 **승차·중간·하차 전부**다. 같은 구간이 급행 8개, 일반 16개 — 급행은 정차역만 담는다 → 가정 "승·하차만" 기각(R5)
  - 환승 도보는 **실제 폴리라인**이다(5–18점, 2점 직선 0%) → 가정 "직선" 기각(R5). 단 **첫·끝 도보는 없다**(제공 범위 밖이라는 공식 답변)
  - 지하철 차량명은 승객 노선명 그대로, 버스 차량은 유형(마을·일반·직행 등) + 노선번호다
  - 정류소 좌표·ID, 노선 ID, 출발 시각, 대기시간이 없다
  - 이름 목록 양끝 = 안내문의 승·하차 이름: 28/28, 이후 5 OD 전부 일치(§5.1)
  - 자동차 경로에 택시 예상요금·통행료가 들어 있다. 같은 OD 에서 몇 시간 차이로 26.3 km · 28,000원 → 28.7 km · 29,300원(실시간 교통)
- **결론**: 알고리즘이 메워야 할 빈칸 목록이 확정됐다(§2.2)

### E3. 버스 정류소의 이중 등록과 상·하행
- **질문**: 서울·경기 정류소 중 같은 물리 정류소가 몇 번씩 등록돼 있나. 무엇으로 식별하나
- **결과**
  - 전국 227,065건. 서울 16,980 = 서울 BIS 12,956 + 경기 BIS 4,024 — 경기 BIS 가 등록한 서울 정류소의 89% 가 서울 BIS 정류소와 50 m 안에 있었다
  - 정류장번호 뒤 9자리가 같은 쌍 **5,405개**, 접두 조합은 전부 (경기, 서울) BIS. 쌍 간 거리 중앙 **5.8 m**, 50 m 이내 **96.7%**
  - 이름이 다른 716쌍은 주로 **이름 절단**(한쪽이 긴 이름의 앞부분)이다
  - 60 m 안의 근접 쌍 32,332개를 거리 × 이름 × 기관으로 나눴다
    - 가까운 거리의 "같은 이름 · 다른 기관" = 이중 등록
    - 15–60 m 의 "같은 이름 · 같은 기관" = 길 양쪽 **상·하행**

| 거리 | 이름 다름·기관 다름 | 이름 다름·기관 같음 | 이름 같음·기관 다름 | 이름 같음·기관 같음 |
|---|---|---|---|---|
| 0–5 m | 263 | 52 | **2,010** | 105 |
| 5–15 m | 433 | 591 | **1,900** | 4,093 |
| 15–30 m | 697 | 1,731 | 1,254 | **6,438** |
| 30–60 m | 1,701 | 3,977 | 1,556 | **5,531** |

  - 정제 결과(설계 단계 예측과 정확히 일치)
    - 대상 지역 레코드 55,047 → 쌍 5,405 = 근접 병합 5,228 + 원거리 병합 170 + 분리 7 → **49,649**
    - 서울·경기 안의 다른 BIS 693건은 번호 체계가 달라 제외했다
  - 상·하행 보존: 서로 다른 9자리 번호끼리의 15–60 m 동명 쌍이 11,547 → 11,347. 사라진 200쌍은 대표 좌표 이동·절단 이름 규칙 때문이고, 합쳐진 상·하행은 없다
    (병합 전 레코드로 세면 이중 등록 때문에 한 쌍이 최대 4번 세져 줄어든 것처럼 보인다)
- **결론**: 9자리 번호 = 물리 정류소 키(§4.1). 거리 기반 중복 제거는 상·하행을 뭉개므로 기각(R2)

### E4. 지역 판정
- **질문**: 정류소의 시군을 무엇으로 판정하나
- **결과**
  - 서울 BIS 레코드 12,956건은 소재지와 무관하게 전부 "서울"로 표기돼 있다. 좌표로는 **1,891곳이 경기**다(고양·성남·부천·광명 순)
  - 도시명 기준과 폴리곤 기준이 양방향으로 어긋났다. 경기 광주시로 표기된 광주광역시 좌표 8건도 있었다
  - 행정동 폴리곤을 시군 단위로 합쳐 판정했다: 서울 25 + 경기 31(일반구 → 시). 5.8만 점 판정에 1초 미만
  - 폴리곤 밖 점 19개는 모두 경기 서해안 매립지·항만(6 m – 1.9 km) → 2 km 흡수 규칙. "가장 가까운 대상 시군"이 아니라 **"가장 가까운 폴리곤이 대상 시군일 때"** 로 정해,
    인접 시도가 더 가까운 점을 끌어오지 않게 했다
  - 2023–2026 사이 일반구 신설·부활(부천·화성)과 인천 구 개편이 있었지만 **시 외곽 경계는 그대로**였다 — 경기를 시 단위로 두는 이유
- **결론**: 지역 = 좌표 + 행정경계(§4.2). 원천 도시명은 쓰지 않는다(R3)

### E5. 택시 요금 — 공개 산식과 자동차 경로 응답
- **질문**: 택시 요금을 공개 산식으로 오프라인 계산할 수 있나. 자동차 경로 응답의 택시 예상요금과 무엇이 다른가
- **산식 조사**
  - 서울: 기본 4,800원/1.6 km · 거리 100원/131 m · 시간 100원/30 s(15.72 km/h 미만 동시 병산) · 심야 +20%/+40%(시간대별) · 시계외 +20% · 복합 최대 60%
  - 경기: 기본 4,800원 공통. 시군 조례에 따라 기본거리 1.6 / 1.8 / 2.0 km 의 3유형(거리·시간 단가도 유형별) · 심야 23–04시 +30% ·
    시계외 +20%(사업구역 경계부터, 서울 진입 포함) · 통행료 실비. 통합사업구역 5곳은 서로 시계외가 아니다
  - 공개 자료로 확정되지 않는 규칙이 남았다: 경기 심야와 시계외의 결합(가산 50% vs 곱 56%), 읍면 복합할증, 요율 인상 논의
- **대조**: 수원역 → 판교역 26.3 km 에서 응답의 택시 예상요금은 28,000원이었다. 산식으로 표준형 주간 거리 요금은 23,600원이고, 시계외 +20% 면 28,320원이다
  → 응답은 시계외 할증을 넣은 값으로 보인다(한 사례 대조이고 계산 방식은 문서화돼 있지 않다). 통행료는 별도 필드로 온다
- **결론**: 택시 요금은 응답값을 쓴다(§7.1). 산식은 시군별 규칙이 복잡하고 확정되지 않는 부분이 남으며, 실시간 교통을 반영하지 못한다 → R13

### E6. 노선–정류소 순서 데이터
- **질문**: 전략 (b)의 입력인 노선별 정류소 순서·배차를 어디서 얻고, 우리 정류소 키와 어떻게 잇나
- **결과**
  - 전국 정류장 목록의 번호 = 국가 대중교통 정보의 정류소 ID. 경기 ID = 접두 + **경기 BIS 정류소 ID(9자리)** — 우리 키와 변환 없이 조인된다.
    서울도 접두 + 서울 정류소 ID(9자리)다 — 버스 순서표를 만들며 조인율로 확인했다(아래)
  - 경기: 기반정보 일괄 자료 한 번으로 노선·노선별 경유정류소(상·하행, 순번, 정류소 ID, 좌표)·요일 유형별 배차를 얻는다
  - 서울: 노선별 정류소(순번, 정류소 ID, ARS, 좌표) 파일. 상·하행 열은 없다. 노선 기본정보 파일은 노선명·ID 뿐이라 배차간격 대표값은 조회로만 얻는다
  - 시간대별 배차는 신청형 자료(GTFS)뿐이다
  - 도시철도 역 순서: 역번호로 정렬해 이으면 서울 지하철 계열은 인접역 최대 3.3 km 로 깔끔하다. 그러나 코레일 노선은 뒤섞인다(수원→서울숲 31 km,
    지평→서울역 59 km 점프) → 기각(R7). 노선정보 표준데이터의 **정거장 구성**(운행 순서대로 역 나열)을 원천으로 한다
  - 정거장 구성의 한계: 일부 노선이 오래됐다(서해선 일산 연장, 인천 1호선 검단 연장, 별내선 이전). 옛 역명(개명 전 이름)·약칭·오타가 있어
    역 파일로 보충하고 이름 대응표가 필요하다
- **도시철도 역 순서표 구축**: 정거장 구성을 역 DB 에 붙이며 원천의 결함을 보정표로 하나씩 고쳤다(26건)
  - 노선 식별: 원천 노선번호가 틀리거나 겹친다(분당선에 수인선 번호, 경부선·1호선 같은 번호) → 노선번호 + 노선명으로 고른다
  - 구분자(`,` · `+`)·따옴표·줄바꿈이 노선마다 다르고, 역번호 형식도 역 DB 와 다르다(`0405`/`405`, `D004`/`D04`, `2111`/`211-1`) → 역 매칭은 이름 키로
  - 목록 안에 지선이 섞여 있다(경부선의 광명·서동탄, 5호선 끝의 마천 지선, 경의중앙선의 서울역 지선) → 분기역부터 시작하는 별도 순서로 뗀다.
    지선 목록에는 분기역이 빠져 있다(2호선 성수·신도림) → 넣는다. 순환은 표시한다(2호선, 6호선 응암순환). 경춘선 상봉–광운대는 노선도대로 지선
  - 빠진 구간: 서해선 일산 연장, 별내선, 수인선이 안산과천선과 함께 쓰는 한대앞–오이도, 분당선 왕십리·청량리, 경의중앙선 용산–회기와 대곡,
    경춘선 청량리–망우(원천 목록이 신내부터)
  - GTX-A 는 노선정보에 없어 역 참조표로 만든다. 북부(운정중앙–서울역)와 남부(수서–동탄)는 이어지지 않은 두 순서다
  - 옛 역명·약칭: 신길온천 → 능길, 디엠시 → 디지털미디어시티
  - 운영 노선이 갈리는 곳(진접선–4호선–안산과천선, 분당선–수인선, 7호선 본선–부천 연장)은 잇는 역을 넣어 같은 노선군 안에서 이어지게 했다
- **결과**: 39개 순서(지선 포함, 22개 노선군). 역 DB 685역이 모두 어느 순서에 들어가고, 노선군마다 한 덩어리로 이어진다(GTX-A 만 북부·남부 두 덩어리).
  권역 밖 52역은 노선 끝에서만 빠진다. 순서 안의 역은 모두 역 DB 좌표를 가진다(원천 좌표 결함 3역은 E8 에서 고쳤다)
- **버스 노선–정류소 순서표 구축**: 서울 노선별 정류소(718 노선)와 경기 노선–정류소 일괄 자료(3,137 노선)를 합쳤다
  - 두 원천의 노선은 **겹치지 않는다** — 경기로 드나드는 서울 노선은 서울 원천에만, 경기 노선은 경기 원천에만 있다
  - 두 원천 모두 **순번 = 운행 순서**다. 경기는 상행 → 하행이 한 순번열에 이어지고, 첫 하행의 순번이 노선의 회차 순번과 모든 노선에서 같다.
    서울은 상·하행 표시 없이 회차를 지나 한 줄로 이어진다(한 노선이 금호역을 40번·68번에 두 번 지난다). 그래서 **같은 노선에서 순번이 작으면 상류** —
    방향 표시 없이 (b)의 상류·하류를 정한다
  - 키 조인은 정류소 ID 를 **자기 BIS 레코드**로 먼저 찾는다. 분리한 이중 등록 7쌍은 두 BIS 가 같은 번호를 서로 다른 정류소에 쓴 것이라, 번호만으로
    찾으면 다른 정류소에 붙는다(§4.1). 처음에는 고유 정류소 서울 98.5% · 경기 96.2% 가 DB 행에 붙었고, 원천 좌표와 DB 좌표 차이는 중앙 0 m · p99 24 m
    (300 m 넘게 다른 곳 8개 — 같은 번호가 옮겨졌거나 다른 정류소에 다시 쓰였다). 정류소 DB 를 최신 파일로 바꾼 뒤 **99.1% · 98.2%**, 300 m 넘는 곳 0 → E12
  - 못 붙은 것: 대상 밖 구간(인천·강원·충청, 인천공항)은 뺐다 — 순번은 비어도 순서는 그대로다. 목록 중간이 빠지는 노선 144개는 대상 밖을 지나거나 거기서 회차한다(인천 108 · 충청 22 · 강원 14).
    대상 안 970곳은 DB 원천 기준일(2025-10-31) 뒤에 생긴 정류소 791과 통과 노드 179였다. 원천 이름·좌표로 순서표에 남기지만 이름 매칭(§5, §6)에는
    걸리지 않는다 → 정류소 DB 를 최신 파일로 바꿔 97곳(96 은 서울 `(가상)` 통과 노드)으로 줄였다(E12)
  - 서울 노선 원천에 배(한강버스) 노선 3개가 버스처럼 섞여 있다 → 뺀다
  - 통과 노드 표기가 원천마다 다르다 — 경기 `(미정차)`, 서울 `(가상)`. 둘 다 통과 노드로 판정해 앵커 후보에서 뺀다
  - 인천 BIS 노선은 두 원천 어디에도 없다. 경기로 드나드는 인천 노선은 대상 지역 안에서도 빠진다
  - 결과: 3,852 노선 · 235,938행 · 고유 정류소 49,883. 정류소 DB 50,019곳 중 어느 노선에도 없는 곳은 233
  - (b) 1–4단계를 흉내 낸 한 사례(동탄역 → 경기도청(광교), 택시 도달은 직선 5 km 로 대신): D 400 m 안 정류소 11곳 → 노선 25개 → O 근처에 상류 정류소가 있는 노선 5개
- **결론**: 전략 (b)의 정적 노선 DB 를 키 변환 없이 구축했다. 버스는 서울·경기 두 원천을 합친 순서표, 도시철도는 정거장 구성 + 보정표로 만든 역 순서표다

### E7. 승·하차 매칭 정확도 — 경기 외곽 5 OD
- **방법**: 5 OD(§8)를 한 번씩 호출해 승·하차 매칭(§5)을 적용. OD 안에서 (종류, 이름 키, 기준점)이 같은 항목은 한 번만 셌다(대안 15개가 같은 정류소를 반복하므로)

| OD | 경로(버스/지하철/혼합) | 매칭 n | 매칭 | 모호 | 미매칭 | 오차 중앙 · p90 · 최대 (m) |
|---|---|---|---|---|---|---|
| 동탄 → 광교 | 8 / 2 / 5 | 27 | 24 | 2 | 1 | 11.6 · 105.8 · 159.8 |
| 수지 → 서현 | 6 / 3 / 6 | 23 | 22 | 1 | 0 | 15.8 · 97.1 · 104.3 |
| 운정 → 대화 | 11 / 0 / 4 | 15 | 10 | 3 | 2 | 6.7 · 17.3 · 18.2 |
| 다산 → 구리 | 10 / 1 / 4 | 30 | 29 | 1 | 0 | 7.1 · 12.0 · 265.3 |
| 배곧 → 안산 | 6 / 0 / 9 | 16 | 16 | 0 | 0 | 7.1 · 71.1 · 135.6 |

- **합계**: 버스 80 = 매칭 73 · 모호 7 · **미매칭 0**(발견률 100%). 지하철 31 = 28 · 0 · 3. 오차(108건) 중앙 **8.6 m** · p90 91.5 · 최대 265.3.
  모든 매칭이 key 수준이었다
- **원인 분류**
  - 지하철 미매칭 3 = 전부 GTX-A(원천 부재) → E8
  - 버스 모호 7 = 동명 정류소 2–4곳이 10 m 안(상·하행, 중앙차로·가로변). 1위와 차이 0.7–9.7 m → 방향 판정 과제(§5.4)
  - 첫 실측(수원역 → 판교역)의 버스 미매칭 2 = 블랙박스 이름에 출구 표기가 붙어 DB 의 짧은 이름(3.8 m · 10 m 옆)과 키가 달랐다.
    prefix 는 최소 길이에 막혔다 → 부분 이름 단계 필요(R9)

### E8. 도시철도 보강과 원천 결함
- **GTX-A**: 역 원천·노선 원천 모두에 없었다. 9역을 참조표로 넣었다(좌표는 공공 지오코딩, 운영사 자료로 이중 확인)
  - 신설역은 역 POI 가 없어 출구·승강장 POI 의 중심을 썼다. 주소 지오코딩은 넓은 역사 필지의 중심을 줘 100–500 m 벗어났다
  - 운영사 좌표 대비 오차 35–57 m
  - **실경로 재확인**: E7 의 미매칭 3건을 포함한 GTX-A 승·하차 5건이 모두 매칭됐다(북부 5.0 m · 1.4 m, 남부 106–171 m — 깊은 승강장과 지상 좌표의 차이)
- **원천 좌표 결함 3역 → 좌표 보정표로 고쳤다**
  - 다른 지역의 동명 역 좌표가 들어간 역: 경의중앙선 양원에 경북 봉화 양원역(영동선) 좌표 → 지역 판정에서 빠져 매칭 불가였다(190 km)
  - 옆 역 좌표가 들어간 역 2곳: 5호선 마곡 = 발산 좌표, 4호선 이촌 = 신용산 좌표 → 오차 ≈0.9–1.2 km 로 오매칭 가능했다
  - 다시 찍은 좌표: 공공 지오코딩의 장소 검색 '지하철역' POI. 지하 주소의 주소 지오코딩은 수 km 벗어나 쓰지 않았다.
    확인: 양원은 주소 지오코딩과 8 m, 이촌은 같은 역 경원선 행과 58 m, 마곡은 같은 이름 지하철역 POI 3곳이 100 m 안
- **원천에 잘못 들어간 노선 행**: 경의중앙선 광운대(경의중앙선은 광운대에 서지 않는다) → 뺐다. 원천 행 보정은 (노선, 역번호) + 역 이름이 모두 맞아야
  적용되므로, 원천이 바뀌면 보정이 조용히 엉뚱한 행에 붙지 않고 멈춘다
- **같은 이름인데 300 m 넘게 떨어진 실제 환승역**(서울역·잠실·노원)은 노선 필터가 없으면 다른 역으로 보여 모호가 될 수 있다

### E9. 경유지 파라미터 부재
- **결과**: 대중교통 경로 조회의 요청 파라미터는 출발·도착 좌표·명칭·좌표계뿐이고 **경유지가 없다**. 같은 계열의 도보·자전거 조회에는 경유지가 있다
- **의미**: 연계 경로는 **구간을 따로 호출해 이어 붙인다** — 택시 O→P + 대중교통 P→D. §3.2 의 `1 + N` 설계가 이 전제와 맞는다. mid-leg(C)는 한 경로를 두 번 이상 나눠 불러야 해 예산이 더 든다

### E10. 경로 위 모든 정류소 위치
- **방법**: E7 과 같은 5 OD. OD 안에서 똑같은 구간은 한 번만 셌다(구간 101개)

| | 정류소 | DB 위치 | 수준 | 추정 위치 | DB 위치의 선까지 거리 |
|---|---|---|---|---|---|
| 버스 | 827 | **818 (98.9%)** | key 807 · prefix 11 | 9 | 중앙 4.2 · p90 9.5 · 최대 89.7 m |
| 지하철 | 92 | **90 (97.8%)** | key 90 | 2 | 중앙 4.5 · p90 104.3 · 최대 265.3 m |

- 정류소 순서 역전(선 위 거리가 앞 정류소보다 30 m 넘게 뒤로) **0건** — 잘못 붙은 같은 이름 정류소가 없다는 간접 증거
- 추정 11건: 버스 9건 중 7건이 **복합 이름**(블랙박스 이름 `A.B`, DB 는 한쪽만), 나머지는 DB 에 없는 이름. 지하철 2건은 **역명 변경**(블랙박스가 옛 이름) → 별칭 한 줄로 풀린다
- 탐욕 선택(1안)의 실패는 합성 사례로 재현했다. 되돌아오는 구간에서 같은 이름 두 번째가 첫 통과 지점에 붙어, 뒤따르는 추정 위치가 선 한가운데로 갔다.
  순서 보존 정렬(2안)은 되돌아오는 구간 위에 놓았다 → R8

### E11. 대중교통 범위 요금
- 동탄역 → 경기도청(광교) 대안 15개 중 3개가 값 하나 대신 **범위(1,850–3,200원)** 로 왔다. 가정 "요금은 값 하나" 기각 → R6
- 정렬과 비용 비교에서는 범위의 낮은 쪽을 쓴다(§7.2)

### E12. 정류소 DB 갱신 — 두 BIS 의 최신 정류소 파일
- **질문**: 연 1회 갱신되는 전국 원본으로 만든 DB 가 노선 원천(월·일 단위 갱신)과 얼마나 어긋나고, 최신 파일로 바꾸면 매칭이 나아지나
- **계기**: 노선 순서표를 만들 때 노선 위 대상 안 정류소 970곳(새 정류소 791 · 통과 노드 179)이 DB 에 없었다(E6)
- **원천**: 경기 = 기반정보 일괄 자료의 정류소 파일(경기 BIS 가 등록한 전부 — 서울·인천 소재 포함). 서울 = 서울 정류소 위치 파일 — **서울 소재만** 담는다
  (경계에 붙은 경기 소재 17곳 말고는 모두 서울)
- **교체 규칙**: 두 파일에 있는 레코드는 최신 값을 쓴다. 최신 파일에서 빠진 레코드는 폐지로 본다 — 단 서울 파일은 서울 소재만 담으므로,
  서울 BIS 가 등록한 서울 밖 정류소(1,880)는 전국 원본 것을 둔다. 배 선착장(한강버스)은 뺀다. 그 뒤 쌍 판정·지역 판정은 §4 규칙 그대로다
- **결과**
  - 경기 BIS: 둘 다 있음 45,037(이름 키 바뀜 689 · 50 m 넘게 옮김 122) · 새 937 · 폐지 630. 서울 BIS: 둘 다 11,022(446 · 64) · 새 206 · 폐지 54
  - DB 에서 빠진 519곳 중 지금 노선이 지나는 곳은 2곳뿐이다 — 폐지 판정이 맞다는 근거
  - 서울 정류소 파일에 **좌표 오류 4곳**(0.98–14.8 km): 전국 원본과 서울 노선 파일은 서로 0 m 로 같은데 정류소 파일만 다르다 → 좌표 보정표로 고쳤다.
    50–300 m 차이 63곳은 노선 파일이 반씩 편을 들어(정류소 파일 쪽 33 · 전국 원본 쪽 29) 판단하지 않았다
  - DB 49,649 → **50,019**. 같은 쌍 판정 규칙으로 근접 병합 5,443 · 원거리 병합 169 · 분리 17쌍. 분리가 7 → 17 로 늘었다 — 두 BIS 가 한 정류소의
    이름·좌표를 따로 고쳐 50 m 를 넘은 쌍이고, 일부는 같은 정류소로 보인다(검토 대상). 병합 뒤 새로 생긴 상·하행 쌍은 1개
  - 노선 순서표: 고유 정류소 서울 **99.1%** · 경기 **98.2%**(나머지는 모두 대상 밖)가 DB 에 붙는다. DB 밖 대상 안 정류소 970 → 97(96 은 서울 `(가상)` 통과 노드).
    노선 원천과 이름 키가 다른 정류소 1,201 → 112, 300 m 넘게 다른 좌표 8 → 0
  - 5 OD 재측정(같은 날 응답에 옛·새 DB 를 똑같이 적용, OD 좌표는 E7 과 비슷한 곳): 버스 승·하차 미매칭 1 → 0, 매칭 수준 prefix 2 → 0(모두 key),
    경로 위 버스 정류소의 DB 위치 845/857 → **855/857**. 지하철은 같다
- **결론**: 정류소 DB 는 두 BIS 의 최신 정류소 파일로 만든다. 전국 원본은 서울 파일이 담지 않는 서울 BIS 의 서울 밖 레코드만 채운다 → §4.1, R15

### E13. 순서표로 접지 — 진행 방향 판정과 구간 읽기
- **질문**: 길 양쪽에 붙은 동명 정류소를 무엇으로 가르고, 이름으로 못 찾는 정류소를 어떻게 놓나
- **방법**: E7 과 같은 5 OD 를 **다시 호출한 응답**에 순서표를 쓴 접지와 안 쓴 접지를 나란히 적용해 비교한다 — 두 접지가 **같은 응답**을 본다.
  승·하차는 E7 과 같은 기준으로 세고(같은 종류·이름 키·기준점은 한 번), 위치는 구간 중복을 뺀 뒤 센다. 응답이 그때와 달라 승·하차 77건 · 구간 100개로
  E7(80건) · E10(구간 101개)과 표본 크기가 다르다 — 표의 두 열끼리만 비교한다

| | 순서표 없이 | 순서표 사용 |
|---|---|---|
| 버스 승·하차 77건 | 매칭 70 · **모호 7** | 매칭 77 · **모호 0** |
| 경로 위 버스 정류소 828곳 | 826 (99.8%) | **828 (100%)** |
| 경로 위 지하철역 92곳 | 90 (97.8%) | **92 (100%)** |
| 역의 선까지 거리 | 중앙 5.0 · p90 106.0 m | 중앙 **4.1** · p90 104.3 m |

- **구간 100개 중 98개**가 순서표에서 위치를 얻었다. 나머지 2개는 급행이다 — 정차역만 오므로 역 개수가 맞지 않는다
- **독립 검증 1(이름 순열)**: 승차 → 하차 구간의 정류소 이름을 블랙박스 목록과 자리마다 비교했다. 검사한 78쌍 중 노선을 찾은 **76쌍이 모두 들어맞았다** —
  74쌍은 이름이 전부 같고(최대 35개 연속), 2쌍은 개명된 이름 하나만 다르다. 나머지 2쌍은 차량 이름이 순서표에 없던 경우다 — 이름을 느슨하게 맞추는 단계를 넣어 그 2쌍도 노선을 찾게 했지만, 순열 검증을 그 상태로 다시 돌리지는 않았다
- **독립 검증 2(기하)**: 선은 블랙박스가 준 것이라 순서표와 무관하다. 순서표가 놓은 위치의 선까지 거리 분포가 그대로였고(버스 중앙 4.2 · p90 9.6 m),
  선에서 150 m 넘게 떨어진 곳은 0이다
- **바뀐 것**
  - 버스 승·하차 1건: 0.7 m 더 가까운 반대 방향 정류장을 버렸다. 순서표 쪽이 선에서 오히려 가깝다(17.3 → 7.5 m). 반대편은 같은 노선의 순서에서 27·56칸 떨어져 있었다
  - 경로 위 버스 정류소 14곳: 좌회전 전용 정류소 · 길 건너편 · 개명된 정류소
  - 지하철 2곳: 개명역(`신길온천` → DB `능길역`). 이름으로는 못 찾아 선 위 추정이었는데, 실제 역은 그 추정점에서 **286 m** 떨어져 있었고 선에서는 0.2 m 다
- **이름으로는 못 찾는 4곳**을 순서표가 놓았다 — 버스 `매탄2동행정복지센터`(DB `매탄2동주민센터`) 2곳, 지하철 개명역 2곳
- **결론**: 진행 방향은 운행 순서로 판정하고(§5.5), 구간의 정류소는 순서표에서 읽는다(§6.6). 부분 이름 매칭은 필요 없어졌다 → R16

### E14. 배차간격과 실시간 도착 — 대기 모델의 입력
- **질문**: (b)의 근사 점수(§3.2)와 대기시간 모델(§7.5)이 쓸 배차간격·실시간 도착을 어디서 얻고, 값의 성질이 무엇이며, 무엇이 빠지나.
  E6 에서 "시간대별 배차는 신청형 자료뿐"이라고 본 그 자료를 받아 확인했다
- **원천**
  - 버스 배차 = 경기 기반정보 노선 파일(요일 유형 4개의 최소·최대 배차와 방향별 첫·막차) + 서울 노선 목록(요일 구분 없는 대표 배차 하나와 첫·막차)
  - 도시철도 배차 = 신청형 시각표 자료(GTFS, **2025년 3월 평일 1일** 기준)의 정차 기록 **18,889,140행**을 훑어 직접 센다
  - 실시간 도착 = 버스는 경기·서울 두 BIS 의 도착정보, 도시철도는 서울 쪽 실시간 도착 1종

- **버스 배차 — 값의 성질**
  - 경기 원천의 두 배차 항목은 이름이 첨두/비첨두처럼 보이지만 항목 정의는 **그 요일의 최소·최대**다. 두 해석은 부등호 방향이 같아 데이터로는 가릴 수 없어
    정의를 따랐다. 최소가 최대보다 크게 들어온 행이 **78개** 있어 알고리즘은 두 값의 순서를 가정하지 않는다
  - 0 과 빈값은 정보 없음이다. 한쪽만 오는 행도 있다(최대만 583 · 최소만 176). 최대 배차가 60분 이상인 행이 **4,129**(37.8%)이고 최댓값이 1,440분이라,
    배차/2 를 그대로 쓰면 기대 대기가 하루가 된다 → 상한이 필요하다(§7.5 에서 아직 정하지 않았다, §0.3)
  - 커버리지는 두 층으로 봐야 한다 — 원천에 행이 있다는 것과 배차 분값이 있다는 것은 다르다

| 층 | 노선 | 비율 |
|---|---|---|
| 순서표 노선 | 3,852 (경기 3,137 · 서울 715) | — |
| 원천에 행이 있는 노선 | 3,823 (경기 3,109 · 서울 714) | 99.2% (99.1% · 99.9%) |
| **배차 분값이 있는 노선** | **3,294** | **85.5%** |

  - 값이 없는 쪽도 두 층이다 — 배차 분값 없이 첫·막차만 주는 **529 노선**은 대기를 첫·막차 구간 안이라는 사실로만 쓸 수 있고, 행이 아예 없는 29 노선
    (경기 28 · 서울 1)은 원천이 배차·첫막차 칸을 모두 비웠거나 목록에서 사라진 노선이다. 값이 빠진 쪽이 변두리 노선만은 아니다 — 529 노선의 유형은
    일반 325 · 마을 94 · 수요응답 54 · 직행 45 · 광역 6 · 간선 4 · 지선 1 로 **일반형 시내버스가 가장 많다**(행이 아예 없는 29 노선은
    마을 22 · 수요응답 5 · 공항 1 · 유형 값이 빈 서울 노선 1 로 치우친다). 첫·막차만으로 대기를 매기는 폴백이 주요 노선에도 걸린다는 뜻이다
  - 요일 유형별로도 고르지 않다(경기 원천, 노선 3,109 기준): 평일 2,567(82.6%) · 일요일 2,294(73.8%) · 토요일 1,559(50.1%) · 공휴일 1,554(50.0%).
    토요일·공휴일 칸이 절반이라 그 요일은 평일 값으로 떨어뜨린다(§7.5). 서울 행 1,308 중 1,197 에 대표 배차가 있고 요일 구분이 없어 평일로 둔다
  - 경계를 넘는 노선은 **배차를 담은 노선 정보 파일에서는 두 원천에 다 나오고(594 노선) 값이 서로 다르다**(한 노선이 경기 10–25분 · 서울 20분,
    다른 노선이 경기 12–14분 · 서울 17분) — 노선–정류소 순서 원천이 겹치지 않는 것(E6)과는 다른 성질이다.
    경기 원천에 값이 없는데 서울 원천에만 값이 있는 노선은 **0개** — 겹침이 커버리지를 넓혀 주지 않으므로 노선이 속한 원천 값을 쓴다.
    노선 이름은 원천마다 지역명이 붙어(같은 노선이 `75` · `75구리`) 조인 키로 쓸 수 없어 번호로 이었다 — 번호도 원천 안에서 유일하지는 않아
    이 594 는 상한으로 읽어야 한다

- **도시철도 배차 — 세는 방법과 정의가 깨지는 자리**
  - 역·방향·시간대별 정차 횟수를 세어 **배차 = 60분 / 그 시간대 정차 횟수**로 둔다. 승차할 수 없는 종착은 세지 않아 (역, 노선군) 685쌍 중 **82쌍은 한 방향만**
    나온다 — 방향이 없는 것을 운행 없음으로 읽으면 안 된다
  - 원천은 도시철도·경전철을 방향·계통까지 쪼개 **91개 노선**으로 담는다. 우리 22개 노선군에 매핑되는 **63개**를 쓰고 나머지 28개(부산·대구·광주·대전·인천 등)는
    대상 밖이다. 인천 1·2호선은 원천에 있지만 우리 노선군이 아니다
  - 급행·지선·순환을 원천이 다른 노선으로 담으므로 한 역에 서는 모든 패턴이 함께 세어진다 — '그 역에서 아무 열차나' 기다리는 배차다
  - 결과는 역 DB **685행 중 683행**(원천에 없는 역 1 = 청산역 · 같은 역의 다른 행이 값을 가진 1)
  - 정의가 깨지는 자리: 정차가 1회뿐이라 60분으로 적힌 행 **738개**(간격이 아니라 그 시간대 1회 정차다) · 운행이 시작·종료되는 시간대(첫차 시간대 배차 중앙
    20.0분 vs 8시 5.0분 — 첫차 전의 무한 대기와 첫차 뒤의 정상 배차가 한 칸에 섞인다) · 자정 넘는 운행은 24·25시로 적힌다(1,070행)
- **시간대별 배차 표본** (평일, 역·방향 단위. 묶은 시간대의 배차 = 그 묶음의 시간 수 × 60 / 묶음 안 정차 합)

| 노선군 | 역·방향 | 07–09 | 09–17 | 17–19 |
|---|---|---|---|---|
| GTX-A | 대곡 상행 | 6.0 | 8.7 | 6.3 |
| GTX-A | 동탄 상행 | 17.1 | 19.2 | 20.0 |
| 신분당선 | 광교(경기대) 상행 | 4.8 | 8.1 | 4.8 |
| 수인분당선 | 기흥 상행 | 5.7 | 13.0 | 12.0 |
| 경강선 | 이천 상행 | 13.3 | 19.2 | 17.1 |
| 경춘선 | 평내호평 상행 | 12.0 | 22.9 | 15.0 |
| 서해선 | 원시 상행 | 10.0 | 14.1 | 10.0 |
| 경의중앙선 | 문산 상행 | 30.0 | 48.0 | 30.0 |
| 용인에버라인 | 명지대 상행 | 3.2 | 5.9 | 4.1 |

  - 노선 이름만으로는 배차를 말할 수 없다. **역별로 크게 갈리는 노선군이 있다** — 평일 8시 기준 1호선 2.2분(신도림)–60분(전곡) 27배,
    경의중앙선 5.5분(가좌)–30분(원덕) 5.5배, GTX-A 6.0분(대곡)–20분(동탄) 3.3배. 그래서 대기의 입력 단위는 노선이 아니라 (역, 노선군, 방향, 시간대)다
  - 다만 편차는 노선군마다 다르다. 22개 노선군의 8시 최대/최소 비는 **8개가 3배 미만**(신림선·용인에버라인·우이신설선 1.0 · 신분당선 1.1 ·
    공항철도 1.2 · 8호선 1.3 · 3호선 2.0 · 7호선 2.9)이고 나머지가 3.0–27.3배다 — 노선군 평균이 통하는 곳도 있다는 뜻이지만, 어느 노선군이 그런지는
    노선 이름으로 알 수 없어 단위를 역으로 둔다
  - 한 노선군에 운행 계통이 갈리는 곳(GTX-A 북부/남부, 경의중앙선 본선/서울역 지선)은 노선군 평균이 어느 구간에도 맞지 않는다 — 노선군으로 올려 평균하지 않는다
  - 첨두 가산이 없는 구간도 있다(동탄 상행은 퇴근 시간대가 오히려 넓다). 극단은 경의중앙선 지평 상행 **120 / 240 / 120분**(하루 6회)이라
    정적 배차만으로 기대 대기 60분이 나오고, 그 앵커는 가지치기에서 자동으로 떨어진다
- **도시철도 배차의 한계**: 평일 1일뿐이라 **주말·공휴일 시각표가 없다**(버스는 경기 원천이 요일 유형별 값을 준다). 원천이 역을 빠뜨리거나 옛 이름으로 담은 자리가 있다 —
  자양(뚝섬한강공원)은 옛 이름 `뚝섬유원지` 로 들어 있어 보정표 한 줄로 이었고, 청산역은 같은 날 개통한 전곡·연천이 담겼는데도 원천에서 빠졌다
  (개통 시점 문제가 아니다). 원천 자체가 7호선 하행 회차를 적게 담아(하행 75 vs 상행 204) 그 방향 배차는 실제보다 넓게 나온다
- **버스 시각표는 기각**: 같은 자료의 버스 시각표는 첫차~막차를 대표 배차로 균등 분배한 **합성값**이다. 표본으로 본 두 노선에서 경기 300번 한 계통이
  04:20부터 정확히 13분 간격(연속 79/79), 서울 143번이 6분 간격(182 운행)이고 시간대 변화가 0 이다 — 같은 노선의 원천 배차값은 경기 10–15분 · 서울 6분이다 → R18

- **실시간 도착 — 가능한 범위와 불가능한 범위**
  - 버스는 두 BIS 가 각자 제공하고, 우리 정류소 DB 와 **같은 번호 체계**를 받는다(원천 표기의 접두어만 뗀다) — 별도 매칭표 없이 정류소를 지목할 수 있다.
    두 BIS 모두 도착 예상시간과 남은 정류소 수를 주고, 서울은 막차 여부를 더한다 — 대기 모델이 읽는 값은 도착 예상시간과 남은 정류소 수다
  - 경계 정류소는 두 BIS 가 모두 등록하므로(두 BIS 동시 등록 **5,612곳** — 병합 뒤 두 원천 ID 를 함께 가진 행을 최신 DB 에서 센 값이라, 교체 전 자료에서 번호 뒤 9자리가 같은 쌍을 센 E3 의 5,405쌍과는 기준이 다르다. 서울 BIS 가 등록한 경기 소재 **1,896곳** — 고양 443 · 성남 321 · 부천 233 …)
    한쪽만 부르면 그 정류소의 도착을 다 보지 못한다. 배차에서 겹친 594 노선과 같은 구조의 문제다
  - 도시철도는 원천이 1종이고 **번호가 아니라 이름으로만** 물을 수 있다. 22개 노선군 중 **19개**를 덮고, 김포골드라인 10 · 용인에버라인 15 · 의정부경전철 15 =
    **우리 역 40개**는 원천에 노선 식별자 자체가 없어 재조회로도 얻을 수 없다. 원천 문서는 서울 밖 구간을 제공하지 않는다고 적었지만 역정보에는 경기 역이
    들어 있고 실호출에서도 경기 역(GTX-A 동탄)의 도착이 왔다
  - 역명은 **정확 일치**만 받아 우리 표시 이름으로 물으면 오류가 아니라 빈 목록이 온다(조용히 실패한다) → 역마다 원천 이름·식별자를 미리 이어 둔다
  - 조회 가능한 642행 중 표기가 다른 행이 **325개**다: '역' 접미만 다른 행 259 · 괄호 부제를 지우면 같은 행 59 · 이름 자체가 다른 행 **7**.
    마지막 7 이 표로 이어야 하는 전부다 — 별칭 1(이수 ↔ 총신대입구(이수))과 보정 6(응암순환(상선) · 뚝섬유원지 · 지제 · 신길온천 2행 · 세종왕릉).
    보정표는 자동 매칭보다 **먼저** 적용한다 — 원천에 살아 있는 옛 이름(`신길온천`)이 엉뚱한 행에 붙지 않게
  - **보정표는 원천마다 따로다**: 실시간 쪽은 이름이 다른 **6행**이고(역별 제공이 빠진 진접선 3역은 보정이 아니라 조회 불가로 따로 센다),
    정적 시각표 쪽은 6행(신길온천→능길 2행 · 화전→한국항공대 · 운동장·송담대→용인중앙시장 · 당고개→불암산 · 뚝섬유원지→자양)이라 내용이 다르다 — 두 원천의 개명 반영
    시점이 어긋나 있다. 이름을 조인 키로 쓰는 단계는 모두 원천별 보정표를 전제한다
  - 결과: 역 DB 685행 중 **642행**을 물을 수 있다(정규화 규칙으로 자동 일치 **635** · 별칭 1 · 보정 6). 나머지는 역별 제공이 빠진 진접선 3역
    (열차 목적지로만 등장)과 노선군이 없는 40역이다. 유일 이름은 **512개**다. 값이 역 단위가 아니라 이름 단위로 묶여 오고 환승역에는 여러 노선이
    섞여 있어, 식별 단위는 이름이고 역은 그 안에서 갈라야 한다(같은 역을 두 행으로 담은 곳은 한 역으로 묶는다).
    매핑이 있다는 것과 응답이 온다는 것은 다르다 — 운행 시간대 밖의 빈 목록은 정상이다
- **도착 예측값의 성질** — 대기 모델이 반드시 알아야 하는 함정
  - 도시철도 예측 초는 **원천이 그 값을 받은 시각 기준**이라 우리가 받은 시각까지의 경과를 빼야 남은 대기가 된다(실측 경과 41–281초).
    원천 시계가 우리보다 앞서 경과가 음수로 오기도 한다(실측 -136초)
  - 예측 **0 에는 두 가지가 섞여 있다** — 당역 진입·당역 도착이면 정말 지금이고, 그 밖(당역 출발 · 앞 역 출발·진입·도착 · 운행중)이면 값이 없다는 뜻이다.
    가르지 않고 0 으로 쓰면 **20역 떨어진 열차가 '지금 도착'** 이 된다(서울역 1호선 실호출에서 나온 사례다). 앞 역 '도착'을 도착으로 읽으면 같은 함정에 빠진다
  - 예측이 없을 때의 유일한 단서는 안내 문구의 **남은 역·정류소 수**다
  - 버스도 예측이 없는 노선이 섞여 오고, 운행종료·출발대기가 0 으로 온다 — 0초 후가 아니다. 원천이 스스로 밝히는 건수가 실제 목록과 어긋난 사례가 있어
    건수는 값으로 쓰지 않는다
  - **실시간을 구했다는 것이 초 단위 예측을 얻는다는 뜻은 아니다**: 실호출 표본 3역 13건(2026-09-12 · 신길온천·세종왕릉·동탄)이 **전부 예측 0** 으로 왔고,
    지금으로 해석할 수 있는 것은 당역 도착·진입 2건뿐이었다. 나머지는 남은 역 수만 문구로 왔다(12번째·6번째·2번째 전역)
- **기각한 전국 단일 원천**: 전국을 한 원천으로 덮는 버스 도착정보도 확인했다 — 도시 목록에 서울이 없고 경기도 일부 시군만 있으며, 같은 정류소에서 오는 도착
  건수가 지역 BIS 의 10분의 1 수준이었다(실호출 기록. 응답은 남기지 않아 건수는 그때의 측정값이다) → R17
- **쿼터가 강제하는 사용 방식**: 실시간 도착은 대중교통 경로와 **별개 풀**이지만 규모가 비슷한 일일 한도를 갖는다 — §2.3 의 `1 + N` 예산은 그대로 두고
  앵커당 실시간 조회를 따로 센다. 후보 단계에서 정류소·역마다 부를 수 없고,
  검증 단계의 최종 앵커 몇 개에만 배정한다 — Tier 2 자원이다
- **남은 확인**: 실시간 매핑 건수는 2026-09-02 자 역정보 파일 기준의 **조회 가능 수**이고, 도착 응답을 실제로 확인한 역이 642 라는 뜻이 아니다.
  실호출로 센 도착 건수는 측정 시각에 절대적으로 의존한다(새벽에 재면 0건) — 인용할 때 시각과 표본 수를 함께 적는다
- **결론**: 대기 모델의 입력이 두 층으로 확정됐다 — Tier 1 은 오프라인 배차표(도시철도는 역·방향·시간대, 버스는 노선·요일 유형), Tier 2 는 최종 앵커의 실시간 도착.
  실시간이 없는 노선군·역과 주말·공휴일 전철은 정적 배차만 쓴다. 이 원천들은 블랙박스와 달리 **응답을 보관할 수 있어** 예측과 실제의 오차를 쌓아 대기시간
  모델을 교정하는 길이 열려 있다(§7.5). 영속화하는 것은 역·정류소 식별 정보다

### E15. 블랙박스 소요 시간의 구성 — 대기는 들어 있지 않다

- **질문**: 블랙박스가 주는 총 소요 시간은 무엇으로 이루어져 있나. 대기시간이 들어 있나 — 우리 비교 기준과 같은 잣대인가
- **방법**: 경기 외곽 5 OD 를 1콜씩 불러 경로 75개. 총 시간에서 응답에 있는 구간 시간의 합을 뺀 잔차를 본다.
  응답에 있는 도보 구간으로 보행 속도·우회율을 역추정해, 응답에 없는 첫·끝 도보(출발지 → 첫 승차, 마지막 하차 → 도착지)를
  추정해 잔차에서 뺀다. 남는 것이 있으면 그것이 대기다
- **잔차는 75개 전부 양수**(중앙 685초)이고, 차량이 1개인 경로에서 오히려 크다(중앙 1,322초 vs 2개 608초) — 승차마다 붙는 값이 아니다
- **블랙박스의 도보 모형** (도보 구간 50개): `distance_m` 은 경로 길이다(경로/거리 0.99). 속도는 구간 길이에 따라 다르다 —
  100 m 이하는 0.32 m/s(중앙 84초, 계단·개찰구가 대부분), 100–300 m 0.74 m/s, 300 m 초과 0.93 m/s. 우회율(경로/직선)은 1.39

| 검증 | 값 |
|---|---|
| 잔차를 첫·끝 도보로 보면 필요한 속도 | 중앙 **0.83 m/s** · 사분위 0.74–0.94 (블랙박스 자신의 도보 속도 0.74–0.93 과 같다) |
| 차량 수별 필요 속도 | 1개 0.91 · 2개 0.82 · 3개 0.82 · 4개 0.82 m/s (차량 수와 무관) |
| 환승 수별 '잔차 − 도보 추정' | 0회 −227초 · 1회 −12초 · 2회 −17초 · 3회 −17초 (환승이 늘어도 붙지 않는다) |
| 승차 1회당 남는 시간 | 중앙 14초 |

- **결론**: 총 소요 시간 = 차내 시간 + 도보 시간(응답에 있는 환승 도보 + 응답에 없는 첫·끝 도보). **대기는 0 이다** —
  '정류소에 닿는 순간 차가 온다' 는 값이라 하한이다. 우리가 대기를 넣으면 같은 경로라도 더 길게 나온다(§7.4)
- **우리 대기를 붙인 재구성**: 차내·도보는 블랙박스 값을 그대로 두고 첫 승차에 실시간 도착, 환승에 배차/2 를 넣으면
  총 시간이 **중앙 +549초(9.2분)** 늘어난다(대기를 모두 채운 10개 경로, 범위 +98–+1,406초).
  한 OD 에서 블랙박스 기준 67초 차이로 나란하던 두 경로가, 실제 대기를 넣으면 **23분** 벌어졌다 —
  배차가 긴 노선이 블랙박스 기준으로는 체계적으로 유리하게 보인다는 뜻이다. 경기 외곽에서 이 차이가 크다(E14 의 배차 표)
- **한계**: 첫·끝 도보는 응답에 없어 추정이다(직선 × 우회율 ÷ 속도) — 환승 0회 경로에서 편차가 ±500초까지 벌어진다.
  환승 대기는 '지금' 실시간으로 알 수 없어 배차/2 로 뒀다(§7.5). 측정일이 토요일인데 도시철도 배차표는 평일뿐이다(E14)

### E16. 승·하차 이름이 글자로 어긋날 때 — 토막 집합과 노선 순서 구제

- **질문**: 블랙박스 이름과 DB 이름이 글자로 다를 때(토막 순서 뒤바뀜·개명·표기 차이) 승·하차를 무엇으로 찾나
- **계기**: `청파동주민센터.숙대입구역` 이 10 m 옆의 `숙대입구역.청파동주민센터` 를 두고 미매칭으로 남았다.
  §6.6 이 **중간** 정류소를 이름 없이 풀었지만 **승·하차는 여전히 이름 매칭에만** 달려 있었다 — 닫았다고 본 문제가 양끝에 남아 있었다
- **부류의 크기**(정류소 DB): 복합 이름 정류소 10,421개가 토막집합 6,667개 그룹을 이루고, 그중 **204 그룹(3.1%)** 이
  같은 토막을 다른 표기로 쓴다. 300 m 넘게 떨어진 쌍은 4개뿐이고(등기소.법원 ↔ 법원.등기소 105.7 km 포함) 나머지는 같은 자리다.
  다만 우리 원천 네 가지는 모두 같은 표기를 쓰므로, 뒤바뀐 표기는 **블랙박스 쪽**이다 — 우리 데이터만으로는 빈도를 잴 수 없다
- **노선 기반 구제는 거리로 고르면 안 된다**: 같은 노선의 다른 정류소가 50 m 안에 있는 (노선, 정류소) 쌍이 51.9%,
  300 m 안이 93.5%(최대 31곳)이고 두 번째까지 거리 중앙이 42.9 m 라, 기준점 오차(중앙 8.6 · p90 91.5 m)와 겹친다 → R20.
  대신 **운행 순서 간격을 정확히 맞추면** 노선당 후보가 하나로 유일해지고, 한 칸 틀린 오선택은 63.7% 가 반경 밖으로 밀려난다
- **규칙** (§5.6): 이름 수준에 `parts`(토막을 하나도 버리지 않고 집합이 같다)를 alias 와 prefix 사이에 넣는다.
  글자가 맞은 후보가 그 노선 위에 하나도 없을 때만 이름을 버리고 그 노선이 서는 정류장을 후보로 삼는다(`route`).
  **간격을 이름 수준보다 먼저 본다** — 이름 수준으로만 고르면 간격이 한 칸 어긋난 상위 수준 후보가 간격이 맞는 낮은 수준 후보를 조용히 밀어낸다

| 부류 (합성 400구간, 하차 이름만 망가뜨림) | 규칙 전 | 규칙 뒤 |
|---|---|---|
| 토막 순서 뒤바뀜 | 미매칭 131 · 조용한 오답 25 | 정답 **394** · 모호 5 · 오답 1 |
| 이름이 DB 에 없음(개명·표기) | 미매칭 500 | 정답 **400** (그중 모호 196) |
| 이름이 그대로 맞는 회귀 표본 (구간 499개 · 승·하차 998건) | — | 전부 그대로(`key`), 구제 발동 **0건** |

- **대가**: 구제로 찾은 것의 절반 가까이가 모호다 — 이름 근거가 없으면 거리로 순위를 가릴 수 없어 경쟁 후보가 있으면 모호로 남긴다.
  고른 정류장은 표본에서 전부 정답이었고, 모호로 남겨도 `chosen` 은 채워져 순서표 구간(§6.6)은 열린다
- **한계**: 블랙박스가 한 이름에 두 표기를 `/` 로 붙여 주는 부류는 아직 토막으로 나뉘지 않는다.
  도시철도에는 구제를 넣지 않았다(반경이 1 km 라 따로 재야 한다). 경기 외곽 5 OD 로는 이 변화를 잴 수 없어 서울 OD 가 필요하다

### E17. 급행 구간 — 순서표로 떼야 하나

- **질문**: 블랙박스가 급행으로 주는 구간은 정차역만 와서 순서표 구간을 뗄 수 없다(§6.6). 시각표 자료의 정차 패턴으로 풀 수 있나,
  그리고 그 일이 앵커 선택(§3)에 필요한가
- **시각표 자료로 급행 사전을 만들 수 있나**: 수도권 도시철도 67 노선 9,152 운행의 정차 기록을 훑어 운행마다 정차역 순서를 뽑고,
  우리 순서표에 놓아 건너뛰는 운행을 가렸다

| 운행 패턴 480개를 순서표에 놓으면 | 개수 |
|---|---|
| 연속 — 짧게 끊어 도는 구간 운행 | 181 |
| **건너뜀 — 급행** | **14** |
| 놓지 못함 (덩어리를 가로지르는 운행 · 대상 밖 역 · 순환선 여러 바퀴) | 262 |
| 대상 밖 노선 (인천 1·2호선) | 23 |

- 급행을 **노선으로 담은 것은 9호선·1호선뿐**이다. 경의중앙선·수인분당선 급행은 같은 노선 안에 패턴으로만 있어,
  "시각표 자료가 급행·일반을 다른 노선으로 담는다"는 전제는 9호선에만 맞다 → R22
- **사전이 필요 없다**: 급행 구간에도 그 급행이 실제로 선 역 목록이 그대로 온다. 순서표에서 승차~하차 구간을 떼고
  그 안에 **순서를 지키는 부분수열**로 놓으면 된다. OD 10개(도시철도 구간 98개)로 재봤다

| | 구간 | 정차역 | 이름 일치 | 지금 위치와 같은 역 |
|---|---|---|---|---|
| 급행 | 38 | 376 | **376 (100%)** | **376 (100%)** |
| 일반 | 43 | 612 | **612 (100%)** | **612 (100%)** |

- **그런데 위치는 이미 나온다**. 급행 구간은 도시철도 구간의 **43%**(42/98)로 작지 않지만, 위치를 얻는 근거가 다를 뿐이다

| 정차역의 위치 근거 | 순서표 | 이름 | 수준 없음 |
|---|---|---|---|
| 급행 462개 | 36% | **60%** | 4% |
| 일반 840개 | 73% | 23% | 4% |

- 부분수열로 넓히면 근거가 순서표로 옮겨 가지만 **고르는 역은 같다**(376/376). 표본에서 이름이 어긋난 급행 정차역은 0 개다.
  일반 구간은 건너뛴 자리가 아예 없어(612/612 연속) 넓혀도 결과가 바뀌지 않는다
- 남은 4%(위치 수준 없음)는 급행과 무관하다 — 1호선 동인천~부개 10개 역이 인천 소재라 DB 에 없다(§1.3 의 범위)
- **필요한가**: 경로 중간 정차역을 쓰는 곳은 (a) 섭동의 지점 후보 하나다(§3.1, §3.6). 그 용도에는 지금 위치로 충분하므로
  순서표 구간 읽기(§6.6)를 부분수열로 넓히지 않는다
- **결론**: 매칭 쪽에서 급행은 열린 문제가 아니다. 남는 것은 **대기** 쪽이다 — 급행만 목적지에 닿는 구간의 대기는
  '그 역에서 아무 열차나' 배차(§7.5)와 다르고, 그 값이 (b)의 근사 점수(§3.2)에 그대로 들어간다 → §0.3

---

## 부록 A. 기각한 설계·가설

| ID | 기각한 것 | 이유 | 근거 |
|---|---|---|---|
| R1 | 앵커 전략 (d) 택시·대중교통 등시선 만남 | 앵커를 이산화하지 않으면 쿼리가 폭발한다. (b)가 그 노선 단위 이산화 | §3.4 |
| R2 | 거리 기반 정류소 중복 제거 | 길 양쪽 상·하행 쌍을 뭉갠다 | E3 |
| R3 | 원천 도시명으로 지역 판정 | 도시명 = BIS 운영 주체. 양방향 오분류 | E4 |
| R4 | 가정 "공개 대중교통 경로 API 는 없다" | 틀렸다 — 무료 1,000콜/일로 개방 | E1 |
| R5 | 가정 "정류소 목록은 승·하차만, 도보는 직선" | 목록은 전부, 환승 도보는 실제 경로 | E2 |
| R6 | 가정 "요금은 값 하나" | 범위 요금이 있다 | E11 |
| R7 | 도시철도 역 순서 = 역번호 정렬 | 코레일 노선에서 수십 km 점프 | E6 |
| R8 | 중간 정류소를 정류소별로 따로 고르기 | 되돌아오는 노선에서 순서가 깨진다 → 순서 보존 동적계획 | §6.3, E10 |
| R9 | 매칭에서 부분 이름 단계를 "근거 없음"으로 뺀 판단 | 실측에서 남은 미매칭의 주원인이 복합 이름이었다 | E7, E10 |
| R10 | 서울 도시철도 운영사 역 자료 | 좌표가 없고 서울 시내 노선의 절반 이상이 빠진다 → 전국 표준데이터 | §4.3 |
| R11 | 상용 지도의 장소 검색으로 정류소 DB 지오코딩 | 결과 저장 금지 → 영속 DB 를 만들 수 없다 → 공공데이터 + 공공 지오코딩 | §2.2 |
| R12 | 좌표·요금을 주는 다른 대중교통 라우터 | 무료 한도 30콜/일로 예산이 안 된다. 유료 전환 시 재검토 | E1 |
| R13 | 택시 요금을 공개 산식으로 오프라인 계산 | 가지치기에는 시간만 필요하고, 요금은 검증 단계의 자동차 경로 응답이 준다(10,000콜/일, 병목 아님). 응답이 실시간 교통·시계외를 반영하고, 산식에는 확정 안 되는 규칙이 남는다 | §7.1, E5 |
| R14 | 진술선호 조사 + 다항로짓으로 VOT 추정 | 범위 축소 — 사용자가 길찾기마다 시간 중시 / 균형 / 비용 중시 중 하나를 고른다 | §7.6 |
| R15 | 정류소 DB 를 전국 원본(연 1회 갱신)만으로 만들기 | 노선 원천보다 오래돼 노선 위 대상 안 정류소 970곳이 DB 밖이었고, 이름이 바뀐 정류소가 이름 매칭을 흔든다 → 두 BIS 의 최신 정류소 파일로 교체 | §4.1, E12 |
| R16 | 매칭에 부분 이름 단계(`.` 로 나눈 부분 일치)를 넣기 | 경로 중간 정류소는 순서표 구간이 이름 없이 자리를 주고(§6.6), 승·하차는 최신 DB 로 미매칭이 없다. 부분 이름은 흔한 토막이라 오매칭 위험만 남는다 → R9 를 대체 | E13 |
| R17 | 전국 단일 버스 도착정보 원천으로 두 지역 BIS 를 대신하기 | 도시 목록에 서울이 없고 경기도 일부 시군만 있으며, 같은 정류소에서 오는 도착 건수가 지역 BIS 의 10분의 1 수준이었다 | E14 |
| R18 | 시각표 자료(GTFS)의 버스 시각표를 배차 원천으로 쓰기 | 첫차~막차를 대표 배차로 균등 분배한 합성값이다 — 표본 두 노선이 하루 내내 정확히 같은 간격이고 시간대 변화가 0 이었다. 버스 배차는 노선 원천의 요일별 최소·최대를 쓴다 | E14 |
| R19 | 실시간 도착 예측 초를 그대로 남은 대기로 쓰기 | 예측은 원천이 값을 받은 시각 기준이라 경과를 빼야 하고, 0 에는 '지금'과 '모름'이 섞여 있다. 그대로 쓰면 여러 역 떨어진 열차가 지금 도착이 된다 → 경과 보정 · 도착 상태로 가르기 · 남은 역 수 폴백 | E14, §7.5 |
| R20 | 노선 기반 승·하차 구제에서 반경 안 최근접 정류장을 고르기 | 같은 노선의 다른 정류소가 300 m 안에 있는 (노선, 정류소) 쌍이 93.5%(최대 31곳)이고 두 번째까지 거리 중앙이 42.9 m 라 기준점 오차와 겹친다 → 운행 순서 간격이 정확히 맞는 후보만 받는다 | E16 |
| R21 | 구간의 노선 후보가 여럿이면 모두 다른 노선으로 보고 빈도를 더하기 | 후보가 여럿인 것은 같은 번호가 시·군마다 있어 매칭이 하나로 좁히지 못한 **식별 모호**이지 함께 기다릴 수 있는 노선이 아니다. 빈도를 더하면 같은 버스를 두 대로 세어 대기가 절반이 된다 → 이름이 같은 후보는 평균 하나로 접고, 빈도 합은 차량 이름이 서로 다른 경우에만 | §7.5 |
| R22 | 시각표 자료의 정차 패턴으로 급행 사전을 만들어 급행 구간을 떼기 | 급행을 노선으로 담은 것은 9호선·1호선뿐이고, 운행 패턴 480개 중 건너뛰는 것은 14개인데 순서표에 놓이지 않는 것이 262개다. 게다가 블랙박스가 급행의 정차역 목록을 이미 주므로 사전이 필요 없다 | E17 |

## 부록 B. 검증된 수치

요약(§0)에서 뺀 실측 수치다 — 무엇을 어떻게 쟀는지는 각 줄 끝의 `E` 번호에 있다.

- 정류소 공간 DB(서울·경기): 버스 **50,019**(두 BIS 최신 정류소 파일 반영) · 도시철도역 **685** → E3, E4, E8, E12
- 도시철도 역 순서표: 22개 노선군 39개 순서(지선 포함), 역 DB 685역 전부 포함, 노선군마다 한 덩어리로 연결(GTX-A 는 북부·남부 둘) → E6
- 버스 노선–정류소 순서표: 서울 715 · 경기 3,137 노선. 원천 정류소 ID 가 DB 키와 **변환 없이** 붙는다 — 고유 정류소 서울 **99.1%** · 경기 **98.2%**,
  못 붙은 것은 대상 밖 구간과 서울 노선의 통과 노드 → E6, E12
- 정류소 DB 갱신 뒤 5 OD 재측정: 버스 승·하차 미매칭 1 → 0, 경로 위 버스 정류소의 DB 위치 98.6% → **99.8%** → E12
- 승·하차 매칭(경기 외곽 5 OD): 버스 80건 **발견률 100%**(모호 7) · 지하철 31건 중 미매칭 3 = 전부 원천에 없던 GTX-A → 보강 후 해소. 오차 중앙 **8.6 m** → E7, E8
- 경로 위 모든 정류소 위치(같은 5 OD, 구간 101개): 버스 **98.9%** · 지하철 **97.8%** 가 DB 위치, 정류소 순서 역전 **0건** → E10
- 순서표로 접지(같은 5 OD, 새로 받은 응답): 버스 승·하차 **모호 7 → 0**, 경로 위 위치는 버스 828곳 · 지하철 92곳 **전부** DB 위치
  (같은 응답에 두 방식을 적용한 99.8% · 97.8% → 100% — 위 E12 줄의 98.6% → 99.8% 와는 표본이 다르다).
  승차 → 하차 구간의 이름 순열은 검사한 78쌍 중 **그때 노선을 찾은 76쌍이 전부 일치**(74쌍은 이름이 모두 같고, 2쌍은 개명된 이름 하나 차이) → E13
- 배차간격표: 버스는 순서표 3,852 노선 중 **3,823**(99.2%)이 원천 행을 얻고, 그 3,852 가운데 배차 분값까지 있는 노선은 **3,294**(85.5%). 도시철도는
  평일 시각표에서 센 (역, 노선군, 방향, 시간대) 배차로 685역 중 **683**역 — 대기의 입력 단위는 노선이 아니라 역이다(8시 1호선 2.2–60분) → E14
- 실시간 도착: 버스는 두 BIS 를 정류소 번호로 바로 부른다. 도시철도는 실시간 원문 역명으로만 물을 수 있어(우리 표기와 다른 행 **325**개) 매핑표를 만들었고,
  685역 중 **642**역 · 22개 노선군 중 **19**개를 조회할 수 있다. 다만 **물을 수 있다는 것이 초 단위 예측을 얻는다는 뜻은 아니다** —
  실호출 표본 13건이 전부 예측 0 이었고 '지금'으로 읽을 수 있는 것은 2건뿐이었다 → E14
- 블랙박스 소요 시간에는 **대기가 없다**: 총 시간 − 구간 시간 합을 첫·끝 도보로 보면 필요한 속도가 중앙 0.83 m/s 로
  블랙박스 자신의 도보 속도와 같고, 환승이 0 → 3회로 늘어도 남는 시간이 평평하다(−227 · −12 · −17 · −17초).
  우리 대기를 넣으면 같은 경로가 중앙 **+9.2분** → E15
- 이름이 글자로 어긋나는 승·하차: 토막 집합 일치와 노선 순서 구제로 합성 표본의 미매칭 131·500 → **0**,
  이름이 맞는 998건은 그대로(구제 발동 0) → E16
- 급행 구간: 블랙박스 도시철도 구간의 **43%**(42/98). 정차역 위치는 급행 462개 중 **96%** 가 나오고(순서표 36% · 이름 60%),
  순서표 구간에 부분수열로 다시 놓아도 **376/376 이 같은 역**이다. 시각표 자료의 운행 패턴 480개 중 건너뛰는(급행) 것은 **14**개 → E17
- 블랙박스: 1콜에 대안 15개 · 출발 시각·경유지·정류소 좌표 없음 · 대안 15개 중 3개가 범위 요금 → E2, E9, E11

## 부록 C. 용어

| 용어 | 뜻 |
|---|---|
| 앵커 | 택시와 대중교통이 만나는 정류소·역 |
| 기준 경로 | 블랙박스가 준 대중교통 전용 경로(대안 최대 15개) |
| 블랙박스 | 내부를 모르고 입출력만 쓰는 상용 대중교통 라우터 |
| 쿼리 예산 | 하루 허용 호출 수(대중교통 1,000). 요청당 `1 + N` |
| Tier 1 / Tier 2 | 오프라인 무료 탐색 / 온라인 쿼터 검증 |
| 접지(grounding) | 블랙박스의 정류소 이름을 DB 좌표에 붙이는 일 |
| 이름 수준 | key(정규화 키 일치) > alias(다른 이름 일치) > prefix(앞부분 일치, 4글자 이상) |
| 발견률 / 매칭률 | (매칭 + 모호) / 대상, 매칭 / 대상 |
| 모호 | 1위와 다른 실체가 버스 10 m · 지하철 150 m 안에 있는 매칭 |
| 복도 | 구간 폴리라인에서 일정 거리(버스 100 m · 지하철 500 m) 안의 띠 |
| 일반화 비용(GC) | 총 소요시간 + 총비용 / VOT |
| VOT | 시간가치(원/분) |
| 시계외 할증 | 택시가 사업구역(시군) 밖으로 나갈 때 붙는 할증 |
