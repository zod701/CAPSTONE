# 바로가 — 대중교통·택시 연계 경로탐색 알고리즘: 설계와 발전

> 과제의 핵심 축인 **수단 연계 경로탐색 알고리즘**이 어떤 문제 인식에서 출발해, 어떤 결정·실험·기각을 거쳐 지금의 구조에 이르렀는지만 적는다.
> 데모 웹 구현, API 호출 방법, 화면·운영 세부는 담지 않는다.
>
> **읽는 법** — §0 은 요약뿐이다. 근거·수치·과정은 뒤에 있고, 요약 각 줄 끝의 `→` 가 그 위치다.
> `E` = 실험(§9), `R` = 기각한 설계·가설(부록 A).

---

## 0. 요약

### 0.1 문제와 기여

- **문제** — 상용 지도 앱은 대중교통만으로 경로를 짠다. "직행이 없으니 A 까지는 택시, A 부터는 대중교통" 같은 수단 연계 판단은
  숙련 이용자의 직관에 맡겨져 있다. 이 판단을 자동화한다. → §1
- **접근** — 대중교통 경로는 상용 라우터를 **블랙박스**로 쓰고, 알고리즘은 **어디서 수단을 잇는가(앵커)** 에 집중한다. → §2
- **기여** — **제한된 쿼리 예산 아래에서 유망한 앵커를 고르는 휴리스틱**. "쿼리 수 대비 해 품질"로 평가한다. → §3, §8
- **선결 문제** — 블랙박스는 정류소를 **이름으로만** 알려 준다. 앵커를 다루려면 이를 **좌표와 순서**로 되살려야 한다.
  알고리즘 이전에 먼저 풀어야 했던 기술 문제다. → §4–§6

### 0.2 알고리즘 구성

```
입력: 출발 O · 도착 D · 선호(시간 중시 / 균형 / 비용 중시 → VOT)
 ① 기준 경로     블랙박스 1콜 → 대안 경로 최대 15개                                  → §2.2
 ② 경로 접지     정류소 이름 → 좌표: 승·하차 매칭 + 경로 위 모든 정류소 위치 복원        → §5, §6
 ③ 앵커 후보     (a) 기준 경로 섭동 · (b) 직행 노선 역추적 · (c) 허브 타원               → §3
 ④ 가지치기      오프라인(Tier 1): 택시 시간 근사 + 배차 근사 → 상위 N 개                → §2.4
 ⑤ 검증         온라인(Tier 2): 택시 O→P(자동차 경로: 시간·요금) + 대중교통 P→D, 요청당 1 + N 콜 → §2.4, §7.1
 ⑥ 비용·제시     일반화 비용 · Pareto(시간·비용·환승·도보) · 원/분 · 구조 다양성          → §7
```

성숙도(큰 단위): ②와 그 기반인 정류소 공간 DB(§4)는 구현·실측됐다. ①은 블랙박스의 성질을 실측으로 확정했다. ③–⑥은 설계 단계다
(③의 입력 중 버스 노선–정류소 순서표와 도시철도 역 순서표는 갖췄다).

### 0.3 발전 과정 한눈에

| 단계 | 생각이 어떻게 바뀌었나 | 계기 | → |
|---|---|---|---|
| 1 | "길찾기 알고리즘을 만든다" → **상용 라우터를 블랙박스로 두고 연계 지점을 푼다** | 무료로 쓸 수 있는 대중교통 경로 API 가 있었다(초기 가정 "없다"는 틀림) | §2.1, E1, R4 |
| 2 | 전수 조합 → **쿼리 예산 아래의 앵커 휴리스틱**, 평가 = 쿼리 수 대비 해 품질 | 정류소 5만 개 × 대중교통 1,000콜/일 | §2.3, §3 |
| 3 | 등시선 만남 (d) → **직행 노선 역추적 (b) 중심 + (a) 섭동 + (c) 허브** | 이산화하지 않은 앵커는 쿼리가 폭발한다 | §3, R1 |
| 4 | "응답에 좌표·순서가 있을 것" → 이름뿐 → **정류소 공간 DB 를 직접 구축** | 응답 구조 실측 | §2.2, E2 |
| 5 | 거리로 중복 제거 → **정류장번호 뒤 9자리로 같은 정류소를 식별**, 길 양쪽 상·하행은 보존 | 이중 등록 5,405쌍 vs 상·하행 ≈1.1만 쌍 | §4.1, E3, R2 |
| 6 | 원천 도시명으로 지역 판정 → **좌표 + 행정경계** | 서울 표기 정류소 1,891곳이 실제로는 경기 | §4.2, E4, R3 |
| 7 | 이름 일치 → **이름 수준(key > alias > prefix) · 반경 · 모호 판정 · 노선 필터** | 경기 외곽 5 OD 에서 버스 발견률 100%, 원천에 없는 노선(GTX-A) 발견 | §5, E7, E8 |
| 8 | 승·하차만 → **경로 위 모든 정류소**. 정류소별 탐욕 선택 → **순서 보존 동적계획** | 갔다가 되돌아오는 노선에서 탐욕 선택이 순서를 깬다 | §6, E10, R8 |
| 9 | 요금 = 값 하나 → **범위 요금도 있음**. 도보 = 환승 도보뿐 → **첫·끝 도보(직선 추정) 포함** | 실측 | §7.2, §7.3, E11 |
| 10 | 택시 요금 = 공개 산식으로 오프라인 계산 → **자동차 경로 응답의 택시 예상요금** | 자동차 경로는 병목이 아니고, 응답이 실시간 교통과 시계외 할증을 반영한다 | §7.1, E5, R13 |
| 11 | 선호 = 진술선호 조사로 VOT 추정 → **길찾기마다 시간 중시 / 균형 / 비용 중시 중 선택** | 범위 축소 | §7.6, R14 |
| 12 | 정류소 DB = 전국 원본(연 1회) → **두 BIS 의 최신 정류소 파일로 교체**, 최신 파일의 좌표 오류는 보정표 | 노선 위 대상 안 정류소 970곳이 DB 밖이었다 | §4.1, E12, R15 |

### 0.4 검증된 수치

- 정류소 공간 DB(서울·경기): 버스 **50,019**(두 BIS 최신 정류소 파일 반영) · 도시철도역 **685** → E3, E4, E8, E12
- 도시철도 역 순서표: 22개 노선군 39개 순서(지선 포함), 역 DB 685역 전부 포함, 노선군마다 한 덩어리로 연결(GTX-A 는 북부·남부 둘) → E6
- 버스 노선–정류소 순서표: 서울 715 · 경기 3,137 노선. 원천 정류소 ID 가 DB 키와 **변환 없이** 붙는다 — 고유 정류소 서울 **99.1%** · 경기 **98.2%**,
  못 붙은 것은 대상 밖 구간과 서울 노선의 통과 노드 → E6, E12
- 정류소 DB 갱신 뒤 5 OD 재측정: 버스 승·하차 미매칭 1 → 0, 경로 위 버스 정류소의 DB 위치 98.6% → **99.8%** → E12
- 승·하차 매칭(경기 외곽 5 OD): 버스 80건 **발견률 100%**(모호 7) · 지하철 31건 중 미매칭 3 = 전부 원천에 없던 GTX-A → 보강 후 해소. 오차 중앙 **8.6 m** → E7, E8
- 경로 위 모든 정류소 위치(같은 5 OD, 구간 101개): 버스 **98.9%** · 지하철 **97.8%** 가 DB 위치, 정류소 순서 역전 **0건** → E10
- 블랙박스: 1콜에 대안 15개 · 출발 시각·경유지·정류소 좌표 없음 · 대안 15개 중 3개가 범위 요금 → E2, E9, E11

### 0.5 열린 설계 문제

| 문제 | 남은 것 | → |
|---|---|---|
| 앵커 전략 구현과 ablation | (a)(b)(c)를 구현해 쿼리 수 대비 해 품질을 비교한다 — 과제의 본 실험 | §3.5 |
| 배차간격·실시간 도착 정보 | (b)의 근사 점수와 대기시간의 입력. 실시간 도착 정보를 쓰고, **구할 수 없으면 배차간격으로 추정하는 대기시간 모델**을 쓴다. 원천과 쓰는 방식은 나중에 정한다 | §3.6, §7.5 |
| 진행 방향(상·하행) 판정 | 모호 매칭과 택시 하차점 결정에 필요하다 | §5.4 |
| 부분 이름 매칭 | 남은 미매칭의 대부분이 `A.B` 꼴 복합 이름이다 | §5.4, §6.5 |
| 선호 3단계의 VOT 값 | 시간 중시 / 균형 / 비용 중시를 원/분 값으로 정한다 | §7.6 |

---

## 1. 문제 정의

### 1.1 동기
기존 길찾기는 정해진 노선·배차 안에서만 경로를 찾아, 크게 우회하거나 환승 대기가 긴 경로를 그대로 제시한다.

> "현재 위치에서 A 지점까지는 직행 버스가 없으니 택시를 타고, A 부터 목적지까지는 대중교통으로 간다"

이런 판단은 숙련 이용자의 경험에만 기대고 있다. 본 과제는 이를 알고리즘으로 자동화한다.

### 1.2 하이브리드 경로 유형

| 유형 | 형태 | 빈도 | 난이도 | 설명 |
|---|---|---|---|---|
| A. First-mile | 택시 → 대중교통 | 높음 | 낮음 | 출발지가 역세권 밖이거나 첫 버스가 우회 |
| B. Last-mile | 대중교통 → 택시 | 높음 | 낮음 | 목적지가 대중교통 사각지대 |
| C. Mid-leg | 대중교통 → 택시 → 대중교통 | 낮음 | 높음 | 환승 건너뛰기 — 과제의 대표 사례 |
| D. Gap-filling | 긴 대기·도보를 택시로 대체 | 중간 | 중간 | 실시간 대기 정보가 실제로 쓰이는 곳 |
| E. 기준선 | 전 구간 대중교통 / 전 구간 택시 | — | — | 비교용 상·하한 |

구현 순서는 A·B → D → C → 공유 PM 확장이다. 편익 대부분이 A·B 에 있고 쿼리도 싸다. C 를 먼저 붙들면 파이프라인이 돌지 않는 채로 시간을 쓴다.

### 1.3 목표와 범위
- **수단**: 택시를 먼저, 공유 PM 은 확장. PM 은 자전거 경로 조회로 A·B 가 같은 구조로 동작하도록 설계에만 반영한다
- **지역**: 대중교통 밀도가 낮은 **경기 외곽**을 지향한다. 서울은 대중교통이 촘촘해 연계 편익이 작다. 정적 데이터는 서울·경기 전역을 쓴다
- **목표 함수**: 단일 최적해가 아니라 **Pareto 집합**을 제시하고, 개인 선호는 정렬·필터로 반영한다(§7.4)

---

## 2. 출발점 — 블랙박스 라우터와 쿼리 예산

### 2.1 라우터를 만들지 않는 이유
상용 라우터는 노선·환승·요금이 이미 최적화돼 있다. 과제의 기여는 라우팅이 아니라 **수단 결합**이다.
처음에는 "공개 대중교통 경로 API 가 없다"고 보고 자체 라우터나 우회책을 검토했다. 그런데 무료로 하루 1,000콜을 쓸 수 있는 대중교통 경로 API 가
있었고, 다른 후보는 실험 규모를 버티지 못했다(무료 30콜/일, 10콜/일) → E1, R4.

### 2.2 블랙박스의 성질 — 알고리즘이 메워야 할 빈칸
응답 구조를 실측해서 확정했다 → E2.

| 준다 | 안 준다 → 알고리즘이 메우는 방법 |
|---|---|
| 1콜에 대안 경로 최대 15개(버스·지하철·혼합) | **정류소 좌표·ID** → 공공데이터로 정류소 공간 DB 구축(§4), 이름 → 좌표 접지(§5, §6) |
| 구간별 유형·시간·거리·차량(노선명·유형) | **첫·끝 도보**(출발지 → 첫 승차, 마지막 하차 → 도착지) → 직선 추정(§7.3) |
| 구간의 **정류소 이름 전부**(승차·중간·하차) | **출발 시각·대기시간** → 대기 모델(§7.5) |
| 구간 폴리라인, 환승 도보의 실제 폴리라인 | **경유지** → 연계 경로는 구간별 호출을 이어 붙인다(E9) |
| 경로 요금 — 값 하나 또는 **범위** | 범위 요금의 비교 규칙(§7.2, E11) |

- 약관상 응답을 저장·캐싱할 수 없다. 그래서 영속 데이터(정류소 DB·행정경계)는 **공공데이터로만** 만들고, 평가에는 **파생 수치만** 남긴다
- 이름 → 좌표 접지가 필요한 이유가 여기서 나온다. 앵커는 좌표가 있어야 택시와 이을 수 있는데, 블랙박스는 이름만 준다

### 2.3 쿼리 예산
- 대중교통 경로 1,000콜/일이 **묶이는 제약**이다. 자동차 경로는 10,000콜/일로 병목이 아니다
- 요청당 `1 + N`(N = 5–10) 콜이면 하루 약 90–160회 탐색. 한 달이면 평가 표본으로 OD 약 2,000쌍을 모을 수 있다
- 이 제약이 앵커 선택 휴리스틱이 존재하는 이유다. 인위적인 가정이 아니다

### 2.4 2단 구조 — 오프라인 탐색, 온라인 검증

```
Tier 1 (오프라인 · 무제한 · 무료)  넓게 탐색하고 가지친다
  정류소·역 공간 DB · 노선–정류소 순서 · 배차간격 · 택시 시간 근사
  → 수천 개 후보를 수십 개로
Tier 2 (온라인 · 쿼터 · 정확)      상위 N 개만 정확히 평가한다
  대중교통 경로(블랙박스) · 자동차 경로(실시간 교통 · 택시 예상요금) · 실시간 도착 정보
```

---

## 3. 앵커 선택 — 핵심 기여

**앵커** = 택시와 대중교통이 만나는 정류소·역. 문제는 "어느 앵커를 온라인으로 검증할까"다. 채택 조합은 **(a) + (b) + (c)** 다.

### 3.1 (a) 기준 경로 섭동
기준 대중교통 경로의 구간 하나를 택시로 바꾼다. 후보 수가 구간 수(3–5)라 매우 싸다. C·D 유형에 대응한다.
경로 위 **중간 정류소까지** 좌표를 되살려 두면(§6) 섭동 지점을 구간 경계에만 두지 않고 경로 위 어느 정류소에나 둘 수 있다.

### 3.2 (b) 직행 노선 역추적 ★ 중심 전략

```
1. 기준 경로: 블랙박스(O, D) → R0 (대안 여러 개)
2. D 주변 반경 r(≈400 m) 안의 정류소·역 S_D
3. S_D 를 지나는 노선 L_D                                    ← 정적 노선 DB (오프라인, 무료)
4. 각 노선 l 의 D 방향 상류 정류소 중 O 에서 택시로 T_max 안에 닿는 P → 앵커 후보 (l, P)   ← 오프라인 등시선
5. 근사 점수 = 택시시간(O→P) + 배차간격/2 + 승차시간(P→D) → 상위 N(5–10)
6. 온라인 검증: 자동차 경로(O→P, 택시 시간·요금) + 블랙박스(P→D)
7. 일반화 비용 → Pareto → 상위 3–4개 제시
```

- 1–5단계는 모두 오프라인이다. 요청당 대중교통 쿼리는 `1 + N`
- 대칭판(O 를 지나는 노선의 하류 정류소 → 택시로 D)이 B 유형을 덮는다
- 후보가 작으면서도 **환승 없이 목적지까지 데려다주는 노선**을 정확히 겨눈다

### 3.3 (c) 허브 타원
O–D 를 초점으로 하는 타원(우회 허용계수 α) 안의 주요 환승 거점만 앵커로 쓴다. (b)에 싸게 덧붙이는 보완재다.

### 3.4 기각: (d) 등시선 만남
O 와 D 에서 택시·대중교통 등시선을 넓혀 만나는 곳을 찾는 방식이다. 앵커를 이산화하지 않으면 쿼리가 폭발한다. (b)가 이 생각을 노선 단위로 이산화한 형태다 → R1.

### 3.5 평가 설계
- **ablation**: (a) / (b) / (c) / 무작위 격자 — **쿼리 수 대비 해 품질**(§8)
- 기준선은 대중교통 전용 최적 경로(유형 E)

### 3.6 필요한 입력
| 입력 | 쓰는 곳 | 출처·상태 |
|---|---|---|
| 정류소·역 좌표, 지역(시군) | 후보 생성, 요금 | §4 — 구축됨 |
| 경로 위 정류소 좌표 | (a) 섭동 지점 | §6 — 구현됨 |
| 버스 노선–정류소 순서 | (b) 상류·하류 정류소 | 서울 노선별 정류소 + 경기 노선–정류소 일괄 자료로 순서표를 만들었다. 정류소 번호가 우리 키와 **변환 없이** 맞물린다. 순번 = 운행 순서라 상류·하류를 순번 비교로 정한다 → E6 |
| 도시철도 역 순서 | (b) 지하철 노선 | 노선정보 표준데이터의 **정거장 구성** + 보정표로 역 순서표를 만들었다(지선·순환·빠진 구간 포함, 역 DB 전부). 역번호 순서는 못 쓴다(코레일 노선에서 최대 59 km 점프) → E6, R7 |
| 배차간격·실시간 도착 | (b) 근사 점수, 대기 모델 | 경기 요일 유형별 최소·최대 배차간격(노선 일괄 자료), 서울 노선별 대표값(파일에는 없고 조회로만). 시간대별 배차는 신청형 자료뿐 → E6. 쓰는 방식은 열린 문제(§0.5) |
| 택시 시간 근사 | 후보 생성·가지치기 | 검증 단계에서는 자동차 경로 응답이 시간·요금을 준다 → §7.1 |

---

## 4. 정류소 공간 기반 — 알고리즘의 좌표계

블랙박스가 좌표를 주지 않으므로, 앵커를 표현할 좌표계를 공공데이터로 직접 만들었다. 핵심은 **같은 정류소를 하나로 식별하는 규칙**과 **지역 판정**이다.

### 4.1 정류소의 정체 — 번호 뒤 9자리
- 전국 버스정류장 자료에서 서울·경기 BIS 가 등록한 레코드(접두 `GGB`·`SEB`)만 쓴다. 두 BIS 가 **같은 물리 정류소를 각각 등록**했고, 정류장번호의
  **접두 3글자를 떼면 나머지 9자리가 같다** → 이 9자리를 정류소 키로 쓴다 → E3
- 쌍 판정(먼저 맞는 규칙): **50 m 이하면 이름과 무관하게 병합** → 50–300 m 이고 이름이 호환되면(키가 같거나 한쪽이 다른 쪽의 앞부분) 병합 → 그 외에는 분리.
  50 m 안에서 이름으로 막지 않는 이유는, 5 m 떨어진 같은 정류소가 이름이 전혀 다르게 등록된 사례가 있기 때문이다(ARS 번호는 같음)
- 대표 레코드는 소재지의 BIS 것으로 둔다(서울이면 서울 BIS). 노선–정류소 원천(서울·경기)과 키가 그대로 맞물린다
- **길 양쪽의 상·하행 정류소는 합치지 않는다.** 15–60 m 떨어진 같은 이름·같은 기관의 쌍은 중복이 아니라 상·하행이다. 거리만으로 중복을 지우면 이들이
  뭉개진다 → R2. 병합 뒤에도 상·하행 쌍이 새로 합쳐지거나 생기지 않았다 → E3
- 전국 원본은 연 1회 갱신이라 노선 원천보다 오래된다. 서울·경기 BIS 레코드는 **두 BIS 의 최신 정류소 파일로 바꾼 뒤** 같은 규칙으로 정제한다.
  최신 파일에서 빠진 레코드는 폐지로 본다(서울 파일은 서울 소재만 담아, 서울 BIS 의 서울 밖 레코드는 전국 원본 것을 둔다). 최신 파일의 좌표 오류는
  보정표로 고친다 → E12, R15
- 결과: **50,019개**(단독 44,373 · 근접 병합 5,443 · 원거리 병합 169 · 분리 34)

### 4.2 지역 판정 — 좌표 + 행정경계
- 원천의 도시명은 **소재지가 아니라 BIS 운영 주체**다. 서울 BIS 레코드는 전부 "서울"로 표기돼 있지만, 좌표로 보면 1,891곳이 경기에 있다 → R3, E4
- 그래서 시도·시군을 **행정경계 폴리곤 포함 판정**으로 다시 매긴다. 서울 25구 · 경기 31시군이다. 경기의 일반구는 시로 합친다 — 최근의 구 신설·부활도
  시 외곽 경계는 바꾸지 않았다
- 폴리곤 밖의 점은 **가장 가까운 폴리곤이 대상 시군이고 2 km 이내일 때만** 그 시군으로 붙인다(서해안 매립지·방조제 위 정류소). 인접 시도가 더 가까우면 제외한다

### 4.3 도시철도역 — 노선군과 보강
- 원천은 전국 도시철도 역사 표준데이터다. 역 좌표와 환승 정보가 있지만, 노선명이 **운영 노선명**이라 블랙박스가 쓰는 승객 노선명과 다르다
  (예: 승객의 1호선 = 운영 노선 5개) → **노선명 → 노선군 매핑**
- 운영 노선 하나가 승객 노선 둘에 걸치는 구간은 **역 단위 보정**으로 푼다(용산–왕십리 구간의 운영 노선명은 1호선 계열이지만 실제 운행은 경의중앙선)
- 원천에 없는 개통 노선은 참조표로 보강한다. GTX-A 9역을 넣었고, 이것으로 실경로 미매칭이 해소됐다 → E8
- 원천 좌표가 틀린 역 3곳(다른 지역의 동명 역 좌표, 옆 역 좌표)은 다시 찍어 보정표로 고치고, 서지 않는 노선으로 잘못 들어간 행은 뺀다 → E8
- 결과: 대상 지역 **685역**

### 4.4 이름 정규화 — 매칭의 기준 키
정류소 이름과 블랙박스의 이름을 같은 규칙으로 키로 만든다.
- 유니코드 정규화(NFKC), 구분자(`·` `∙` `,` 등)를 `.` 로 통일
- 괄호 부기명 제거: `천호(풍납토성)` → `천호`, `(중)` · `(미정차)` 도 제거
- **지하철만** 끝의 "역"을 뗀다: `주안역` → `주안`. 버스의 `판교역` 은 정류소 이름의 일부라 두 경우를 다르게 다룬다
- "미정차" 가상 정류소(승하차 불가)는 매칭 후보에서 뺀다

---

## 5. 이름 → 좌표: 승·하차 매칭

### 5.1 기준점
블랙박스가 좌표 대신 주는 유일한 공간 단서는 **구간 폴리라인**이다. 그 **시작점을 승차**, **끝점을 하차**의 기준점으로 쓴다.
구간의 정류소 이름 목록 양끝이 안내문의 승·하차 이름과 모든 실측에서 일치했다. 그래서 목록 양끝을 승·하차 이름으로 쓴다 → E2.

### 5.2 후보와 순위
- 기준점 반경(버스 **300 m**, 지하철 **1,000 m**) 안의 DB 행을 모은다
- 이름 수준을 매긴다: **key**(정규화 키 일치) > **alias**(병합으로 모인 다른 이름과 일치) > **prefix**(한쪽이 다른 쪽의 앞부분, 둘 다 4글자 이상 — 잘린 이름 대응)
- **가장 높은 수준**의 후보 중 **가장 가까운 것**을 고른다
- 지하철은 차량의 노선명을 노선군으로 바꿔 **같은 노선군 후보만** 남긴다. 맞는 노선이 없으면 필터 없이 고르고 그 사실을 표시한다

### 5.3 모호 판정
1위와 **다른 실체**인 후보가 1위보다 버스 **10 m**, 지하철 **150 m** 이내로 더 멀리 있으면 **모호(ambiguous)** 로 표시한다.
같은 이름의 지하철 행이 300 m 안에 있으면 한 역의 환승 노선 행들로 보고 같은 실체로 친다.

### 5.4 실측과 한계 → E7, E8
- 버스는 모두 찾았고(발견률 100%) 오차 중앙은 6–7 m 대다. 지하철 오차가 큰 것(중앙 25–97 m)은 역 대표 좌표와 승강장 쪽 경로 끝점의 차이다
- **모호 7건**은 동명 정류소 2–4곳이 10 m 안에 붙은 경우다(상·하행, 중앙차로 정류소와 가로변 정류소). 택시 하차점으로는 무해하다.
  노선 연결에는 **진행 방향 판정**이 필요하다 — 노선–정류소 순서의 상·하행 정보로 풀 문제다
- **복합 이름**: 블랙박스 이름이 DB 이름보다 자세하면(`A.B` 꼴, 출구 번호가 붙은 이름) key·alias·prefix 에 모두 걸리지 않는다.
  처음 설계에서 "근거 없음"으로 뺀 **부분 이름 단계**(`.` 로 나눈 부분 일치)가 필요했다 → R9
- **원천에 없는 노선**은 매칭이 원리적으로 불가능하다. GTX-A 보강으로 해소됐다 → E8

---

## 6. 경로 위 모든 정류소의 위치 복원

### 6.1 왜 필요한가
앵커 섭동(a)과 택시 승·하차점 후보는 **경로 중간**의 정류소다. 블랙박스는 중간 정류소도 이름만 준다.
승·하차 매칭(§5)만으로는 경로 중간에 어떤 정류소가 어디 있는지 알 수 없다.

### 6.2 기준점이 없다 → 구간 선 복도
중간 정류소에는 기준점이 없다. 그래서 반경 대신 **구간 폴리라인까지의 거리**를 쓴다. 이름이 맞는 DB 행 중 선에서 **버스 100 m, 지하철 500 m** 안에 있는 것이 후보다.
복도를 좁게 두는 것은 DB 에 없는 정류소가 옆 도로의 같은 이름 정류소로 잘못 붙지 않게 하기 위해서다.

### 6.3 발전: 정류소별 탐욕 선택 → 순서 보존 동적계획
- **1안 — 정류소별 탐욕 선택**: 정류소마다 따로 최선 후보(노선 일치 > 이름 수준 > 선까지 거리)를 고른다.
  갔다가 되돌아오는 노선에서 같은 이름이 한 구간에 두 번 나오면, 둘 다 **첫 통과 지점**의 행에 붙는다. 그 뒤 위치를 추정하는 정류소가 선 한가운데로 끌려간다 → R8
- **2안 — 순서 보존 정렬**:
  1. DB 행이 선 곁을 **지나는 곳마다** 후보를 따로 만든다. 선이 꼭짓점에서 복도 밖으로 나가면 한 번의 통과로 끊는다. 긴 선분 하나가 복도를 스치는 것도 한 번으로 센다
  2. 각 후보에 (정류소 순번, 선 위 거리, 점수)를 매긴다. 점수는 노선 일치 > 이름 수준 > 선까지 거리이고, 어떤 후보든 0보다 커서 **하나라도 더 맞히는 조합이 이긴다**
  3. **정류소 순서와 선 위 순서가 어긋나지 않는**(뒤로 50 m 까지 허용) 후보 조합 중 점수 합이 최대인 것을 동적계획으로 고른다 — 가중 최장 증가 부분열과 같은 꼴, O(후보²)
- 정류소마다 후보가 몇 개뿐이라 후보² 는 작다. 대안 15개가 같은 구간을 자주 공유하므로 같은 구간은 한 번만 계산한다

### 6.4 못 찾은 정류소
앞뒤로 위치가 정해진 정류소의 **선 위 거리 사이를 정류소 순번 비율로** 나눈 점에 둔다. 양 끝의 기준은 선의 시작과 끝이다. 추정 위치임을 표시해 DB 위치와 구분한다.

### 6.5 실측 → E10
5 OD 에서 버스 98.9%, 지하철 97.8% 를 DB 에서 찾았고 순서 역전은 없었다. 남은 추정 위치의 대부분이 §5.4 의 복합 이름 문제다.
나머지는 DB 에 없는 이름과 **역명 변경**(블랙박스가 옛 이름을 씀)이다.

---

## 7. 비용과 선호

### 7.1 택시
- **소요시간·거리·요금 = 자동차 경로 응답**(실시간 교통 반영). 택시 경로는 차량 경로와 같다고 본다
- 요금은 응답의 **택시 예상요금**에 따로 오는 **통행료**를 더한다(통행료는 승객이 실비로 낸다)
- 처음에는 공개 요금 산식(서울, 경기 시군별 3유형, 심야·시계외 할증)으로 오프라인 계산하려 했다. Tier 1 이 온라인 호출 전에 요금을 알아야 한다고 봤기 때문이다.
  응답값으로 바꾼 이유 → R13
  - 가지치기에는 시간만 있으면 된다. 요금은 검증(Tier 2)에서 받으면 되고, 자동차 경로는 10,000콜/일로 병목이 아니다(§2.3)
  - 응답은 실시간 교통과 시계외 할증을 반영한 값이다. 한 사례에서 시계외 할증을 넣은 산식 추정과 맞았다 → E5
  - 산식에는 공개 자료로 확정되지 않는 규칙이 남는다(경기 심야와 시계외의 결합 방식, 읍면 복합할증)
- 같은 OD 라도 실시간 교통에 따라 경로·요금이 달라진다 → E2

### 7.2 대중교통 요금
경로 요금은 값 하나로 오기도 하고 **범위(최소–최대)** 로 오기도 한다. 한 OD 의 대안 15개 중 3개가 범위였다 → E11.
정렬에서는 낮은 쪽을 쓰고, 택시와 비용을 비교할 때도 같은 규칙을 그대로 쓴다.

### 7.3 도보 거리
블랙박스는 환승 도보만 실제 경로로 주고 **첫·끝 도보는 주지 않는다**. 그래서 도보 거리를 이렇게 정의한다.
- 도보 거리 = 환승 도보 거리 + **O → 첫 승차점, 마지막 하차점 → D 의 직선거리**(추정)
- 첫·끝을 빼면 첫 정류장이 1 km 떨어진 경로도 도보 0으로 계산돼 비교가 왜곡된다
- 한 OD 의 대안 15개에서 이 값이 304 m – 1.4 km 로 갈렸다. 경로 선택을 가르는 차원이다

### 7.4 비교 기준과 제시
- **비교 차원**: 소요 시간 · 비용 · 이동 거리 · 환승 수 · 도보 거리
- **일반화 비용** `GC = 총 소요시간 + 총비용 / VOT` (VOT = 시간가치, 원/분)
- **Pareto(skyline) 필터**: (시간, 비용) 최소를 기본으로, 필요하면 환승·도보를 더한다
- **절감 효율을 원/분 하나로**: "택시 6,000원 추가 → 14분 절약 → 분당 429원". 사용자가 고른 선호 단계의 VOT 보다 비싸면 비추천으로 표시한다
- **구조 다양성**: 3–4개로 자르되 서로 다르게 — 전 구간 대중교통 · first-mile 택시 · mid-leg 택시 · 전 구간 택시

### 7.5 대기시간과 불확실성 (설계)
- 블랙박스는 출발 시각과 무관한 평균 소요시간만 준다. "택시 도착 10:23 → 다음 버스 10:31 → 8분 대기" 같은 시간 정합성을 스스로 판단해야 한다
- 실시간 도착 정보가 있으면 그것을 쓰고, 구할 수 없으면 배차간격으로 대기시간을 추정한다(대기시간 모델 — §3.2 의 배차간격/2 가 가장 단순한 형태).
  "다음 버스 14분 후"가 곧 택시 개입의 표적(D 유형)이다
- **연결 버퍼**: 택시 도착 예정 시각은 추정치다. `필요시간 = 택시 ETA × 1.2 + 3분` 같은 버퍼로 취약한 환승을 거른다
- 확장: 이동 중 재평가("지금 택시를 타면?")

### 7.6 선호 (설계)
사용자가 길찾기마다 **시간 중시 / 균형 / 비용 중시** 중 하나를 고른다. 단계마다 VOT 값 하나(원/분)를 두고 일반화 비용과 원/분 비추천 기준(§7.4)에 쓴다.
단계별 값은 정해야 한다.
처음에는 소규모 진술선호(SP) 조사로 시간·비용 조합 선택을 받아 다항로짓(MNL)으로 VOT 를 추정하려 했다. 범위를 줄여 사용자가 직접 고르게 했다 → R14

---

## 8. 평가 설계

- **기준선**: 대중교통 전용 최적 경로
- **지표**: 시간 절감(분) · 비용 증가(원) · 절감 효율(원/분) · Pareto 개선율 · **연계 경로가 비지배해인 OD 비율**(핵심)
- **OD 표본**: (도심↔도심 / 도심↔외곽 / 외곽↔외곽) × (피크 / 평시 / 심야) 층화. 편익이 가장 클 것으로 보이는 심야는 반드시 넣는다
- **ablation**: 앵커 전략별 쿼리 수 대비 해 품질(§3.5)
- **현장 검증**: 대표 경로 3–5개를 실제로 타고 시간을 잰다
- **사용자 연구**: 제시한 대안의 선택률·만족도
- **측정 원칙**: 블랙박스 응답은 저장하지 않고 파생 수치만 남긴다. 접지 단계의 실측(E7, E10)은 경기 외곽 5개 OD 를 공통 표본으로 썼다:
  동탄역 → 경기도청(광교) · 수지구청 → 서현역 · 운정신도시 → 대화역 · 다산신도시 → 구리시청 · 배곧신도시 → 안산 중앙역

---

## 9. 실험 상세

각 항목: 질문 · 방법 · 결과 · 결론.

### E1. 대중교통 경로 원천 조사
- **질문**: 상용 지도의 대중교통 길찾기를 프로그램으로 쓸 수 있나. 쓸 수 있다면 실험 규모를 버티나
- **처음 가정과 기각**: "공개 오픈 API 에는 대중교통 경로가 없다(자동차 경로만 있다)" — 틀렸다. 대중교통·도보·자전거 경로 조회가 심사 없이 개방돼 있었다 → R4

| 후보 | 대중교통 | 무료 한도 | 판정 |
|---|---|---|---|
| 카카오맵 | 제공 | 1,000콜/일 | 채택 |
| ODsay | 제공(좌표·요금 포함) | 30콜/일 | 보류 — 유료 전환 시 재검토 가치(R12) |
| TMAP 대중교통 | 제공 | 10콜/일 | 유료 교차검증 후보 |
| 네이버 | 자동차만 | — | 탈락 |
| 구글 | 한국 도보 경로 불가 | — | 탈락 |

- **결론**: 블랙박스 라우터로 쓸 수 있는 것은 하나뿐이고, 그 한도(1,000콜/일)가 알고리즘의 예산 제약이 된다(§2.3)

### E2. 블랙박스 응답 구조
- **방법**: 수원역 → 판교역 등 실제 OD 호출. 응답은 저장하지 않고 구조·건수만 확인했다
- **결과**
  - 1콜에 대안 15개(한 사례: 버스 3 · 지하철 4 · 혼합 8). 기준 경로 확보가 1콜로 끝난다
  - 구간 정류소 이름 목록은 **승차·중간·하차 전부**다. 같은 구간이 급행 8개, 일반 16개 — 급행은 정차역만 담는다 → 가정 "승·하차만" 기각(R5)
  - 환승 도보는 **실제 폴리라인**이다(5–18점, 2점 직선 0%) → 가정 "직선" 기각(R5). 단 **첫·끝 도보는 없다**(제공 범위 밖이라는 공식 답변)
  - 지하철 차량명은 승객 노선명 그대로, 버스 차량은 유형(마을·일반·직행 등) + 노선번호다
  - 정류소 좌표·ID, 노선 ID, 출발 시각, 대기시간이 없다
  - 이름 목록 양끝 = 안내문의 승·하차 이름: 28/28, 이후 5 OD 전부 일치(§5.1)
  - 자동차 경로에 택시 예상요금·통행료가 들어 있다. 같은 OD 에서 몇 시간 차이로 26.3 km · 28,000원 → 28.7 km · 29,300원(실시간 교통)
- **결론**: 알고리즘이 메워야 할 빈칸 목록이 확정됐다(§2.2)

### E3. 버스 정류소의 이중 등록과 상·하행
- **질문**: 서울·경기 정류소 중 같은 물리 정류소가 몇 번씩 등록돼 있나. 무엇으로 식별하나
- **결과**
  - 전국 227,065건. 서울 16,980 = 서울 BIS 12,956 + 경기 BIS 4,024 — 경기 BIS 가 등록한 서울 정류소의 89% 가 서울 BIS 정류소와 50 m 안에 있었다
  - 정류장번호 뒤 9자리가 같은 쌍 **5,405개**, 접두 조합은 전부 (경기, 서울) BIS. 쌍 간 거리 중앙 **5.8 m**, 50 m 이내 **96.7%**
  - 이름이 다른 716쌍은 주로 **이름 절단**(한쪽이 긴 이름의 앞부분)이다
  - 60 m 안의 근접 쌍 32,332개를 거리 × 이름 × 기관으로 나눴다
    - 가까운 거리의 "같은 이름 · 다른 기관" = 이중 등록
    - 15–60 m 의 "같은 이름 · 같은 기관" = 길 양쪽 **상·하행**

| 거리 | 이름 다름·기관 다름 | 이름 다름·기관 같음 | 이름 같음·기관 다름 | 이름 같음·기관 같음 |
|---|---|---|---|---|
| 0–5 m | 263 | 52 | **2,010** | 105 |
| 5–15 m | 433 | 591 | **1,900** | 4,093 |
| 15–30 m | 697 | 1,731 | 1,254 | **6,438** |
| 30–60 m | 1,701 | 3,977 | 1,556 | **5,531** |

  - 정제 결과(설계 단계 예측과 정확히 일치)
    - 대상 지역 레코드 55,047 → 쌍 5,405 = 근접 병합 5,228 + 원거리 병합 170 + 분리 7 → **49,649**
    - 서울·경기 안의 다른 BIS 693건은 번호 체계가 달라 제외했다
  - 상·하행 보존: 서로 다른 9자리 번호끼리의 15–60 m 동명 쌍이 11,547 → 11,347. 사라진 200쌍은 대표 좌표 이동·절단 이름 규칙 때문이고, 합쳐진 상·하행은 없다
    (병합 전 레코드로 세면 이중 등록 때문에 한 쌍이 최대 4번 세져 줄어든 것처럼 보인다)
- **결론**: 9자리 번호 = 물리 정류소 키(§4.1). 거리 기반 중복 제거는 상·하행을 뭉개므로 기각(R2)

### E4. 지역 판정
- **질문**: 정류소의 시군을 무엇으로 판정하나
- **결과**
  - 서울 BIS 레코드 12,956건은 소재지와 무관하게 전부 "서울"로 표기돼 있다. 좌표로는 **1,891곳이 경기**다(고양·성남·부천·광명 순)
  - 도시명 기준과 폴리곤 기준이 양방향으로 어긋났다. 경기 광주시로 표기된 광주광역시 좌표 8건도 있었다
  - 행정동 폴리곤을 시군 단위로 합쳐 판정했다: 서울 25 + 경기 31(일반구 → 시). 5.8만 점 판정에 1초 미만
  - 폴리곤 밖 점 19개는 모두 경기 서해안 매립지·항만(6 m – 1.9 km) → 2 km 흡수 규칙. "가장 가까운 대상 시군"이 아니라 **"가장 가까운 폴리곤이 대상 시군일 때"** 로 정해,
    인접 시도가 더 가까운 점을 끌어오지 않게 했다
  - 2023–2026 사이 일반구 신설·부활(부천·화성)과 인천 구 개편이 있었지만 **시 외곽 경계는 그대로**였다 — 경기를 시 단위로 두는 이유
- **결론**: 지역 = 좌표 + 행정경계(§4.2). 원천 도시명은 쓰지 않는다(R3)

### E5. 택시 요금 — 공개 산식과 자동차 경로 응답
- **질문**: 택시 요금을 공개 산식으로 오프라인 계산할 수 있나. 자동차 경로 응답의 택시 예상요금과 무엇이 다른가
- **산식 조사**
  - 서울: 기본 4,800원/1.6 km · 거리 100원/131 m · 시간 100원/30 s(15.72 km/h 미만 동시 병산) · 심야 +20%/+40%(시간대별) · 시계외 +20% · 복합 최대 60%
  - 경기: 기본 4,800원 공통. 시군 조례에 따라 기본거리 1.6 / 1.8 / 2.0 km 의 3유형(거리·시간 단가도 유형별) · 심야 23–04시 +30% ·
    시계외 +20%(사업구역 경계부터, 서울 진입 포함) · 통행료 실비. 통합사업구역 5곳은 서로 시계외가 아니다
  - 공개 자료로 확정되지 않는 규칙이 남았다: 경기 심야와 시계외의 결합(가산 50% vs 곱 56%), 읍면 복합할증, 요율 인상 논의
- **대조**: 수원역 → 판교역 26.3 km 에서 응답의 택시 예상요금은 28,000원이었다. 산식으로 표준형 주간 거리 요금은 23,600원이고, 시계외 +20% 면 28,320원이다
  → 응답은 시계외 할증을 넣은 값으로 보인다(한 사례 대조이고 계산 방식은 문서화돼 있지 않다). 통행료는 별도 필드로 온다
- **결론**: 택시 요금은 응답값을 쓴다(§7.1). 산식은 시군별 규칙이 복잡하고 확정되지 않는 부분이 남으며, 실시간 교통을 반영하지 못한다 → R13

### E6. 노선–정류소 순서 데이터
- **질문**: 전략 (b)의 입력인 노선별 정류소 순서·배차를 어디서 얻고, 우리 정류소 키와 어떻게 잇나
- **결과**
  - 전국 정류장 목록의 번호 = 국가 대중교통 정보의 정류소 ID. 경기 ID = 접두 + **경기 BIS 정류소 ID(9자리)** — 우리 키와 변환 없이 조인된다.
    서울도 접두 + 서울 정류소 ID(9자리)다 — 버스 순서표를 만들며 조인율로 확인했다(아래)
  - 경기: 기반정보 일괄 자료 한 번으로 노선·노선별 경유정류소(상·하행, 순번, 정류소 ID, 좌표)·요일 유형별 배차를 얻는다
  - 서울: 노선별 정류소(순번, 정류소 ID, ARS, 좌표) 파일. 상·하행 열은 없다. 노선 기본정보 파일은 노선명·ID 뿐이라 배차간격 대표값은 조회로만 얻는다
  - 시간대별 배차는 신청형 자료(GTFS)뿐이다
  - 도시철도 역 순서: 역번호로 정렬해 이으면 서울 지하철 계열은 인접역 최대 3.3 km 로 깔끔하다. 그러나 코레일 노선은 뒤섞인다(수원→서울숲 31 km,
    지평→서울역 59 km 점프) → 기각(R7). 노선정보 표준데이터의 **정거장 구성**(운행 순서대로 역 나열)을 원천으로 한다
  - 정거장 구성의 한계: 일부 노선이 오래됐다(서해선 일산 연장, 인천 1호선 검단 연장, 별내선 이전). 옛 역명(개명 전 이름)·약칭·오타가 있어
    역 파일로 보충하고 이름 대응표가 필요하다
- **도시철도 역 순서표 구축**: 정거장 구성을 역 DB 에 붙이며 원천의 결함을 보정표로 하나씩 고쳤다(26건)
  - 노선 식별: 원천 노선번호가 틀리거나 겹친다(분당선에 수인선 번호, 경부선·1호선 같은 번호) → 노선번호 + 노선명으로 고른다
  - 구분자(`,` · `+`)·따옴표·줄바꿈이 노선마다 다르고, 역번호 형식도 역 DB 와 다르다(`0405`/`405`, `D004`/`D04`, `2111`/`211-1`) → 역 매칭은 이름 키로
  - 목록 안에 지선이 섞여 있다(경부선의 광명·서동탄, 5호선 끝의 마천 지선, 경의중앙선의 서울역 지선) → 분기역부터 시작하는 별도 순서로 뗀다.
    지선 목록에는 분기역이 빠져 있다(2호선 성수·신도림) → 넣는다. 순환은 표시한다(2호선, 6호선 응암순환). 경춘선 상봉–광운대는 노선도대로 지선
  - 빠진 구간: 서해선 일산 연장, 별내선, 수인선이 안산과천선과 함께 쓰는 한대앞–오이도, 분당선 왕십리·청량리, 경의중앙선 용산–회기와 대곡,
    경춘선 청량리–망우(원천 목록이 신내부터)
  - GTX-A 는 노선정보에 없어 역 참조표로 만든다. 북부(운정중앙–서울역)와 남부(수서–동탄)는 이어지지 않은 두 순서다
  - 옛 역명·약칭: 신길온천 → 능길, 디엠시 → 디지털미디어시티
  - 운영 노선이 갈리는 곳(진접선–4호선–안산과천선, 분당선–수인선, 7호선 본선–부천 연장)은 잇는 역을 넣어 같은 노선군 안에서 이어지게 했다
- **결과**: 39개 순서(지선 포함, 22개 노선군). 역 DB 685역이 모두 어느 순서에 들어가고, 노선군마다 한 덩어리로 이어진다(GTX-A 만 북부·남부 두 덩어리).
  권역 밖 52역은 노선 끝에서만 빠진다. 순서 안의 역은 모두 역 DB 좌표를 가진다(원천 좌표 결함 3역은 E8 에서 고쳤다)
- **버스 노선–정류소 순서표 구축**: 서울 노선별 정류소(718 노선)와 경기 노선–정류소 일괄 자료(3,137 노선)를 합쳤다
  - 두 원천의 노선은 **겹치지 않는다** — 경기로 드나드는 서울 노선은 서울 원천에만, 경기 노선은 경기 원천에만 있다
  - 두 원천 모두 **순번 = 운행 순서**다. 경기는 상행 → 하행이 한 순번열에 이어지고, 첫 하행의 순번이 노선의 회차 순번과 모든 노선에서 같다.
    서울은 상·하행 표시 없이 회차를 지나 한 줄로 이어진다(한 노선이 금호역을 40번·68번에 두 번 지난다). 그래서 **같은 노선에서 순번이 작으면 상류** —
    방향 표시 없이 (b)의 상류·하류를 정한다
  - 키 조인은 정류소 ID 를 **자기 BIS 레코드**로 먼저 찾는다. 분리한 이중 등록 7쌍은 두 BIS 가 같은 번호를 서로 다른 정류소에 쓴 것이라, 번호만으로
    찾으면 다른 정류소에 붙는다(§4.1). 처음에는 고유 정류소 서울 98.5% · 경기 96.2% 가 DB 행에 붙었고, 원천 좌표와 DB 좌표 차이는 중앙 0 m · p99 24 m
    (300 m 넘게 다른 곳 8개 — 같은 번호가 옮겨졌거나 다른 정류소에 다시 쓰였다). 정류소 DB 를 최신 파일로 바꾼 뒤 **99.1% · 98.2%**, 300 m 넘는 곳 0 → E12
  - 못 붙은 것: 대상 밖 구간(인천·강원·충청, 인천공항)은 뺐다 — 순번은 비어도 순서는 그대로다. 목록 중간이 빠지는 노선 144개는 대상 밖을 지나거나 거기서 회차한다(인천 108 · 충청 22 · 강원 14).
    대상 안 970곳은 DB 원천 기준일(2025-10-31) 뒤에 생긴 정류소 791과 통과 노드 179였다. 원천 이름·좌표로 순서표에 남기지만 이름 매칭(§5, §6)에는
    걸리지 않는다 → 정류소 DB 를 최신 파일로 바꿔 97곳(96 은 서울 `(가상)` 통과 노드)으로 줄였다(E12)
  - 서울 노선 원천에 배(한강버스) 노선 3개가 버스처럼 섞여 있다 → 뺀다
  - 통과 노드 표기가 원천마다 다르다 — 경기 `(미정차)`, 서울 `(가상)`. 둘 다 통과 노드로 판정해 앵커 후보에서 뺀다
  - 인천 BIS 노선은 두 원천 어디에도 없다. 경기로 드나드는 인천 노선은 대상 지역 안에서도 빠진다
  - 결과: 3,852 노선 · 235,938행 · 고유 정류소 49,883. 정류소 DB 50,019곳 중 어느 노선에도 없는 곳은 233
  - (b) 1–4단계를 흉내 낸 한 사례(동탄역 → 경기도청(광교), 택시 도달은 직선 5 km 로 대신): D 400 m 안 정류소 11곳 → 노선 25개 → O 근처에 상류 정류소가 있는 노선 5개
- **결론**: 전략 (b)의 정적 노선 DB 를 키 변환 없이 구축했다. 버스는 서울·경기 두 원천을 합친 순서표, 도시철도는 정거장 구성 + 보정표로 만든 역 순서표다

### E7. 승·하차 매칭 정확도 — 경기 외곽 5 OD
- **방법**: 5 OD(§8)를 한 번씩 호출해 승·하차 매칭(§5)을 적용. OD 안에서 (종류, 이름 키, 기준점)이 같은 항목은 한 번만 셌다(대안 15개가 같은 정류소를 반복하므로)

| OD | 경로(버스/지하철/혼합) | 매칭 n | 매칭 | 모호 | 미매칭 | 오차 중앙 · p90 · 최대 (m) |
|---|---|---|---|---|---|---|
| 동탄 → 광교 | 8 / 2 / 5 | 27 | 24 | 2 | 1 | 11.6 · 105.8 · 159.8 |
| 수지 → 서현 | 6 / 3 / 6 | 23 | 22 | 1 | 0 | 15.8 · 97.1 · 104.3 |
| 운정 → 대화 | 11 / 0 / 4 | 15 | 10 | 3 | 2 | 6.7 · 17.3 · 18.2 |
| 다산 → 구리 | 10 / 1 / 4 | 30 | 29 | 1 | 0 | 7.1 · 12.0 · 265.3 |
| 배곧 → 안산 | 6 / 0 / 9 | 16 | 16 | 0 | 0 | 7.1 · 71.1 · 135.6 |

- **합계**: 버스 80 = 매칭 73 · 모호 7 · **미매칭 0**(발견률 100%). 지하철 31 = 28 · 0 · 3. 오차(108건) 중앙 **8.6 m** · p90 91.5 · 최대 265.3.
  모든 매칭이 key 수준이었다
- **원인 분류**
  - 지하철 미매칭 3 = 전부 GTX-A(원천 부재) → E8
  - 버스 모호 7 = 동명 정류소 2–4곳이 10 m 안(상·하행, 중앙차로·가로변). 1위와 차이 0.7–9.7 m → 방향 판정 과제(§5.4)
  - 첫 실측(수원역 → 판교역)의 버스 미매칭 2 = 블랙박스 이름에 출구 표기가 붙어 DB 의 짧은 이름(3.8 m · 10 m 옆)과 키가 달랐다.
    prefix 는 최소 길이에 막혔다 → 부분 이름 단계 필요(R9)

### E8. 도시철도 보강과 원천 결함
- **GTX-A**: 역 원천·노선 원천 모두에 없었다. 9역을 참조표로 넣었다(좌표는 공공 지오코딩, 운영사 자료로 이중 확인)
  - 신설역은 역 POI 가 없어 출구·승강장 POI 의 중심을 썼다. 주소 지오코딩은 넓은 역사 필지의 중심을 줘 100–500 m 벗어났다
  - 운영사 좌표 대비 오차 35–57 m
  - **실경로 재확인**: E7 의 미매칭 3건을 포함한 GTX-A 승·하차 5건이 모두 매칭됐다(북부 5.0 m · 1.4 m, 남부 106–171 m — 깊은 승강장과 지상 좌표의 차이)
- **원천 좌표 결함 3역 → 좌표 보정표로 고쳤다**
  - 다른 지역의 동명 역 좌표가 들어간 역: 경의중앙선 양원에 경북 봉화 양원역(영동선) 좌표 → 지역 판정에서 빠져 매칭 불가였다(190 km)
  - 옆 역 좌표가 들어간 역 2곳: 5호선 마곡 = 발산 좌표, 4호선 이촌 = 신용산 좌표 → 오차 ≈0.9–1.2 km 로 오매칭 가능했다
  - 다시 찍은 좌표: 공공 지오코딩의 장소 검색 '지하철역' POI. 지하 주소의 주소 지오코딩은 수 km 벗어나 쓰지 않았다.
    확인: 양원은 주소 지오코딩과 8 m, 이촌은 같은 역 경원선 행과 58 m, 마곡은 같은 이름 지하철역 POI 3곳이 100 m 안
- **원천에 잘못 들어간 노선 행**: 경의중앙선 광운대(경의중앙선은 광운대에 서지 않는다) → 뺐다. 원천 행 보정은 (노선, 역번호) + 역 이름이 모두 맞아야
  적용되므로, 원천이 바뀌면 보정이 조용히 엉뚱한 행에 붙지 않고 멈춘다
- **같은 이름인데 300 m 넘게 떨어진 실제 환승역**(서울역·잠실·노원)은 노선 필터가 없으면 다른 역으로 보여 모호가 될 수 있다

### E9. 경유지 파라미터 부재
- **결과**: 대중교통 경로 조회의 요청 파라미터는 출발·도착 좌표·명칭·좌표계뿐이고 **경유지가 없다**. 같은 계열의 도보·자전거 조회에는 경유지가 있다
- **의미**: 연계 경로는 **구간을 따로 호출해 이어 붙인다** — 택시 O→P + 대중교통 P→D. §3.2 의 `1 + N` 설계가 이 전제와 맞는다. mid-leg(C)는 한 경로를 두 번 이상 나눠 불러야 해 예산이 더 든다

### E10. 경로 위 모든 정류소 위치
- **방법**: E7 과 같은 5 OD. OD 안에서 똑같은 구간은 한 번만 셌다(구간 101개)

| | 정류소 | DB 위치 | 수준 | 추정 위치 | DB 위치의 선까지 거리 |
|---|---|---|---|---|---|
| 버스 | 827 | **818 (98.9%)** | key 807 · prefix 11 | 9 | 중앙 4.2 · p90 9.5 · 최대 89.7 m |
| 지하철 | 92 | **90 (97.8%)** | key 90 | 2 | 중앙 4.5 · p90 104.3 · 최대 265.3 m |

- 정류소 순서 역전(선 위 거리가 앞 정류소보다 30 m 넘게 뒤로) **0건** — 잘못 붙은 같은 이름 정류소가 없다는 간접 증거
- 추정 11건: 버스 9건 중 7건이 **복합 이름**(블랙박스 이름 `A.B`, DB 는 한쪽만), 나머지는 DB 에 없는 이름. 지하철 2건은 **역명 변경**(블랙박스가 옛 이름) → 별칭 한 줄로 풀린다
- 탐욕 선택(1안)의 실패는 합성 사례로 재현했다. 되돌아오는 구간에서 같은 이름 두 번째가 첫 통과 지점에 붙어, 뒤따르는 추정 위치가 선 한가운데로 갔다.
  순서 보존 정렬(2안)은 되돌아오는 구간 위에 놓았다 → R8

### E11. 대중교통 범위 요금
- 동탄역 → 경기도청(광교) 대안 15개 중 3개가 값 하나 대신 **범위(1,850–3,200원)** 로 왔다. 가정 "요금은 값 하나" 기각 → R6
- 정렬과 비용 비교에서는 범위의 낮은 쪽을 쓴다(§7.2)

### E12. 정류소 DB 갱신 — 두 BIS 의 최신 정류소 파일
- **질문**: 연 1회 갱신되는 전국 원본으로 만든 DB 가 노선 원천(월·일 단위 갱신)과 얼마나 어긋나고, 최신 파일로 바꾸면 매칭이 나아지나
- **계기**: 노선 순서표를 만들 때 노선 위 대상 안 정류소 970곳(새 정류소 791 · 통과 노드 179)이 DB 에 없었다(E6)
- **원천**: 경기 = 기반정보 일괄 자료의 정류소 파일(경기 BIS 가 등록한 전부 — 서울·인천 소재 포함). 서울 = 서울 정류소 위치 파일 — **서울 소재만** 담는다
  (경계에 붙은 경기 소재 17곳 말고는 모두 서울)
- **교체 규칙**: 두 파일에 있는 레코드는 최신 값을 쓴다. 최신 파일에서 빠진 레코드는 폐지로 본다 — 단 서울 파일은 서울 소재만 담으므로,
  서울 BIS 가 등록한 서울 밖 정류소(1,880)는 전국 원본 것을 둔다. 배 선착장(한강버스)은 뺀다. 그 뒤 쌍 판정·지역 판정은 §4 규칙 그대로다
- **결과**
  - 경기 BIS: 둘 다 있음 45,037(이름 키 바뀜 689 · 50 m 넘게 옮김 122) · 새 937 · 폐지 630. 서울 BIS: 둘 다 11,022(446 · 64) · 새 206 · 폐지 54
  - DB 에서 빠진 519곳 중 지금 노선이 지나는 곳은 2곳뿐이다 — 폐지 판정이 맞다는 근거
  - 서울 정류소 파일에 **좌표 오류 4곳**(0.98–14.8 km): 전국 원본과 서울 노선 파일은 서로 0 m 로 같은데 정류소 파일만 다르다 → 좌표 보정표로 고쳤다.
    50–300 m 차이 63곳은 노선 파일이 반씩 편을 들어(정류소 파일 쪽 33 · 전국 원본 쪽 29) 판단하지 않았다
  - DB 49,649 → **50,019**. 같은 쌍 판정 규칙으로 근접 병합 5,443 · 원거리 병합 169 · 분리 17쌍. 분리가 7 → 17 로 늘었다 — 두 BIS 가 한 정류소의
    이름·좌표를 따로 고쳐 50 m 를 넘은 쌍이고, 일부는 같은 정류소로 보인다(검토 대상). 병합 뒤 새로 생긴 상·하행 쌍은 1개
  - 노선 순서표: 고유 정류소 서울 **99.1%** · 경기 **98.2%**(나머지는 모두 대상 밖)가 DB 에 붙는다. DB 밖 대상 안 정류소 970 → 97(96 은 서울 `(가상)` 통과 노드).
    노선 원천과 이름 키가 다른 정류소 1,201 → 112, 300 m 넘게 다른 좌표 8 → 0
  - 5 OD 재측정(같은 날 응답에 옛·새 DB 를 똑같이 적용, OD 좌표는 E7 과 비슷한 곳): 버스 승·하차 미매칭 1 → 0, 매칭 수준 prefix 2 → 0(모두 key),
    경로 위 버스 정류소의 DB 위치 845/857 → **855/857**. 지하철은 같다
- **결론**: 정류소 DB 는 두 BIS 의 최신 정류소 파일로 만든다. 전국 원본은 서울 파일이 담지 않는 서울 BIS 의 서울 밖 레코드만 채운다 → §4.1, R15

---

## 부록 A. 기각한 설계·가설

| ID | 기각한 것 | 이유 | 근거 |
|---|---|---|---|
| R1 | 앵커 전략 (d) 택시·대중교통 등시선 만남 | 앵커를 이산화하지 않으면 쿼리가 폭발한다. (b)가 그 노선 단위 이산화 | §3.4 |
| R2 | 거리 기반 정류소 중복 제거 | 길 양쪽 상·하행 쌍을 뭉갠다 | E3 |
| R3 | 원천 도시명으로 지역 판정 | 도시명 = BIS 운영 주체. 양방향 오분류 | E4 |
| R4 | 가정 "공개 대중교통 경로 API 는 없다" | 틀렸다 — 무료 1,000콜/일로 개방 | E1 |
| R5 | 가정 "정류소 목록은 승·하차만, 도보는 직선" | 목록은 전부, 환승 도보는 실제 경로 | E2 |
| R6 | 가정 "요금은 값 하나" | 범위 요금이 있다 | E11 |
| R7 | 도시철도 역 순서 = 역번호 정렬 | 코레일 노선에서 수십 km 점프 | E6 |
| R8 | 중간 정류소를 정류소별로 따로 고르기 | 되돌아오는 노선에서 순서가 깨진다 → 순서 보존 동적계획 | §6.3, E10 |
| R9 | 매칭에서 부분 이름 단계를 "근거 없음"으로 뺀 판단 | 실측에서 남은 미매칭의 주원인이 복합 이름이었다 | E7, E10 |
| R10 | 서울 도시철도 운영사 역 자료 | 좌표가 없고 서울 시내 노선의 절반 이상이 빠진다 → 전국 표준데이터 | §4.3 |
| R11 | 상용 지도의 장소 검색으로 정류소 DB 지오코딩 | 결과 저장 금지 → 영속 DB 를 만들 수 없다 → 공공데이터 + 공공 지오코딩 | §2.2 |
| R12 | 좌표·요금을 주는 다른 대중교통 라우터 | 무료 한도 30콜/일로 예산이 안 된다. 유료 전환 시 재검토 | E1 |
| R13 | 택시 요금을 공개 산식으로 오프라인 계산 | 가지치기에는 시간만 필요하고, 요금은 검증 단계의 자동차 경로 응답이 준다(10,000콜/일, 병목 아님). 응답이 실시간 교통·시계외를 반영하고, 산식에는 확정 안 되는 규칙이 남는다 | §7.1, E5 |
| R14 | 진술선호 조사 + 다항로짓으로 VOT 추정 | 범위 축소 — 사용자가 길찾기마다 시간 중시 / 균형 / 비용 중시 중 하나를 고른다 | §7.6 |
| R15 | 정류소 DB 를 전국 원본(연 1회 갱신)만으로 만들기 | 노선 원천보다 오래돼 노선 위 대상 안 정류소 970곳이 DB 밖이었고, 이름이 바뀐 정류소가 이름 매칭을 흔든다 → 두 BIS 의 최신 정류소 파일로 교체 | §4.1, E12 |

## 부록 B. 용어

| 용어 | 뜻 |
|---|---|
| 앵커 | 택시와 대중교통이 만나는 정류소·역 |
| 기준 경로 | 블랙박스가 준 대중교통 전용 경로(대안 최대 15개) |
| 블랙박스 | 내부를 모르고 입출력만 쓰는 상용 대중교통 라우터 |
| 쿼리 예산 | 하루 허용 호출 수(대중교통 1,000). 요청당 `1 + N` |
| Tier 1 / Tier 2 | 오프라인 무료 탐색 / 온라인 쿼터 검증 |
| 접지(grounding) | 블랙박스의 정류소 이름을 DB 좌표에 붙이는 일 |
| 이름 수준 | key(정규화 키 일치) > alias(다른 이름 일치) > prefix(앞부분 일치, 4글자 이상) |
| 발견률 / 매칭률 | (매칭 + 모호) / 대상, 매칭 / 대상 |
| 모호 | 1위와 다른 실체가 버스 10 m · 지하철 150 m 안에 있는 매칭 |
| 복도 | 구간 폴리라인에서 일정 거리(버스 100 m · 지하철 500 m) 안의 띠 |
| 일반화 비용(GC) | 총 소요시간 + 총비용 / VOT |
| VOT | 시간가치(원/분) |
| 시계외 할증 | 택시가 사업구역(시군) 밖으로 나갈 때 붙는 할증 |
